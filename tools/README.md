# Flight Alert Tools

Small utilities for generating app assets and collecting repeatable performance evidence.

## Folders

- `map/` - source-data generator for bundled local map reference labels.
- `perf/` - Android device performance runners, frame summaries, workbook rebuilds, and trace correlation.

Tool output belongs in ignored folders such as `tools/perf/out/` or `tmp/`, not in git.
