# Flight Alert Agent Rules

Flight Alert is a drone situational-awareness app. Refactors must preserve the current user experience and styling unless a visible difference fixes a real bug.

These rules are ordered by priority. If two rules conflict, follow the higher-priority rule.

## 1. No Pretending

Never fake aircraft, map, route, photo, alert, altitude, location, or source data.

If real data is unavailable, show `Unavailable`, hide the feature, disable the control, or label uncertainty clearly. Never use pregenerated aircraft/location/path data as if it were live.

## 2. Creative Reinvention, Never The Axe

Preserve the visible and user-facing result first, then reinvent the implementation underneath until it is fast.

When performance is bad, solve the expensive work directly with profiling, caching, batching, precomputation, data-structure changes, request scheduling, retained real imagery/data, or another smarter equivalent. Do not win frame time by removing, hiding, delaying, shrinking, darkening, simplifying, skipping, or approximating accepted visuals, features, information density, layer content, aircraft morphing, map labels, tile transitions, details, photos, or alerts unless the user explicitly asks for that tradeoff.

If a protected visual or feature is a real bottleneck, replace its implementation with one that produces the same accepted pixels, information, timing honesty, and interaction behavior faster. The axe is not an optimizer.

## 3. Real Sources Only

Map tiles must come from real map providers such as OpenStreetMap street tiles or real satellite imagery.

Aircraft traffic must come from live aircraft APIs. Flight paths must come from real trace/path APIs after an aircraft is selected. Do not accumulate app-session aircraft positions and call that a real flight path.

## 4. Safety Bias

Accuracy matters more than impressive visuals. Vertical separation is critical.

If altitude, location, source freshness, or feed data is missing, do not claim an aircraft is safe or inside/outside an alert volume unless the calculation is supported.

## 5. Alerts

Alerts are based on a 3D volume around the user: horizontal distance plus vertical separation.

Notify when an aircraft enters or leaves the configured alert volume. Do not spam repeat notifications for the same aircraft every interval. The persistent extreme-priority notification means non-clearable while active, self-clearing when the extreme-priority list empties, and text-updatable while active; it should only beep or buzz on the first transition from no extreme-priority aircraft to at least one extreme-priority aircraft.

## 6. UI Honesty

Anything that looks like a button must be interactable.

Unavailable or unverified features must be hidden, disabled, or labeled honestly. Do not show fake radar graphics or fake aviation overlays. Attribution can move later, but do not misrepresent providers.

## 7. Layout Quality

No text clipping, hidden labels, or strange overlaps.

Support portrait, landscape, folding devices, resizing, and emulator hardware input. Text should stay physically consistent unless constraints require smart adaptation. Back should close overlays/screens before leaving the app.

## 8. Map Interaction

Panning, pinch zoom, wheel zoom, and keyboard zoom should feel smooth.

Aircraft sprites may move smoothly every frame using interpolation from speed, heading, and last report. Do not request aircraft positions every frame. Sprite/dot morphing should be smooth and should not pop through the wrong intermediate shape.

## 9. Flight Paths

Only show the path button when a real usable path was retrieved.

The path must represent the selected aircraft's actual current flight/trace, not stale old legs. If the trace endpoint stops before the live sprite position, extend the visible trail to the current live position only when the live report is fresh. Fix path-jump root causes instead of hiding bugs with arbitrary implausible-jump filters.

## 10. Aircraft Details And Photos

Try exact aircraft photos first from real aircraft-photo sources.

If exact photo lookup fails, a representative same make/model photo is acceptable only with a clear "not this exact aircraft" note. Search-engine fallback photos must be labeled investigable and include source/proof view buttons. Claimed make/model/owner data must come from official or documented sources.

## 11. Military Handling

Only show military-specific stats if the aircraft is actually tagged military.

Military origin/base claims require real flight-origin and aerodrome/source data. Otherwise show unavailable or non-military status honestly.

## Styling Preservation

Keep the existing styling, layout feel, visual hierarchy, theme behavior, and interaction rhythm.

When comparing `test` to `master`, the app should look and behave the same except where `test` intentionally fixes an obvious bug or honesty issue. Do not redesign during refactors.

Past visual fixes are protected behavior. Aircraft morphing, icon sizing, dot outlines, satellite tile transitions, layer readability, aircraft details/photo loading states, and other accepted UI fixes must not be optimized out, hidden, gated away, or approximated unless motion testing proves the new implementation is visually identical while preserving the same information.

## 12. Codebase / Repo Discipline

Use Kotlin and work directly in the Android Studio project structure.

Keep one real project: one manifest, one root Gradle setup, and no duplicate generated/source projects. Do not commit Android Studio local machine-state churn, build outputs, temporary screenshots, secrets, `local.properties`, generated junk, or stale comparison artifacts.

## 13. Testing

Build and lint before release. Verify visual/UI changes on a connected device or emulator and use screenshots for comparison when useful.

Use emulators for visual inconsistency, layout, aspect-ratio, clipping, and theme checks. Use physical devices for optimization, timing, responsiveness, smoothness, and performance tests.

For map performance work, automated and manual panning/zooming tests must exercise dense aircraft regions around a fresh random major-major city center with major airports/corridors in the United States, Europe, or a Mexico City-class North American megacity on every pass, not open ocean, empty polar regions, unintended rural/upstate drift, upper Canada, smaller fallback metros, or the launcher/home screen. Target cities should be in the same broad population/traffic class as Los Angeles, New York, London, Paris, or Mexico City. Include quick anchored pinch gestures, smooth slow anchored pinch gestures, horizontal pans, vertical pans, diagonal pans, and combined pinch-while-panning gestures over visible aircraft at multiple zoom levels. Pinch tests must keep the target city/traffic focus anchored so zooming back in returns to the same target instead of drifting northeast. Combined zoom+pan tests must cover horizontal, vertical, diagonal down, and diagonal up directions at the current fast stress speed and at roughly half speed for root-cause discovery.

For visual completeness checks, compare against the latest valid feature-complete UI state, including accepted Artist/frontend edits when they exist. Do not compare a post-Artist optimizer build to an older pre-Artist look unless the user explicitly asks for that baseline. Capture the app at rest, right as movement starts, and during active movement so panning/zooming-only flashes, outline changes, tile flicker, or morph jumps are treated as regressions.

Every jank/frame-time test pass must include visual artifacts from the same run when practical. Save screenshots or sampled frames with the same city, zoom, map-source, restricted-layer, and gesture label as the perf report, including at least rest, movement start, and active movement. Treat a lower jank percentage as invalid if those artifacts show darker/smaller aircraft, line traces, sprite-size jumps, missing outlines, missing aircraft information, map blanking, satellite flicker, or any other degradation from the accepted look.

Every optimizer test result must report frame timing and jank evidence, not only subjective smoothness or screenshots. For each meaningful pass, capture or summarize `gfxinfo`/FrameTimeline data when available, including Android jank percentage, 120Hz present-drop or latency-miss percentage, p50/p95/p99 frame timing, and an FPS-equivalent or present cadence note. If a visual-only diagnostic is unavoidable, label it visual-only and do not use it to claim performance progress.

During device tests, accept runtime permission prompts that are specifically requested by the Flight Alert app so permission dialogs do not invalidate smoothness or visual captures.

When maintaining a progress checklist or progress screen, only remove, collapse, or clean up items that are completed and verified. Active, pending, blocked, or deferred work must stay visible so required tasks such as satellite tile loading, details/photo snappiness, morph verification, and full settings-matrix retests survive context resets.

Major workflow rule: right before any likely context compaction, context cleanup, handoff, or long pause, push the active progress checklist into this file using the exact item wording. Immediately after resuming from compaction, pull the checklist wording back from this file before continuing. Do not rename, paraphrase, merge, or soften progress items across compaction unless the user explicitly asks for that wording change.

Optimizer memory rule: keep the `Optimizer Tried And Denied` and `Optimizer Successes To Preserve` sections current. After each meaningful optimization attempt, add or update an entry if the result teaches what style of optimization to avoid, what implementation must be preserved, or what measurement/visual evidence changed the direction. These sections are part of the compaction handoff and must be read before making new optimization choices.

For smooth-map optimization work, aircraft dot-to-symbol morph and dot sizing must be governed smoothly by zoom, not by visible aircraft density. Density may choose batching and bounded symbol counts for performance, but it must not lock dots small, skip the morph, or cause abrupt dot-to-aircraft jumps. Validate this with smooth two-finger pinch motion captures or sampled frames that cover a little below the transition, the full dot-to-plane transition, and a little above it, in both zoom directions. Still screenshots alone are not enough for morph approval.

No aircraft sprite, dot, outline, or symbol gate may create a sudden apparent size jump anywhere in the zoom range. Transition starts, outline introduction, dot fading, symbol entry, and selected/priority styling must be continuous curves tied to zoom/appearance state, and motion tests should explicitly watch for one-frame outline flashes or size pops at those gates.

Aircraft overlay reuse, batching, and viewport-cache optimizations must never reduce the visible representation of aircraft during motion. If the accepted view at a zoom level renders an aircraft symbol, quick panning, edge-of-screen movement, cache reuse, and transition-state drawing must still render the same symbol/morph rather than falling back to a dot, popping in later, or changing the amount of aircraft information on screen.

Failed optimization note: the experimental shared vertex/line batched aircraft-symbol renderer caused visible line/traces while zooming and made some aircraft sprites look dark or too small. Do not reintroduce that renderer or any equivalent visual shortcut unless motion captures prove exact parity with the accepted aircraft look during rest, movement start, and active zoom/pan.

## Optimizer Tried And Denied

These optimization attempts were rejected by the user or by visual/perf evidence. Do not repeat them after compaction unless the implementation is fundamentally different and visual parity is proven before it reaches the user.

- Low-zoom aircraft shadow batching with round-cap `drawPoints`: rejected/reverted because `iter212-shadowbatch-country-zoom-street-noscreens` worsened the street continent zoom run to PresentDrop120Pct 37.29% versus `iter209-maskres0375-country-zoom-street-noscreens` at 16.81%, and it did not solve the active pinch bottleneck. Do not repeat unless fundamentally redesigned and proven with same-city visual/perf evidence.
- Low-zoom pre-rotated aircraft alpha masks with destination-rect drawing: rejected/reverted because `iter215-rotmask-country-zoom-street-visual` did not improve 120Hz present cadence versus `iter213` and worsened the country-band Android jank to 98/652 frames (15.03%). Logs also showed the active bottleneck is not only canvas rotation: one bad continent frame was dense-dot work (`symbols=0 dots=9503 traffic=24.84ms`) and later frames still had direct-symbol spikes. Do not repeat this as a narrow mask-rotation optimization; solve the shared dense traffic pipeline instead.
- Screen-space transformed dense-dot drawing: rejected/reverted because `iter216-screenspace-dots-country-zoom-street-visual` worsened the street continent zoom PresentDrop120Pct to 44.83% and Android jank to 83/267 frames (31.09%), despite a better country-band Android-jank number. It preserved the dot/outline look in sampled frames but moved CPU/GPU cost rather than fixing it. Do not replace the transformed-canvas dot path with per-point screen-space transforms as tested.
- Textured `drawVertices` aircraft-symbol batching with fill/stroke/shadow atlases: rejected/reverted because `iter225-texturedbatch-street-visual` did not improve the dense street continent zoom run enough (PresentDrop120Pct 29.31%, Android jank 65/285 frames) and active screenshots showed visibly wrong oversized/dark aircraft sprites instead of the accepted symbol look. Do not repeat this batch-atlas approach unless it is fundamentally redesigned and visual parity is proven before performance claims.
- Reducing the tiniest aircraft symbol mask resolution from 0.375x to 0.25x: rejected/reverted because `iter226-maskres025-street-visual` worsened the dense street continent zoom run to PresentDrop120Pct 32.48% and Android jank 100/363 frames (27.55%). Keep the 0.375x floor unless a different renderer proves better with same-run visual and frame evidence.
- Exact precomposed ARGB aircraft-symbol sprite cache for the direct Canvas path: rejected/reverted because `iter229-symbolsprite-street-video` worsened the street continent run to PresentDrop120Pct 50.85% and Android jank 98/283 frames (34.63%) versus `iter228-directloop-street-video` at PresentDrop120Pct 33.62% and 88/335 frames (26.27%). Do not repeat a per-color/per-alpha sprite-cache variant unless it is redesigned to avoid cache churn and proves same-run visual parity plus better frame data.
- Redesigned exact precomposed ARGB aircraft-symbol sprite cache for the direct Canvas path: rejected/reverted again because `iter277-directsymbols-spritecache-fixednyc-full` worsened the fixed New York full-product satellite transition run to PresentDrop120Pct 94.12% and Android jank 255/582 frames (43.81%), with p95 app frame 300ms, p99 700ms, and max 1000ms. Do not repeat this direct ARGB sprite-cache approach; solve the symbol scale-miss bottleneck through a different root cause path.
- Direct-symbol cull math hoisting inside `draw_aircraft_symbols`: rejected/reverted because the fixed New York full-product satellite transition repeat runs `iter282-directsymbol-cullframe-fixednyc-full-novideo` and `iter283-directsymbol-cullframe-repeat-fixednyc-full-novideo` both reported PresentDrop120Pct 100% and Android jank around 31-32%, versus the protected `iter280` satellite-blend baseline at PresentDrop120Pct 19.49% and Android jank 18.12%. Even same-pixel-looking CPU micro-optimizations must prove better on FrameTimeline before being kept.
- Early stable-appearance retained aircraft-symbol overlay prewarm/cache: rejected/reverted because `iter290-street-earlystablecache-fixedchicago-full-novideo` worsened the active weak street continent band to PresentDrop120Pct 46.55% versus the immediately prior `iter289-street-rasterhandoff-fixedchicago-full-novideo` at 43.48%, and still left the first active frame at `symbolCache=miss reason=active_coverage direct=5366 traffic=209.33ms`. The country band improved slightly, but the continent-scale bottleneck got worse and the root `active_coverage`/`scale` direct-symbol burst remained. Do not repeat this partial-appearance cache-key/idle-refresh approach unless it is fundamentally redesigned.
- Shared vertex/line batched aircraft-symbol renderer: rejected because zooming produced visible line/traces and some aircraft looked dark or too small.
- Density-driven dot sizing or density-locked morphing: rejected because dot-to-aircraft morph must be governed smoothly by zoom, not traffic density.
- Outline draw gates for dense dots: rejected because they caused outline flashes and apparent sprite-size jumps; outlines must look like the accepted baseline throughout motion.
- Center-ranked/capped dense symbol overlay during motion: rejected because edge aircraft and quick-pan aircraft that should be aircraft symbols remained dots or popped into symbols later. Symbol/cache optimizations must preserve the same visible aircraft representation.
- Visual-performance tradeoffs that hide, drop, delay, darken, shrink, or simplify aircraft symbols: rejected as not a smart optimization. Optimize the same pixels with caching, batching, precomputation, or data-structure changes instead.
- Screen-space satellite interim snapshot overlay: rejected/reverted because `iter37-satellite-snapshot-skipchrome` worsened the country satellite skip-chrome run to 318/486 Android janky frames (65.43%, PresentDrop120Pct 76.47%) and the video showed ugly satellite fallback frames with label-colored/blankish imagery during zoom. Do not revive this as a one-bitmap screen snapshot unless it is fundamentally redesigned and video proves exact satellite visual continuity before perf claims.
- Lower-zoom-biased retained satellite overlay selection during zoom-out: rejected/reverted because `iter40-satellite-tileonly-lowerzoom` worsened the tile-only diagnostic versus `iter39` (continent PresentDrop120Pct 15.13% vs 3.42%, country 32.77% vs 13.45%). Keep the child-fallback zoom-out suppression, but do not bias the coherent retained overlay selector downward as designed.
- Active symbol-overlay background recording during gesture coverage misses: rejected/reverted because same-city Chicago full satellite `iter42-symbol-active-refresh-satellite-full` worsened continent PresentDrop120Pct to 44.07% versus reverted baseline `iter44-symbol-baseline-satellite-full` at 31.93%. It did not remove the `active_coverage`/`scale` direct-symbol bursts and risked competing with gesture rendering.
- Cached-morph mask construction plus background-priority symbol overlay recording: rejected/reverted because same-city Chicago full satellite `iter43-symbol-maskcache-satellite-full` worsened continent PresentDrop120Pct to 47.06% and country to 25.21% versus reverted baseline `iter44-symbol-baseline-satellite-full` at 31.93% and 3.39%. Do not switch mask creation to the cached morph path or alter symbol overlay recording priority as tested unless a fundamentally different implementation proves better with same-city visual and frame evidence.
- Matrix-based direct symbol mask drawing: rejected/reverted because `iter46-symbol-matrixdraw-satellite-full` worsened the Chicago full satellite continent run to PresentDrop120Pct 48.31% versus reverted baseline `iter44-symbol-baseline-satellite-full` at 31.93%. The existing canvas save/translate/scale/rotate path is faster on this device/driver for the accepted ALPHA mask sprites.
- Disabling `symbol_mask_paint.isFilterBitmap` for direct symbol masks: rejected/reverted because `iter47-symbol-nofilter-satellite-full` worsened Chicago full satellite continent PresentDrop120Pct to 46.22% and country to 72.27% versus reverted baseline `iter44-symbol-baseline-satellite-full` at 31.93% and 3.39%. Keep direct symbol mask filtering enabled unless a different implementation proves both visual parity and better frame data.
- Idle retained symbol overlay reuse using a sub-pixel position-signature guard: rejected/reverted because same-scenario Chicago satellite country-scale `iter49-idlepositioncache-chicago-z54-satellite-off-diagonal` worsened PresentDrop120Pct to 55.75% and Android jank to 19.61% versus diagnostic baseline `iter48-trafficdetail-chicago-z54-satellite-off-diagonal` at 46.96% and 13.36%. The cache rarely became usable before idle direct-symbol frames and the added signature/refresh work raised cacheAttempt/direct costs. Do not repeat this approach unless it is fundamentally redesigned and proves visual parity plus better same-city frame data.
- Shared frame timestamp for direct aircraft symbol motion/appearance: rejected/reverted because same-scenario Chicago satellite country-scale `iter50-frametimestamp-chicago-z54-satellite-off-diagonal` worsened PresentDrop120Pct to 56.14% and Android jank to 17.16% versus diagnostic baseline `iter48-trafficdetail-chicago-z54-satellite-off-diagonal` at 46.96% and 13.36%. It did not reduce idle direct-symbol cost enough to matter and the focused same-city evidence moved the wrong way.
- Oversized 560dp dense symbol/dot cache padding: rejected because it inflated dense symbol work from roughly 950 aircraft to roughly 1700-1800 aircraft and worsened physical-phone p95 frame time to 77ms.
- Increasing `DENSE_SYMBOL_CACHE_MAX_REUSE_DP` from 180dp to 320dp or 240dp: rejected because it inflated the active symbol list to roughly 1271 aircraft and worsened p95 to about 20ms without enough benefit. Keep the 180dp dense symbol cache reuse padding unless a smarter edge-stability method is proven with motion visuals and real-phone frame data.
- Matrix-based direct cached-symbol bitmap transform: rejected/reverted because the New York City zoom 8.4 street/off shell-pan run stayed at 164/393 janky frames (41.73%, p50 30ms, p95 57ms), matching the older direct-draw bottleneck rather than improving it.
- Exact-color precomposed aircraft symbol sprite cache: rejected/reverted because altitude/color variability caused cache churn and the physical-phone New York City zoom 8.4 street/off shell-pan run worsened to 60/89 janky frames (67.42%, p50 150ms, p95 200ms) with 40-55ms traffic frames.
- Clearing `drag_started` on `ACTION_UP` is a real state cleanup fix, but it exposed expensive idle direct symbol rendering. Do not restore the old accidental "drag active forever" behavior as a performance trick.
- Removing idle prewarm entirely is rejected because the first active bitmap cache build caused a roughly 608ms traffic frame and 55/433 janky frames (12.70%) in `adaptive-street-on-nyc-z84-shellpan-20260614`.
- Calling `Bitmap.prepareToDraw()` on the retained aircraft-symbol overlay is rejected for now because it worsened the first active cache spike. Removing it improved that spike from roughly 608ms to roughly 402ms in `noprepare-street-on-nyc-z84-shellpan-20260614`, though the path still needs more work.
- One-time cold idle prewarm plus no `prepareToDraw()` is not complete enough by itself: `oneprewarm-street-on-nyc-z84-shellpan-20260614` avoided the first active 400ms spike and active pan frames were smooth, but post-gesture idle direct frames still caused 28/511 janky frames (5.48%, p95 34ms), and the motion-active screenshot showed dots where accepted aircraft symbols should be visible at zoom 8.4.
- RenderNode display-list aircraft-symbol overlay cache: rejected/reverted because `rendernode-street-on-nyc-z84-shellpan-20260614-120745` preserved the concept but worsened the frame distribution to 30/473 janky frames (6.34%, p50 14ms, p95 19ms, p99 46ms), worse than the bitmap overlay with the visual-readiness guard.
- Motion-bounded post-interaction cache hold using projected aircraft speed/drift thresholds: rejected/reverted because `motionhold-street-on-nyc-z84-shellpan-20260614-121847` worsened the physical-phone New York City zoom 8.4 street/on shell-pan run to 42/541 janky frames (7.76%, p95 40ms, p99 53ms). Do not reintroduce bounded cache-hold as currently designed.
- Per-frame precomputed symbol-mask arrays/context: rejected/reverted because `precomputedmasks-trafficonly-street-on-nyc-z84-shellpan-20260614-1242` worsened the physical-phone traffic-only run to 32/521 janky frames (6.14%, p95 32ms, p99 44ms) and introduced large cold idle direct-render spikes around 320-363ms. Do not pre-warm every symbol mask type per draw as a blanket optimization.
- Direct cached-vector aircraft-symbol drawing instead of alpha-mask bitmap drawing: rejected/reverted because `vectorpath-trafficonly-nyc-z84-shellpan-20260614-1350` worsened the physical-phone New York City zoom 8.4 traffic-only shell-pan run from 24/504 janky frames (4.76%, p50 14ms, p95 29ms) with alpha masks to 29/501 janky frames (5.79%, p50 19ms, p95 34ms). Keep the alpha-mask symbol path unless a fundamentally different vector approach proves faster with matching motion screenshots.
- Aviation restricted-layer zoom LOD using mid-zoom/low-zoom ring subsets: rejected/reverted because `maponly-street-on-nyc-z36-shellpan-airlod-20260614` worsened the physical-phone wide street/layer-on run to 9/501 janky frames (1.80%, p95 6ms, p99 38ms) while layer cache spikes remained.
- Low-zoom aviation settled-cache bypass/direct draw: rejected/reverted because `maponly-street-on-nyc-z36-shellpan-layerdirect-20260614` worsened the physical-phone wide street/layer-on run to 6/494 janky frames (1.21%, p95 6ms, p99 36ms) compared with the path-transform implementation's best 4/485 janky frames (0.82%). Keep settled-cache behavior unless a smarter invalidation or precomputation method proves better.
- Current-symbol-state edge-strip merge during active pan: rejected/reverted because `full-street-off-chicago-z84-shellpan-edgecache-20260614` worsened the physical-phone Chicago zoom 8.4 street/off transition run to 26/536 janky frames (4.85%, p50 6ms, p95 21ms, p99 29ms), worse than the partial retained-symbol bitmap cache run at 15/510 (2.94%, p50 6ms, p95 17ms, p99 32ms). Do not reintroduce edge-strip state merging as designed.
- Scaling the retained aircraft-symbol bitmap through active zoom/pinch is rejected because it can decouple aircraft size/morph/outline pixels from the live zoom-governed dot-to-plane curve and visibly break continuity while zooming and panning together. Keep the retained bitmap for panning at stable zoom only; when transform scale changes beyond `SYMBOL_OVERLAY_PAN_SCALE_EPSILON`, return to the exact symbol draw path or a future smarter equivalent that recomputes the same morph pixels.
- Disabling filtering on the retained aircraft-symbol overlay bitmap is rejected as tested. `iter102-symboloverlay-nofilter-maptraffic-nochrome-chicago-country-satellite` worsened the Chicago country-scale satellite map+traffic no-chrome run to 69/116 Android janky frames (59.48%, PresentDrop120Pct 56.14%, p50 32ms, p95 48ms). Keep `symbol_overlay_bitmap_paint.isFilterBitmap = true` unless a fundamentally redesigned retained-symbol path proves visual parity and better frame data.
- Padded satellite-label pan-cache warmup that requests or composites missing offscreen tiles from a stable-background hit is rejected as tested. `iter104-pancache-warmup-maptraffic-nochrome-chicago-country` worsened the Chicago country-scale satellite map+traffic no-chrome diagnostic to 30/159 Android janky frames (18.87%, PresentDrop120Pct 20.34%) with 30-50ms pre-touch map frames and no observed `mapPanCache=hit`.
- Async stable-background composition is rejected as tested. `iter105-async-stablebg-maptraffic-nochrome-chicago-country` worsened the same diagnostic to 52/121 Android janky frames (42.98%, PresentDrop120Pct 37.93%). Do not move stable-background recording off the UI thread this way unless the design is fundamentally different and proves visual parity plus better frame data.
- Removing satellite-label composite invalidation of the stable background is rejected as tested. `iter106-composite-no-stable-invalidate-maptraffic-nochrome-chicago-country` worsened the diagnostic to 72/134 Android janky frames (53.73%, PresentDrop120Pct 51.26%). Keep invalidating or otherwise updating stable backgrounds when label composites materially change.
- Key-aware stable-background invalidation plus request-enabled satellite-label pan cache is rejected as tested. `iter112-keyaware-pan-cache-requests-maptraffic-nochrome-chicago-country` worsened the diagnostic to 30/181 Android janky frames (16.57%, PresentDrop120Pct 17.80%) with no observed `mapPanCache=hit`; the key-aware memory-only variant `iter113-keyaware-memoryonly-pan-cache-maptraffic-nochrome-chicago-country` was also worse at 34/191 (17.80%, PresentDrop120Pct 19.33%).
- Predictive idle aircraft-symbol overlay refresh using a 0.18px trigger is rejected as tested. `iter118-symboltile-predictrefresh-fullchrome-chicago-country` worsened the full-chrome Chicago country-scale satellite run to 25/205 Android janky frames (12.20%, PresentDrop120Pct 6.78%) and also produced a motion screenshot with oversized reference-label fragments. Do not revive that refresh trigger without a fundamentally different design and screenshot-backed proof.
- Recorder-side direct mask drawing instead of precomposed colored recorder sprites is rejected as tested. `iter125-recorder-maskdraw-street-country-noscreen` worsened the same-scenario Chicago country street no-screenshot run to 9/182 Android janky frames (4.95%, PresentDrop120Pct 4.24%) versus `iter124-trackbucket-street-country-noscreen` at 6/181 (3.31%, PresentDrop120Pct 1.68%), and the retained symbol cache did not become ready sooner. Do not replace the recorder's colored sprite reuse with direct mask draws unless a fundamentally different design proves better with same-city frame evidence.
- Reducing wide/country retained symbol overlay padding from 420dp to 300dp is rejected as tested. `iter127-widepad300-street-country-noscreen` reduced cached aircraft from roughly 2,150 to roughly 1,216, but still worsened same-scenario Chicago country street no-screenshot performance to 10/196 Android janky frames (5.10%, PresentDrop120Pct 5.04%) and introduced edge direct fallback during cache hits. Keep the 420dp wide padding unless a smarter coverage model proves better with visual evidence.
- Partial/tile-valid retained symbol overlay reuse is rejected as tested. `iter130-tilevalid-symbolcache-street-country-noscreen` allowed visually compatible but traffic-signature-stale cache results to install and validated retained tiles against current aircraft signatures; it often drew only 7/22 retained slices, kept direct-drawing about 634 symbols, added roughly 2-3ms cache validation overhead, and worsened same-scenario Chicago country street no-screenshot performance to 10/193 Android janky frames (5.18%) despite PresentDrop120Pct 2.52%. Do not revive this tile-validation model unless it avoids the low-valid-slice overhead and proves visual parity plus better same-city frame data.
- Renderer-side memoization of retained-symbol appearance/signature scans is rejected as tested. It reduced local `cacheAttempt` timing after warmup, but the valid forced Chicago country street no-screenshot run `iter133-signaturememo-street-country-noscreen-forced` worsened to 10/196 Android janky frames (5.10%, PresentDrop120Pct 5.88%) versus the prior best same-target diagnostic, and an unforced repeat was invalid because the viewport did not recenter on Chicago. Do not count tests whose `Debug perf viewport` lat/lon does not match the requested target city.
- Strict single-zoom satellite handoff coverage is rejected as tested. `iter141-satellite-transition-coverage-maponly-video` required one retained previous zoom to cover the viewport before drawing the handoff layer; this overcorrected to `keepInterim=false` throughout the focused transition run and brought back visible exact-tile color blocks. Do not repeat that approach; use a coherent previous-zoom tile grid with cached parent fallback for satellite zoom handoff instead.
- Idle shifted symbol-overlay reuse after feed/signature churn is rejected/reverted as tested. `iter146-street-country-la-off-diag-idleshift` allowed a visually matching retained bitmap to draw while idle even when the exact aircraft signature did not match; it did not improve the full street country run (9/171 Android janky frames, 5.26%; PresentDrop120Pct 4.31%; P99 75.06ms) and risks hiding real feed changes if expanded. Do not repeat this approach unless the new design proves exact aircraft/state correctness plus visual parity and better same-source same-city frame data.
- Latest-wins single-worker aircraft-symbol overlay recorder is rejected/reverted as tested. `iter155-street-country-la-recworker-screens` preserved the visible street/traffic look, but delayed cache readiness and worsened the focused LA street country screenshot-backed run to 9/174 Android janky frames (5.17%), P95 30ms, P99 69ms versus the rebuilt baseline around 4-5% jank with lower app p95. Do not repeat this threading shape unless the recorder work is cancellable or otherwise proven to keep cache readiness while reducing CPU contention.
- Street map use of the satellite-label pan cache plus exact offscreen tile prefetch is rejected/reverted as tested. The memory-only street pan-cache attempt `iter156-street-pan-cache-screens` produced no `mapPanCache=hit` evidence and worsened app p95 to 32ms, and the prefetch variant `iter157-street-pan-cache-prefetch-screens` hard-failed at 25/44 Android janky frames (56.82%), PresentDrop120Pct 51.22%, p50 53ms, p95 200ms while still producing no `mapPanCache=hit`. Do not flood street tiles into the satellite pan-cache path; solve street smoothness with a different design that preserves the exact accepted street visual quality.
- Lowering the aircraft-symbol overlay recorder thread to Android background priority is rejected as tested. `iter170-symbolrecorder-bgprio-chicago-street-on-noscreens` worsened the focused Chicago street/layer-on no-screenshot run to 7/177 Android janky frames (3.95%) and PresentDrop120Pct 2.52% versus `iter169-street-active-memorydecode-chicago-on-noscreens` at 6/189 (3.17%) and PresentDrop120Pct 1.68%. Do not repeat recorder-priority demotion unless the threading design is fundamentally different and proves same-city visual parity plus better frame data.
- Geometry-aware async aircraft-symbol overlay request identity is rejected/reverted as tested. Tracking pending/result work by cache key plus center/dimensions removed one `active_coverage` burst in a stateful no-force-stop diagnostic, but `iter189-geometryonly-london-country-satellite-full-forcestop-noscreens` worsened the London country satellite focused run to 28/144 Android janky frames (19.44%) and PresentDrop120Pct 19.33% versus `iter181` at 21/160 (13.12%) and PresentDrop120Pct 10.08%. Do not repeat this request-identity shape unless it is fundamentally redesigned and proves same-city visual parity plus better frame data.
- Short post-interaction cached-symbol bridge is rejected/reverted as tested. It reduced one post-up direct burst and `iter187-postbridge-london-country-satellite-full-screens` looked visually clean, but `iter188-geometry-postbridge-short-noscreens` worsened most short-matrix scenarios versus `iter181`, including London satellite, Chicago satellite transition, London street, Chicago transition street, close New York street, and wide Dallas-Fort Worth satellite. Do not hold symbol overlays after interaction this way unless a fundamentally different implementation proves visual parity and broad matrix improvement.
- User observation to preserve: the visual continuity break is primarily visible while zooming; simultaneous panning can make it easier to notice, but zoom is the core trigger. Test zoom-only and zoom+pan against master before accepting any symbol-cache, morph, or scale optimization.
- User observation to preserve: the zoom visual inconsistency is much more apparent at country scale than at local scale. Country-scale street and satellite pinch/zoom/pan captures over dense traffic are required before accepting fixes for morph, symbol scale, outline stability, or tile-transition behavior; local-scale passes are necessary but not sufficient.
- Async dense-dot overlay bitmap recording: rejected because it removed the 51.91ms UI-thread dot-batch recording spike seen in `iter241-current-trafficdetail-street-noscreen`, but same-phone no-detail `iter247-async-dotcache-street-noscreen` worsened the continent run to PresentDrop120Pct 36.44% and Android jank 89/295 frames (30.17%) with background bitmap/GC contention. Do not use the large-bitmap async dot recorder as tested.
- Direct-dot-only drawing with the dot overlay cache disabled: rejected because the traffic-detail diagnostic looked better in `iter243-direct-dots-trafficdetail-street-noscreen`, but same-phone no-detail `iter246-direct-dots-only-street-noscreen` worsened the continent run to PresentDrop120Pct 28.57% and Android jank 91/348 frames (26.15%). Do not keep dense dots permanently on the direct path as tested.
- Shifted dense-symbol source-space culling with a new visible-state list and 180dp padding: rejected because it gave only a small continent traffic-detail improvement in `iter244-shifted-symbol-cull-trafficdetail-street-noscreen` while worsening country/no-detail behavior and adding list/GC churn. Do not reintroduce this exact list-cull path.
- Stroke-only ARGB aircraft-symbol sprite cache for the direct Canvas path: rejected/reverted because `iter250-stroke-sprite-street-trafficdetail` worsened the continent diagnostic to PresentDrop120Pct 43.97% and Android jank 112/380 frames (29.47%). Logs showed cache-attempt spikes up to 16.35ms and a catastrophic 248ms frame while creating/reusing stroke sprites. Do not repeat stroke/fill sprite caching unless the implementation avoids per-frame bitmap churn and proves visual parity plus better same-phone frame data.
- Traffic symbol overlay idle motion-bound, idle-motion-refresh, and delayed post-interaction prewarm experiments are rejected/reverted as tested. After the satellite tile-only transition path improved, these attempts worsened full satellite transition runs instead of fixing the traffic bottleneck: `iter264-satellite-idlemotionskip-full-video` hit PresentDrop120Pct 100% and Android jank 38%, `iter265-satellite-motionbound-full-video` hit PresentDrop120Pct 57.98% and Android jank 24.52%, and `iter266-satellite-delayed-prewarm-full-video` hit PresentDrop120Pct 96.64% and Android jank 28.88%. Do not repeat this local cache-hold/motion-bound/prewarm shape unless it is fundamentally redesigned and proves broad visual parity plus better full-composition frame data.

- Letting the aircraft-symbol external-view warmup reset fire after a scaled pinch frame is rejected. `iter07-instrumentation-pinch-perf-noscreens` still produced roughly 600ms post-pinch `symbolCache=hit` traffic frames because scale early-returns stopped the renderer from updating the last-seen viewport, so the settled pinch looked like an external jump and re-armed idle cache recording. Preserve the fix direction where last-seen viewport updates happen before scale early-returns, and do not classify a real pinch/zoom gesture as a fresh external view jump.

Promising optimization direction from the 2026-06-14 optimizer pass: cache and transform the full visible aircraft-symbol overlay, including edge padding, while drawing symbols through visually equivalent cached masks or other parity-proven rendering primitives. Validate with moving screenshots/video before trusting frame improvements.

## Optimizer Successes To Preserve

These optimizations improved smoothness or reliability without an accepted feature/visual loss. Preserve them unless a newer measured change proves better and keeps parity.

- Fast binCraft/direct HTTP inventory feed for all-aircraft locations: preserve as the primary traffic inventory path. Do not reintroduce Chromium/WebView for live inventory.
- Cached aircraft-symbol overlay direct-fallback skip when the retained overlay already covers the viewport plus conservative edge padding: preserve unless a broader matrix proves otherwise. Same-city physical-phone `iter57-fallbackscan-skip-longpan-chicago-z54-satellite-off-diagonal` improved the Chicago country-scale satellite/off diagonal longpan from prior normal `iter53` at 29/410 Android janky frames (7.07%, PresentDrop120Pct 15.25%, p95 38ms, p99 53ms) to 12/545 janky frames (2.20%, PresentDrop120Pct 5.08%, p95 28ms, p99 48ms). Same-run motion-start/active/late screenshots showed satellite imagery, labels, dots, selected details, and edge traffic intact, with no map blanking or visual fallback loss.
- Military registry-country fallback uses real ICAO 24-bit allocation ranges when a registration prefix is unavailable. Registration-derived countries still win, non-ICAO `~` addresses stay unresolved instead of guessed, and allocation-derived labels remain marked as `(ICAO allocation)`.
- Extreme-priority notification semantics: preserve the 2026-06-14 service behavior where the notification enters foreground and alerts only on the first active extreme-priority transition, then updates the same ongoing notification quietly until the classifier-produced extreme list clears. The faster safety-biased alert pipeline may use a short bounded projection from fresh real position/speed/track data and marks notification rows with `est.` when the alert position is estimated.
- Priority tracker range adjusters support long press on horizontal/vertical +/- controls. The themed radial fill must take exactly Android's long-press timeout to fill the button, a completed long press must suppress the normal tap increment, and the large step must be unit-honest: imperial mode changes clean foot values while metric mode changes clean meter values and only then converts to the stored feet value for safety math.
- Cached dense dot batches with projected per-aircraft motion: preserves visible aircraft locations while avoiding per-frame spatial rebuilds at wide zooms. Keep interpolation tied to real last-known position, speed, heading, and freshness.
- Transform/reuse of dense dot batches during active pan/pinch: successful direction for responsiveness as long as cache coverage is sufficient and visual representation does not degrade.
- Zoom-governed dot-to-symbol morph and dot sizing: preserves smooth transitions independent of traffic density and avoids density-induced morph lockups.
- Cached symbol morph paths/mask primitives: successful direction for drawing the same accepted aircraft shapes more cheaply than rebuilding vector paths per symbol each frame, provided screenshots/video confirm no darkening, shrinking, stepping, or shape drift.
- Cached aircraft-symbol alpha masks for the full visible dense symbol overlay: successful on the physical phone transition sweep over New York City at zoom 8.4 street/off. Full symbol coverage was preserved in rest/motion-start/motion-active screenshots, and jank improved from 166/191 frames (86.91%, p50 53ms, p95 73ms) with per-frame path symbols to 28/467 frames (6.00%, p50 15ms, p95 27ms) with cached masks.
- Cached-mask traffic-only isolation over New York City zoom 8.4 street/off with `-SkipMap -SkipChrome` reached 1/444 janky frames (0.23%, p50 15ms, p95 16ms, p99 18ms), showing the traffic renderer itself is close to smooth after the cached-mask pass.
- No-traffic isolation over New York City zoom 8.4 street/off with `-SkipTraffic` reached 0/477 janky frames (0.00%, p50 6ms, p95 9ms, p99 9ms), so map/chrome without traffic is not the bottleneck in that transition test.
- Warm cached-mask full sweep over New York City zoom 8.4 street/off reached 2/465 janky frames (0.43%, p50 16ms, p95 19ms, p99 20ms), confirming remaining cold-run transition jank mostly comes from mask-cache buckets being created during the gesture.
- Retained bitmap aircraft-symbol overlay during active pan: preserves the same accepted aircraft-symbol pixels while turning the dense moving symbol layer into one bitmap draw during active map motion. On physical phone `RFCX40KPN3B`, New York City zoom 8.4 street/off traffic-only improved from 34/1066 janky frames (3.19%) with RenderNode replay to 7/1014 (0.69%), then idle prewarm improved to 4/1097 (0.36%). Full street/off with the retained bitmap/prewarm reached 2/1072 janky frames (0.19%, p50 8ms, p95 9ms, p99 10ms) and a confirmation run reached 2/1094 (0.18%) with clean rest/motion screenshots.
- Satellite tile retention/fallback pass: preserve real satellite imagery while panning by keeping the last complete interim raster above partially loaded exact tiles, fading exact tiles over interim only when safe, using access-order LRU tile memory, and resetting tile transitions without clearing useful tile memory on ordinary source/label/perf switches. Verified on physical phone `RFCX40KPN3B` over New York City zoom 8.4 satellite/off with a real shell-input pan: 1/371 janky frames (0.27%, p50 5ms, p95 8ms, p99 8ms), and rest/motion-start/motion-active/end screenshots stayed filled with satellite imagery and showed no blank loading blocks or harsh blank flicker. Reverified after compaction with map-only satellite/off shell-pan `satellite-tiles-maponly-nyc-z84-off-shellpan-retention-20260614-103512`: 1/631 janky frames (0.16%, p50 5ms, p95 15ms, p99 16ms), app-side map draw under roughly 2ms, and rest/motion-start/motion-active/motion-late/end screenshots stayed filled with real satellite imagery. Reverified again before the anti-jank loop with `satellite-tiles-maponly-nyc-z84-off-shellpan-recheck-20260614-113942`: 1/483 janky frames (0.21%, p50 5ms, p95 6ms, p99 7ms), clean rest/motion-start/motion-active/motion-late/end screenshots, and no blank or loading tiles. Reverified on the exact post-revert installed build with `satellite-tiles-maponly-nyc-z84-off-shellpan-postinstall-20260614-1224`: 3/480 janky frames (0.62%, p50 5ms, p95 7ms, p99 7ms), and motion-start/motion-active/motion-late screenshots stayed filled with satellite imagery. A zoom-transition check using `keyboardzoom` (`satellite-tiles-maponly-nyc-z84-off-keyzoom-recheck-20260614-114317`) produced 5/438 janky frames (1.14%, p50 5ms, p95 7ms, p99 22ms), clearly changed zoom level in the screenshots, and stayed filled with satellite imagery without blank loading blocks or harsh empty-tile swaps.
- Satellite zoom-out high-detail snap fix: preserve the time-bounded satellite zoom-out rule that suppresses higher-detail child fallback mosaics and child prefetch during the active zoom-out transition window while keeping parent/retained real imagery and normal child fallback outside that window. This fixes the "blink back to high-detail before lower-detail imagery is ready" without dropping real map data. Rejected adjacent attempts are recorded in `Optimizer Tried And Denied`. Best diagnostic evidence: `iter39-satellite-tileonly-childfallback` on physical phone `RFCX40KPN3B` with `skipChrome=true` and `skipTraffic=true` reached continent PresentDrop120Pct 3.42% and country 13.45%, with active tile logs mostly around 5-10ms after initial load bursts. Full-app acceptance evidence: `iter41-satellite-full-childfallback` video showed no blank-map flicker or harsh high-detail snap-back, country PresentDrop120Pct was 8.4%, and logs showed remaining country bursts were transient map loads while wide/continent problems were mostly traffic/symbol-cache churn.
- Current pre-anti-jank satellite gate is clear on the active dirty tree: physical-phone map-only satellite/off New York City zoom 8.4 shell-pan `satellite-tiles-maponly-nyc-z84-off-shellpan-current-recheck-20260614` rendered 482 frames with 2 janky frames (0.41%, p50 5ms, p95 8ms, p99 9ms). Rest, motion-start, motion-active, motion-late, and end screenshots stayed filled with real satellite imagery and showed no loading placeholders, blank blocks, or harsh empty-tile swaps.
- Satellite tile retention/interim merge verification: after `MapTileRenderer` changed interim raster replacement from whole-set clearing to merge-and-prune, physical-phone `FlightMapGesturePerfTest#zoomLowToHighSweep` was repaired to launch with explicit `mapSource=SATELLITE`, accept/grant Flight Alert permissions, and save motion-phase screenshots. Valid two-finger pinch run `instrumentation-zoomLowToHighSweep-satellite-motion-20260614-125831` captured rest, motion-start, motion-active-out, reverse-start, motion-active-in, and final frames; all stayed filled with real satellite imagery with no `Loading map` placeholders or blank flicker. A 90-second Paris satellite/off soak `satellite-soak-paris-off-20260614-1302` also kept rest/active/end imagery filled without requiring an app restart. A 60-second Paris street/off soak `street-soak-paris-off-20260614-1305` kept street tiles filled too, so the broader base-map disappearance note is visually cleared for this pass.
- Post-partial-cache base-map disappearance check: physical-phone `RFCX40KPN3B` 90-second Paris street/off soak `street-soak-paris-off-postpartial-20260614` kept rest, mid-soak, and end screenshots filled with real street tiles and aircraft. Matching 90-second Paris satellite/off soak `satellite-soak-paris-off-postpartial-20260614` kept rest, mid-soak, and end screenshots filled with real satellite imagery and aircraft. No local PowerShell perf jobs remained after the soaks. These runs visually clear the reported base-map disappearance for this iteration, though idle aircraft-symbol direct drawing remains too janky and should be optimized separately.
- Perf harness foreground checks: preserve because they prevent accidental launcher/home-screen tests from being counted as Flight Alert results.
- Hidden perf chrome/details hit-test fix: in debug perf skip-chrome mode, hidden details/chrome no longer intercept map gestures. Preserve this because it makes shell-pan tests reach the map (`mapTouchActive=true`, symbol cache hit, direct symbol drawing avoided during active pan) instead of accidentally testing invisible controls or the launcher.
- Jank test visual artifacts tied to the same run as framestats: preserve and fix when broken, because frame wins are invalid if rest/motion-start/motion-active/end captures show visual regression. On multi-display phones, choose the screencap display with real image content; the first SurfaceFlinger display id can be a black/blank capture.
- `ACTION_UP` now clears `drag_started` and `drag_start_center`, and records map interaction only when the drag was not blocked. Preserve this cleanup because it prevents late stale active-cache state and avoids hiding real idle/direct rendering cost.
- Adaptive aircraft motion redraw gating now uses cached maximum projected aircraft speed (`max_projected_speed_zoom_zero`) with zoom-aware ticker thresholds. Preserve the O(1) direction for avoiding unnecessary subpixel idle redraws, but keep optimizing because the current post-gesture direct path is still too expensive.
- The retained aircraft-symbol overlay no longer calls `prepareToDraw()` before being drawn. Preserve unless a measured replacement proves better with matching screenshots, because avoiding the upload hint reduced the first active cache stall in physical-phone testing.
- Aircraft-symbol overlay visual-readiness guard: preserve the cache key's coarse appearance bucket and the rule that retained symbol overlays are not drawn until the aircraft symbols are actually visible. This fixed the oneprewarm visual regression where rest frames showed aircraft symbols but motion-start/motion-active showed dot-only fallback at New York City zoom 8.4. `cacheappearance-street-on-nyc-z84-shellpan-20260614-115702` and `cacheinitialonly-street-on-nyc-z84-shellpan-20260614-120243` restored the accepted aircraft-symbol look in motion-start/motion-active/motion-late screenshots.
- Pre-interaction-only idle refresh for the retained aircraft-symbol overlay: preserve the `symbol_overlay_saw_interaction` guard unless a measured replacement proves better with matching motion visuals. It keeps the useful initial warmup while preventing repeated post-pan idle cache refreshes from landing in the measured tail.
- Aircraft-symbol overlay active key refresh guard: preserve the rule that active pan/pinch does not rebuild the retained symbol overlay when only visual cache keys drift. `instrumentation-zoomLowToHighSweep-satellite-activekey-20260614-131326` removed the previous 600-800ms active-pinch symbol-overlay rebuild spikes while keeping satellite motion frames filled and aircraft symbols visible; later New York shell-pan logs showed active map motion mostly in the 0.3-3.4ms traffic range. The remaining bottleneck is idle direct symbol drawing, not active touch rebuilds.
- Aircraft-symbol overlay external-view warmup reset: preserve the view-jump and visual-key-aware idle refresh logic that resets the retained symbol overlay guard when the map jumps to a new city/zoom before a gesture. Physical-phone `iter01` moved the Chicago dot-to-aircraft transition tests from `matrix00-transition-chicago-z84-street` at 100% 120Hz latency misses to `iter01-transition-chicago-z84-street` at 45.23%, and from `matrix00-transition-chicago-z84-satellite` at 100% to `iter01-transition-chicago-z84-satellite` at 39.37%. Active motion used `symbolCache=hit direct=0` or tiny direct edge counts while preserving accepted aircraft symbols/outlines in street and satellite motion-active screenshots. Keep this direction because it fixes cold external viewport tests without changing aircraft pixels.
- Dense symbol state idle reuse with projected per-aircraft motion: preserve the direction where dense symbol overlay states carry screen velocity, motion lifetime, and appearance timing so the app can reuse state between feed updates without freezing aircraft or appearance curves. `idlesymbolreuse-street-off-nyc-z84-shellpan-20260614` improved the physical-phone New York City zoom 8.4 street/off full shell-pan from the prior 40/494 janky frames (8.10%, p50 18ms, p95 34ms, p99 44ms) to 34/500 janky frames (6.80%, p50 18ms, p95 27ms, p99 36ms), with motion-start/motion-active screenshots preserving the accepted outlined aircraft-symbol look and no dot fallback, dark/small symbols, or line traces. It is not complete by itself: the traffic-only diagnostic `idlesymbolreuse-trafficonly-nyc-z84-shellpan-20260614` still measured 26/524 janky frames (4.96%, p50 14ms, p95 29ms), so continue optimizing direct fallback frames.
- Aviation restricted-layer prebuilt world-space path rendering: preserve the direction of preparing airspace rings once in projected world coordinates and drawing them through canvas translation/scale instead of rebuilding screen paths/interaction line arrays during movement. On physical phone `RFCX40KPN3B`, New York City zoom 3.6 map-only street/layer-on improved from `maponly-street-on-nyc-z36-shellpan-layeron-20260614` at 7/494 janky frames (1.42%, p50 5ms, p95 8ms, p99 34ms) to best run `maponly-street-on-nyc-z36-shellpan-airpath-20260614` at 4/485 janky frames (0.82%, p50 5ms, p95 8ms, p99 31ms), with confirmation `maponly-street-on-nyc-z36-shellpan-airpathconfirm-20260614` at 6/483 (1.24%, p50 5ms, p95 7ms, p99 38ms). Motion screenshots stayed visually intact.
- Retained bitmap dot overlay for wide dense dot batches: preserve because it draws the same accepted colored/outlined aircraft dots as a cached bitmap during active wide-map movement instead of sending roughly eleven thousand individual point draw commands through RenderThread. On physical phone `RFCX40KPN3B`, New York City zoom 3.6 full street/off improved from `full-street-off-nyc-z36-shellpan-airpathconfirm-20260614` at 77/435 janky frames (17.70%, p50 20ms, p95 28ms, p99 31ms) to `full-street-off-nyc-z36-shellpan-dotbitmap-20260614` at 7/495 janky frames (1.41%, p50 5ms, p95 7ms, p99 12ms). The same dot-bitmap build measured full street/on at 9/490 (1.84%, p50 5ms, p95 8ms, p99 38ms), full satellite/off at 4/487 (0.82%, p50 5ms, p95 6ms, p99 14ms), and full satellite/on at 7/490 (1.43%, p50 5ms, p95 7ms, p99 36ms), with same-run screenshots showing real map imagery and visible aircraft dots/outlines.
- Partial retained aircraft-symbol bitmap cache reuse during active pan: preserve the active cache rule where center changes do not invalidate the retained symbol bitmap as long as zoom/shape/theme visual keys match and the bitmap still intersects the viewport; uncovered edges are direct-drawn by the existing parity path. On physical phone `RFCX40KPN3B`, Chicago zoom 8.4 full street/off improved from `full-street-off-chicago-z84-shellpan-dotbitmap-20260614` at 23/510 janky frames (4.51%, p50 13ms, p95 17ms, p99 30ms) to `full-street-off-chicago-z84-shellpan-partialcache-20260614` at 15/510 (2.94%, p50 6ms, p95 17ms, p99 32ms). Motion screenshots showed accepted aircraft symbols/outlines with no line traces, dark/small shortcut, or dot fallback.
- Post-pinch aircraft-symbol cache handoff fix: preserve the combined `iter08`/`iter09` renderer behavior where idle cache recording is pre-interaction/external-jump only, last-seen viewport updates happen before scale early-returns, and a newly recorded symbol bitmap uses the newly recorded center for its coverage rectangle. This removed the previous 300-650ms post-gesture idle cache rebuild in the no-screenshot pinch/morph suite; remaining post-gesture direct-symbol frames were about 15-34ms worst case instead of hundreds. Screenshot-backed Chicago zoom 8.4 transition pans in `iter08-visual-transition-chicago-z84-*` preserved aircraft silhouettes, outlines, dot underlay, satellite imagery, and restricted-zone visuals in street/satellite with layer off/on.
- `iter09` active pan matrix to preserve: physical-phone no-screenshot matrix over Dallas-Fort Worth zoom 3.6, Toronto zoom 5.4, Chicago zoom 8.4, and New York City zoom 12.0 covered street/satellite with restricted layer off/on. Android jank stayed between 0.00% and 1.41%, restricted-layer impact stayed within the user's 2% target, and present cadence was generally 8.34ms p50/p95 with 0% present drops except a few close-view 0.85-1.71% present-drop cases. Direction sweep `iter09-noscreen-directions-chicago-z84-*` covered horizontal, vertical, diagonal down, and diagonal up in both street and satellite with the layer on; Android jank stayed 0.59-0.98% and present p95 stayed 8.34ms.
- Tiled retained aircraft-symbol overlay slices: preserve the direction where the cached aircraft-symbol bitmap carries occupied source rectangles and draws only those slices during cached pan/idle hits. This keeps the accepted aircraft pixels while avoiding a full-screen transparent bitmap over satellite imagery. `iter115-symboltile-cache-maptraffic-nochrome-chicago-country` improved the focused no-chrome Chicago country satellite map+traffic diagnostic to 15/214 Android janky frames (7.01%, PresentDrop120Pct 6.72%), and screenshot-backed `iter116-symboltile-cache-visual-nochrome-chicago-country` reached 10/197 (5.08%, PresentDrop120Pct 4.27%) with no visible slice seams, dark/small aircraft, or missing traffic.
- Exact-only reference-label overlay drawing: preserve the rule that reference labels/borders do not use scaled parent/child tile fallback. Scaled imagery fallback is valid for real satellite/street imagery, but scaled text creates wrong-size labels. `iter118` exposed oversized "California/Los..." label fragments during motion; after exact-only label overlay fallback, `iter119-exact-labels-symboltiles-fullchrome-chicago-country` screenshots removed the giant label fragments while preserving map labels/borders and traffic.
- Motion-split retained symbol cache: preserve the direction where the cached symbol overlay records the aircraft keys it actually contains and keeps live direct fallback available for aircraft deliberately excluded from the cache. `iter121-motion-split-symbolcache-fullchrome-chicago-country` over Chicago country-scale satellite/full chrome reached 8/190 Android janky frames (4.21%, PresentDrop120Pct 2.52%, present p95 8.34ms) with clean motion screenshots and no visible aircraft duplication/missing-dot compromise. This is not final 120Hz everywhere: `iter122` street/full-chrome remained 12/189 (6.35%, PresentDrop120Pct 6.78%).
- Satellite zoom handoff fix: preserve the current `MapTileRenderer` behavior where satellite tile load fade remains time-based, but zoom-level color/detail handoff is tied to zoom position across a wide fractional zoom band. During satellite transition frames, draw the previous zoom as a coherent real-tile grid using cached exact tiles plus cached parent fallback, instead of a partial retained-tile stack or a timer fade. Physical-phone `iter143-satellite-transition-grid-maponly-video` over Los Angeles with `skipChrome=true`/`skipTraffic=true` measured 1015 frames, PresentDrop120Pct 1.68%, p50/p95 8.34ms, and active zoom-out/zoom-in/pan-after screenshots stayed filled with real satellite imagery, readable labels/borders, no tan blanks, and no harsh app-caused swap. Source provider tile seams may still be visible in desert/ocean imagery, but do not regress the no-flicker retained-grid handoff.
- Satellite transition retained-underlay and Esri no-data-tile filtering: preserve the direction where a retained real satellite grid is used as an opaque underlay during zoom-level handoff, but only after proving the retained grid covers sampled viewport points, and where Esri World Imagery grey "Map data not yet available" placeholder tiles are treated as missing so they do not replace lower-zoom real imagery. Physical-phone `iter255-satellite-transition-band-tileonly-video` over Los Angeles with `skipChrome=true`/`skipTraffic=true` measured 975 frames, PresentDrop120Pct 3.39%, Android jank 21/975 (2.15%), p50/p95 present 8.34ms, and active transition screenshots stayed filled with real satellite imagery and labels with no grey no-data blocks or app loading blanks. This is a tile-path success, not full-user-composition acceptance.
- Satellite zoom-position alpha and single-layer retained-grid blending: preserve the `MapTileRenderer` behavior where satellite zoom handoff alpha is tied to fractional zoom position rather than elapsed time, pending exact-tile grids keep a complete retained real-imagery layer opaque, and retained satellite grids fade as one canvas layer instead of per-tile alpha. During the blend, current labels/borders draw below retained imagery so old large labels do not ghost over the live map. Physical-phone `iter261-satellite-layeralpha-currentlabels-video` over Atlanta with `skipChrome=true`/`skipTraffic=true` measured 958 frames, PresentDrop120Pct 2.54%, Android jank 23/958 (2.40%), p50/p95 present 8.34ms, and p95 app frame 8ms. Visual review showed clean zoom-in, no old-label ghosting, and only subtle provider imagery variance during zoom-out rather than app-created alpha tile seams. This is the current best satellite tile-only transition result and should not be regressed while optimizing street or traffic.
- Raster zoom-position handoff margin refinement: preserve the widened `RASTER_TILE_ZOOM_BLEND_MARGIN = 0.52f` unless a same-run video/screenshot comparison proves a better curve. It stretches the zoom-position alpha zone so satellite color/detail does not brighten abruptly at the transition point while staying tied to zoom rather than elapsed time. Fixed New York full-product evidence: `iter286-satellite-zoommarg052-fixednyc-full-novideo` stayed performance-neutral versus `iter280` at PresentDrop120Pct 19.49%, Android jank 152/827 (18.38%), p50/p95 present 8.34/16.68ms, with active screenshots showing filled real imagery, labels/borders present, and no blank or harsh tile blocks. Tile-only diagnostic `iter287-satellite-zoommarg052-tileonly-video` with `skipChrome=true`/`skipTraffic=true` reached PresentDrop120Pct 2.52%, Android jank 17/889 (1.91%), and p50/p95 present 8.34/8.34ms. The remaining full-product spikes were `symbolCache=miss reason=scale` traffic-symbol frames, not satellite tile handoff regressions.
- Generic raster/street retained zoom-grid handoff: preserve the current `MapTileRenderer` behavior where non-satellite street tiles can use the same retained previous-zoom real-tile grid during zoom transitions instead of flashing between partial tile sets. Diagnostic street skip-traffic evidence over Chicago: `iter288-street-rasterhandoff-fixedchicago-skiptraffic-novideo` improved continent PresentDrop120Pct from `iter285` at 10.26% to 8.55% and country from 12.17% to 7.69%, with motion screenshots preserving real street tiles, roads, labels, borders, and no blank blocks. Full-product evidence `iter289-street-rasterhandoff-fixedchicago-full-novideo` improved the street continent band versus `iter284` from 50% to 43.48% PresentDrop120Pct while country stayed excellent at 2.54%; logs showed the remaining weak continent frames were traffic/symbol bursts such as `symbolCache=miss reason=active_coverage direct=5814 traffic=206.42ms`, not the base street map.
- Active street visible-tile UI disk-decode avoidance plus the pending symbol-overlay-recording guard are provisional successes to keep testing, not final acceptance. `iter169-street-active-memorydecode-chicago-on-noscreens` improved the focused Chicago street/layer-on active pan to 6/189 Android janky frames (3.17%) and PresentDrop120Pct 1.68%, and `iter172` repeated at 3/158 (1.90%) with PresentDrop120Pct 1.68%. Screenshot-backed `iter168`/`iter173` preserved street tiles, layers, and aircraft visuals, though screenshot capture disturbed framestats. Keep this direction only if the fixed pan matrix continues to prove visual parity and broader street/satellite performance.
- Active visible-tile UI disk-decode avoidance for all map sources is a provisional success to keep testing. `iter192-active-no-ui-diskdecode-chicago-country-satellite-on-noscreens` improved the Chicago zoom 5.4 satellite/layer-on diagonal run from `iter191` at 23/124 Android janky frames (18.55%, PresentDrop120Pct 14.41%) to 11/156 janky frames (7.05%, PresentDrop120Pct 4.24%). Screenshot-backed `iter193-active-no-ui-diskdecode-chicago-country-satellite-on-screens` kept satellite imagery, labels/borders, restricted polygons, dots, and density intact with no blanks, dark/small symbols, or visual shortcut.
- Chrome surface bitmap caching is a provisional success to preserve unless a later screenshot-backed run shows a panel/control visual regression. `iter198-chromecache-transition-satellite-full-noscreens` improved Chicago zoom 8.4 satellite/off diagonal transition to 10/192 Android janky frames (5.21%, PresentDrop120Pct 6.03%), and `iter200-chromecache-short-noscreens` broadly improved the short matrix: country Chicago satellite/on 6.25% Android jank and 5.04% PresentDrop120Pct, transition Chicago satellite/off 2.78% and 2.56%, close New York street/off 2.11% and 3.39%, close New York satellite/off 2.58% and 3.39%, country London street/off 1.30% and 2.54%, wide DFW satellite/off 1.47% and 0.84%. `iter199-chromecache-transition-satellite-full-screens` visually preserved panels, controls, satellite fill, aircraft, labels, and restricted-layer appearance; screenshot overhead means its frame data is visual-only.

## Optimizer Testing Notes

Keep this section current with testing methods that worked, failed, or had device quirks.

- After code changes, install the freshly built APK on the physical phone before trusting any device perf run. `RunFlightMapPerf.ps1` launches and measures the app but does not install the latest compiled APK. Use `.\gradlew.bat installDebug` or an equivalent explicit install, then grant/accept Flight Alert runtime permissions as needed.
- Do not run two `RunFlightMapPerf.ps1` invocations against the same device at the same time. They race the perf viewport/run id, invalidate both runs, and can produce misleading ownership errors.
- Optimizer test runs should produce usable evidence within 5 minutes. If a test exceeds 5 minutes, treat that as a device-state problem or a test-design problem: stop the run, record what was happening, and either repair the device/app state or shorten/split the test before continuing. Do not let a normal optimization pass wait indefinitely on an overlong test.
- Physical phone `RFCX40KPN3B` exposes multiple SurfaceFlinger display ids. The first listed id can capture a 19 KB black PNG; the app content was on the larger PNG display id in recent tests. Use the harness display probe or verify screenshot file size/content before trusting visual artifacts.
- For `screenrecord` evidence on foldable/dual-display physical phones, pass the active physical display id from `dumpsys display` / `dumpsys SurfaceFlinger --display-id`. The recorder can otherwise capture launcher/home frames or the inactive panel. `RunFlightMapGesturePerf.ps1 -RecordVideo` now auto-detects the active physical id, with `-VideoDisplayId` as an override.
- Do not run Gradle compile/build tasks for the same project in parallel. Parallel `compileDebugKotlin` and Android-test compilation can race the same Kotlin output directory and produce a false file-lock/delete failure.
- Valid visual artifacts should include rest, motion-start, motion-active, and end captures from the same run label as framestats. Black frames or launcher/home frames invalidate visual evidence.
- Traffic-only isolation should use `-SkipMap -SkipChrome`; no-traffic isolation should use `-SkipTraffic`. Run them sequentially after a full run to separate overlay cost from map/chrome cost.
- New York City zoom 8.4 street/off sweep is a good dense transition-zone test for dot-to-aircraft morph, edge symbols, and quick zoom/pan behavior.
- App draw logs are useful for renderer cost (`Debug draw perf ... traffic=... symbols=... dots=...`), while system framestats capture end-to-end jank. Screenshot jobs can add some overhead, so compare app logs and system framestats together.
- Map-only and traffic-only isolation can both be clean while the combined satellite map+traffic run still misses 120Hz due to RenderThread/GPU/present pressure. `iter99-maponly-stable-bg-chicago-country-satellite` measured 3/165 Android janky frames (1.82%, PresentDrop120Pct 0.84%), and `iter100-trafficonly-idlecache-chicago-country-satellite` measured 2/162 (1.23%, PresentDrop120Pct 0.84%), while `iter101-maptraffic-nochrome-chicago-country-satellite` still hit 32/118 (27.12%, PresentDrop120Pct 27.03%). For this class of bug, keep checking full map+traffic runs and GPU/issue-draw histograms, not only isolated renderer timings.
- Direct satellite symbols during active pan are a promising diagnostic direction, not yet a final success. `iter103-direct-satellite-symbols-maptraffic-nochrome-chicago-country` improved the combined no-chrome Chicago country satellite run to 18/187 Android janky frames (9.63%, PresentDrop120Pct 8.40%), with active app draw logs mostly around 5.5-6.7ms but remaining `Slow issue draw commands` / GPU buckets. Verify with screenshot-backed full-chrome runs before preserving it.
- The memory-only satellite-label pan cache is not yet a proven success. `iter111-memoryonly-pan-cache-maptraffic-nochrome-chicago-country` looked promising at 16/207 Android janky frames (7.73%, PresentDrop120Pct 8.40%), but `iter114-memoryonly-pan-cache-confirm-maptraffic-nochrome-chicago-country` regressed to 33/187 (17.65%, PresentDrop120Pct 17.65%) and logs still showed no `mapPanCache=hit`. Treat it as pending or remove it if it remains dead code after the next focused investigation.
- Empty scheduler/display control `iter108-empty-scheduler-chicago-country-control` measured 2/171 Android janky frames (1.17%, PresentDrop120Pct 0.84%, present p50/p95 8.34ms), proving the physical phone and harness can present at 120Hz in this test setup. If a full map+traffic run falls to 16.67ms cadence, isolate map-only and traffic-only before blaming display mode.
- Current combined satellite country bottleneck after `iter109`/`iter110`: map-only is clean enough (4/165 Android jank, 2.42%, PresentDrop120Pct 0.84%) and traffic-only is modest (8/171, 4.68%, PresentDrop120Pct 4.20%), but combined map+traffic can still miss 120Hz. Active logs show map around 1-2ms and direct satellite symbols around 5-6ms, so the next smart target is reducing symbol/map present pressure without changing aircraft pixels or satellite imagery behavior.
- After `iter123-debug-motion-split-street-country-noscreen`, the remaining country-scale street/satellite idle churn is mostly over-sensitive symbol cache invalidation after feed/signature updates, not screenshot overhead. Logs showed cached hits such as `symbolCache=hit slices=22 cached=2175 motionEx=false direct=0`, followed by `symbolCache=miss reason=inactive direct=~653-655`. This means one-second-ish feed visual signature changes still force direct fallback; the next optimization should reduce signature churn or direct fallback cost without showing stale/incorrect aircraft positions or throwing away visible symbol orientation.
- `RunFlightMapPerf.ps1` intentionally settles the app before resetting gfxinfo, so logcat can contain idle direct-render logs from before the measured gesture. Judge measured motion with the run framestats plus post-touch log slices, not pre-reset idle spikes.
- Do not run multiple `RunFlightMapPerf.ps1` or matrix jobs against the same physical phone at the same time. Parallel device perf runs race the single foreground app, overwrite each other's viewport/run IDs, and must be discarded as invalid. Parallelize file/code inspection only, not real-device gesture tests.
- Device-side `nohup sh -c 'sleep ...; screencap ...' &` screenshots have been validated for motion captures on the real phone. Keep tying those visual artifacts to the same run label as the framestats.
- The old standalone `tools/perf` Java multi-touch runner was removed after it failed to deliver reliable real touch streams on the physical phone. Do not recreate `MultiTouchGestureRunner`, `PinchGestureTest`, concurrent shell-pinch, raw `sendevent`, or explicit-display shell-input pinch paths for optimizer proof. Use `RunFlightMapPerf.ps1`/`RunFlightMapPanMatrix.ps1` for panning, soak, and keyboard-zoom diagnostics, and use instrumentation/UIAutomator for real two-finger pinch/morph evidence.
- Physical-phone UIAutomator instrumentation `FlightMapGesturePerfTest#zoomLowToHighSweep` produced valid two-finger evidence when the app log showed real `pointers=2` touch streams. Use that path for pinch/morph/satellite zoom evidence when shell or Java injection does not deliver real multi-touch.
- Physical-phone UIAutomator instrumentation `FlightMapGesturePerfTest#countryScaleZoomContinuityStreet` and `#countryScaleZoomContinuitySatellite` are the targeted country-scale visual-continuity tests. They start at zoom 5.4 over a fresh random United States, European, or Mexico City-class major-major city/major-airport corridor, capture rest, outward zoom motion, inward zoom motion, and quick anchored pinch motion over dense traffic, and must visually confirm the zoom-back-in target did not drift northeast away from the intended traffic focus. Upper Canada, ocean-centered targets, rural/smaller fallback metros, and small-city targets are invalid. Use these before accepting fixes for the country-scale zoom inconsistency, especially when local-scale tests look fine.
- The first valid `zoomLowToHighSweep` run caught the active map/tile blanking bug: `tools/perf/out/instrumentation-zoomLowToHighSweep-20260614/flightalert-perf-zoomLowToHighSweep.png` showed aircraft overlays over mostly dark satellite "Loading map" tiles with only a small loaded imagery patch. This validates the pending map/tile bug as base imagery disappearing, not aircraft disappearing.
- A later `zoomLowToHighSweep` rerun after the current tile-retention patch failed before gestures with `Refusing to run gestures outside Flight Alert`; that run is invalid and must not be counted as evidence for or against the tile fix.
- `connectedDebugAndroidTest` can remove the app package after a run on the physical phone. Reinstall `build\outputs\apk\debug\Flight Alert-debug.apk` and grant Flight Alert runtime permissions before subsequent `RunFlightMapPerf.ps1` or manual phone tests if `adb` reports package/class not found.
- UIAutomator `takeScreenshot()` cannot save directly under `/sdcard` on the physical phone because MediaProvider rejects the root directory; save instrumentation motion screenshots under `/sdcard/Download/` and pull from there. Shell `screencap` can still write final/rest images to `/sdcard`.
- For full-map perf screenshots with `PERF_SKIP_CHROME=true`, hidden details state can otherwise block map gestures. The app now ignores hidden details as a map-gesture blocker only in debug perf skip-chrome mode; production visible details still block map drags normally.
- `oneprewarm-street-on-nyc-z84-shellpan-20260614` active gesture logs were promising (`symbolCache=hit direct=0`, traffic mostly around 0.3-0.6ms), but the run is not acceptable because the bad frames came before touch and after the gesture (`symbolCache=miss reason=inactive direct=~619`, traffic roughly 8-24ms), and the motion-active artifact showed dot fallback at a zoom level where aircraft symbols should remain visible.
- If an optimization produces lower jank but a motion screenshot shows dots instead of aircraft symbols, edge aircraft staying dots during quick pan, outline flashes, dark/small symbols, line traces, map blanking, or satellite tile swaps, that run fails visual completeness and must not be counted as progress.
- Master/baseline comparisons are for visual consistency, feature completeness, data honesty, and accepted interaction behavior only. Master is not the performance target and may itself fail the 120Hz goal; the optimizer branch must match or exceed master visually while improving beyond master performance. Known optimizer-branch fixes may supersede master behavior, so do not regress protected branch fixes such as satellite tile continuity, no blanking, and no harsh flicker just to match master.
- Satellite tile transition verification must happen before the broad anti-jank loop whenever that item is active: use moving screenshots over real satellite imagery while panning/zooming and confirm there are no blank loading blocks, harsh imagery swaps, or flicker. The `keyboardzoom` perf mode is an acceptable satellite zoom-transition diagnostic only when it proves the zoom level changed; it is not aircraft morph approval, because morph approval still requires smooth touch-style two-pointer samples through the full dot-to-plane transition.
- Satellite flicker approval requires normal human-speed continuous touch-style zoom through the tile transition point, plus panning at and around that transition point. Slower zooms are useful diagnostics only if they reliably expose blinking; avoid relying on super-slow crawls or extreme quick pinches for approval. Quick pinches remain a stress test, but the main pass should match how a person actually pinches while inspecting satellite imagery and should prove the Google-Earth-style imagery handoff is clean.
- `RunFlightMapPerf.ps1 -NoScreenshots` is diagnostic only; screenshot-backed runs remain required for visual completeness. `noscreens-cacheinitialonly-street-on-nyc-z84-shellpan-20260614-121421` still measured 33/526 janky frames (6.27%, p50 7ms, p95 36ms, p99 48ms), so screenshot capture was not the main remaining tail in that case.
- `iter147-street-country-la-off-diag-noscreens` is useful only as a diagnostic that street active draw work can hold 120Hz present cadence without screenshot capture; it was run before reinstalling the source-reverted APK after `iter146`, so do not treat it as acceptance evidence for the current source until the app is rebuilt/reinstalled and retested.
- `RunFlightMapPerf.ps1` shell-pan gestures must stay on the real map surface, not over the persistent nearest-traffic/details panel. The left-map shell-pan route (`x` roughly 16%-48% of display width) fixed the earlier control-hit issue where the right-side pan started inside the details panel.
- `RunFlightMapPerf.ps1` pan routes also must stay vertically inside the open map area. Pre-fix DiagonalUp long pans started too low, over bottom controls or the nearest/details panel, producing invalid `interaction_active=false` catastrophic-looking satellite/layer results in `iter176`/`iter177`. The current safe band uses roughly y 30%-60% of the display height; validate with log evidence that the map actually moves before treating DiagonalUp results as renderer evidence.
- `iter178-pansafeband-satellite-country-on-noscreens` is invalid evidence: the run targeted the previous phone during a phone swap/disconnect and timed out after roughly 304 seconds. Do not count its frame data for or against the pan-safe-band fix.
- Canonical optimizer pan matrix: use `tools/perf/RunFlightMapPanMatrix.ps1 -Matrix Short` for most iterations and `-Matrix Full` before handoff/push, after substantial renderer/tile/feed/layer changes, and at least once every three to five quick optimizer test passes. Both matrices must exercise street and satellite view; a single-map-source run is diagnostic only and cannot be used to claim a completed iteration, FPS target, or visual parity. The short matrix should get roughly 90% of the useful signal quickly: wide/continent zoom around Dallas-Fort Worth, New York, Los Angeles, London, Paris, or another dense United States/European major-major corridor, country zoom around the same class of city/corridor, dot-to-aircraft transition zoom around Chicago/New York/London/Paris, and close-city zoom around a random United States/European major-major city center. Include both map modes, mostly human-like fast/medium pans, a country-level pulse or ease profile, a transition-zone stress pan, combined pinch-while-panning direction probes, and a restricted-layer spot check in both map modes. The full matrix is a compact stronger signal matrix, not an exhaustive cross product: keep it short enough to encourage more iterations, while covering wide/country/transition/close zooms, both street and satellite, restricted off/on, horizontal/vertical/diagonal directions, quick/medium/long human-like swipes, combined zoom+pan gestures, and pulse/ease/steady profiles. Super-slow pans are safely culled from the canonical matrix unless a specific investigation requires one; use bespoke diagnostics for rare exhaustive combinations instead of making every full run too long. Older ad hoc matrix commands are historical notes, not the canonical test settings.
- `RunFlightMapPerf.ps1` now supports `-PanDirection`, `-PanProfile`, `-PanLength`, and `-PanDurationMs`. `Steady` uses a plain shell-input swipe and is closest to the reliable real-phone pan path; `EaseIn`, `EaseOut`, and `Pulse` are segmented stress probes for frame-time signal, not proof of continuous one-finger physical motion. Pinch/morph approval still requires real two-pointer touch evidence, preferably UIAutomator instrumentation logs with `pointers=2`, plus motion frames through the full dot-to-aircraft transition.
- `iter08-instrumentation-pinch-perf-noscreens` and `iter09` panning logs show that system framestats latency columns can report p50/p95 values above 8.33ms while present cadence remains 8.34ms and app draw logs show active frames under budget. For smoothness claims, report both Android jank and present cadence, and use app `Debug draw perf` logs to separate active motion from pre-touch warmup or post-touch idle frames.
- `iter32-anchored-country-satellite-metrics` proved the instrumentation artifact path works, but it is invalid as optimization evidence: the target was Mexico City-class and therefore acceptable, but the synthetic pinch was still centered by the root/app bounds and visibly biased the zoom toward the Pacific/northeast instead of keeping the intended city/traffic focus under the open map area. Preserve the fix direction where perf launch places the target under the open-map focus and the pinch injector uses the same focus point.
- Country/continent zoom tests must preserve zoom-band-specific evidence. Do not collapse continent, country, and transition-band results into one aggregate summary only; frame/jank metrics should identify which scale band produced the bad frames, because wide dot-heavy views, country views, and dot-to-symbol/label/tile transition views can have very different bottlenecks.
- The current country/continent continuity instrumentation is intentionally center-map anchored for those tests. `iter230-centered-reverted-street-video` proved the old northeast/ocean drift was fixed by launching the target at physical center and reporting `focus=center-map`; however, its country phase had too little traffic wait and is not valid traffic evidence. Keep the centered focus behavior for these tests and do not go back to root/app-bound pinch centers.
- `iter231-centered-trafficwait-street-video` is the first useful centered street continuity pass after restoring the full traffic wait: Dallas-Fort Worth centered correctly, continent measured PresentDrop120Pct 27.12% with Android jank 90/305 frames (29.51%), and the country/global phase measured PresentDrop120Pct 2.54% with Android jank 80/667 frames (11.99%). Treat the continent band as the active weak scale for this evidence, and keep reporting country/continent separately.
- Centered isolation diagnostics after `iter231` show both map/tile and traffic work matter. `iter232-centered-street-skiptraffic` measured continent PresentDrop120Pct 8.40% and Android jank 23/343 frames (6.71%), while `iter233-centered-street-maponly` with `-SkipTraffic -SkipChrome` still measured continent PresentDrop120Pct 6.72% / Android jank 24/351 frames (6.84%) and country/global PresentDrop120Pct 17.65% / Android jank 51/567 frames (8.99%). Do not claim the remaining problem is only aircraft symbols; the street/base-map path also has a real 120Hz floor that needs a visually equivalent fix.
- During phone swaps, leave the installed Flight Alert app on the phone the user asks to keep. Future builds should install as normal updates on the currently connected physical test phone only after verifying the device id; do not uninstall old phones just to clean up tests.
- Current open optimizer item: country/transition pre-gesture aircraft-symbol overlay warmup can still create large UI-thread cache-recording spikes before the measured gesture, for example `iter09-noscreen-matrix-country-toronto-z54-street-layeroff-horizontal` logged a pre-touch 1014ms `symbolCache=hit direct=0` frame with about 5800 symbols, and `iter09-noscreen-matrix-transition-chicago-z84-street-layeroff-horizontal` logged a pre-touch 269ms cache-recording frame. Active panning after the cache is ready is smooth; the remaining work is startup/layer-warmup smoothness and faster first aircraft appearance without removing or simplifying the accepted aircraft visuals.
- Current open optimizer item: nearest-traffic/binCraft cadence must stay consistent. Lightweight binCraft snapshots should publish about once per second while the richer API/hybrid request is still pending, except during the first `BINCRAFT_API_GRACE_MS` where the API is expected to arrive soon. This must preserve already-real metadata/detail seeds from richer sources, must not create a one-second UI cache rebuild jank pattern, and must not replace API-enriched rows with avoidable `Unavailable` values.
- Current open optimizer item: full satellite continent/wide zoom still has traffic/symbol-cache jank even after the satellite tile transition fix. In `iter41-satellite-full-childfallback`, continent PresentDrop120Pct was 14.29% and Android jank was 29.98%; app logs showed wide-view bursts such as `symbolCache=miss reason=scale/active_coverage` with thousands of dots/symbols, while tile-only `iter39` proved the map path itself was much healthier. Optimize the wide aircraft overlay/cache path next without changing accepted aircraft dots, outlines, morphing, or information density.
- Current open optimizer item: active two-finger country/continent zoom still bottlenecks on traffic symbols, not map tiles alone. `iter203-country-zoom-street-perf-noscreens` hit continent PresentDrop120Pct 57.26% and Android jank 139/306 (45.42%) while country was 12.33% Android jank; `iter204-country-zoom-satellite-perf-noscreens` hit continent PresentDrop120Pct 50.85% and Android jank 149/428 (34.81%) while country was 13.01% Android jank. `iter205-country-zoom-street-skiptraffic-noscreens` dropped the street continent run to PresentDrop120Pct 5.04% and Android jank 31/310 (10.00%), proving traffic/symbol rendering is the primary shared bottleneck. Logs show `symbolCache=miss reason=scale` during active pinch with thousands of dots/symbols and 10-23ms traffic bursts. The fix must preserve the live zoom-governed morph, aircraft outlines, icon sizes, information density, and exact aircraft positions; do not use the rejected scaled-retained-bitmap shortcut.
- Current open optimizer item: full satellite transition-band composition still bottlenecks on traffic scale-miss direct drawing after the tile-only satellite handoff is clean. `iter261-satellite-layeralpha-currentlabels-video` measured tile-only PresentDrop120Pct 2.54%, Android jank 23/958 (2.40%), p50/p95 present 8.34ms, and p95 app frame 8ms with the accepted no-flicker retained satellite handoff. The newer full-chrome+traffic `iter262-satellite-layeralpha-full-video` over New York City improved greatly from `iter256` but still measured PresentDrop120Pct 23.73% and Android jank 194/839 (23.12%). Logs still show `symbolCache=miss reason=scale` and direct traffic/symbol-mask work around the transition while map and chrome are comparatively small. Optimize this with a visually exact traffic-symbol implementation; do not remove traffic, morphing, outlines, labels, or information density, and do not disturb the protected satellite tile-only transition fix to chase this traffic bottleneck.
- Current open optimizer item: dense wide-view dot cache recording has a real UI-thread spike that still needs a smarter equivalent fix. `iter241-current-trafficdetail-street-noscreen` logged `trafficDetail dotBatch=51.91ms` for about 11,882 dots, but the rejected async and direct-only experiments worsened broader frame cadence. Future fixes should avoid large UI-thread bitmap recording without adding background GC or changing accepted dot outlines, colors, sizes, or motion.

Smooth-map optimization target: panning, diagonal panning, quick pinching, slow pinching, zooming through all transition bands, pinch-while-panning in all directions, selection, details entry, street tiles, satellite tiles, and restricted/no-fly layers on and off should stay silky in dense traffic. Test the combinations while the screen is moving, not only while it is still: pan at different zoom levels, pinch at different zoom levels, combine pan and pinch in horizontal/vertical/diagonal directions, and use small, medium, and large zoom deltas across the transition ranges. Run fast human-like gesture probes and roughly half-speed diagnostic probes to expose issue bands for root-cause fixes. Cover street and satellite map modes with restricted/no-fly zones both on and off. Aim for 120 Hz-class interaction on capable real phones. Further optimizer passes should keep iterating until protected visuals are intact and the app is definitively above 100 fps in the full required settings matrix.

Current 120Hz optimization branch goal: `optimizer-120hz-map-tiles-layers` was created from feature-complete `master` commit `920845743204219ecac4ecf5a8e77bb462d920ce`. Master is the feature/layout comparison baseline, not the performance target, and known optimizer-branch fixes may supersede master behavior. Keep optimizing the test branch until real-phone panning, diagonal panning, quick/slow anchored pinch zooming, country-level zoom, transition-band zoom, street/satellite tile loading, restricted/no-fly layer loading, and layer toggles are 120Hz-class everywhere while preserving full feature completeness, real-data accuracy, and the accepted visual/UI behavior. Do not edit or merge into `master` until the user explicitly says to. If a protected visual causes measurable frame-time cost, replace the implementation with a smarter visually equivalent one rather than removing, simplifying, delaying, hiding, shrinking, darkening, or otherwise degrading it. Use videos plus same-run screenshots and frame/jank evidence against the saved master baseline on every meaningful iteration, while preserving protected branch fixes such as satellite tile continuity/no blanking/no harsh flicker.

120Hz/FPS measurement rule: never claim an FPS or frame-time goal is met from one zoom level. Every optimizer pass must measure a variety of zoom levels, including wide/continent, country, dot-to-aircraft transition, and close-city/local views, with panning and zooming variants over dense traffic. Every real performance test matrix must include both street view and satellite view; restricted/no-fly layer off/on must be covered before saying the branch is silky everywhere.

Active progress item, remove only when completed and verified: before writing renderer optimization code after the split-band checkpoint push, update the wide-ranging test/matrix reporting so it returns the same per-scenario frame/jank metrics now used by the focused gesture tests, including Android jank %, 120Hz present-drop/latency-miss %, p50/p95/p99 timings, and scenario labels that expose which zoom level, map mode, layer mode, direction, speed/profile, and gesture type caused slowdown.

Base-map disappearance reliability note for this optimizer pass: the earlier failure was a map/tile retention/loading bug where the base map layer disappeared or blanked while aircraft overlays remained visible, not aircraft disappearing. It is visually cleared for the current pass by the satellite/street moving and soak evidence listed above; reopen only if a new same-run screenshot shows real map tiles disappearing again.

Completed optimizer item: `iter134-satellite-country-zoom-maponly-video` caught a real satellite quick-zoom blank/fallback regression in `flightalert-perf-countryScaleZoomContinuitySatellite-country-quick-start.png`: the map area turned solid tan while logs showed `mapTiles=21 loaded=0 requested=21`, meaning no exact visible satellite tiles and no drawable retained/interim/parent fallback covered that frame. This is visually cleared for the current pass by `iter143-satellite-transition-grid-maponly-video`, which used no traffic/no chrome focused satellite transition motion, kept real satellite imagery filled in active zoom-out/zoom-in/pan-after frames, and preserved 120Hz-class map-only timing. Reopen only if a new same-run motion artifact shows app-caused blanks or harsh satellite color swaps.

Satellite tile patch verification contract: retained/interim raster tiles merge across recent zoom levels instead of clearing the whole interim tile set every time one complete exact tile frame loads. This preserves older lower-zoom imagery as a full-screen fallback during rapid zoom-out/zoom-in cycles while exact satellite tiles load. Physical-phone satellite pinch motion frames and satellite/street soak screenshots listed in `Optimizer Successes To Preserve` verify no blank map placeholders or restart-required tile disappearance in this pass.

Protected satellite success criteria: satellite imagery transitions are now protected behavior. Do not change or simplify the zoom-position handoff, retained real imagery, fast high-detail tile arrival, no blank loading blocks, and no harsh app-caused color/detail swaps in later optimization passes. If street-view or aircraft-overlay optimization touches tile drawing, retest satellite transition with normal human-speed pinch/pan motion and same-run screenshots before accepting the change.

Current open optimizer item: make street map view as smooth as the current satellite map path while preserving the exact accepted street-map visual quality, labels, borders, traffic overlays, and layer behavior. Use the same creative-reinvention rule: if street tiles or labels are a bottleneck, replace the implementation with a faster visually equivalent one, not by hiding labels, reducing map detail, or changing the look.

As a general optimization rule, solve the expensive work directly before reducing user-visible output. Prefer profiling, data-structure changes, caching, batching, precomputation, and drawing the same pixels more efficiently. If stuck, research how similar map/overlay/rendering systems solve the bottleneck before making visual tradeoffs. Do not remove visual fixes, lower information density, or add thresholds that make accepted behavior disappear unless the user explicitly asks for that tradeoff.

Delete temporary screenshots taken by agents after verification. Use bounded verify/fix loops: improve weak features, but do not get stuck forever and block handoff or release.

## 14. Security / Privacy

Prefer HTTPS-only APIs and assets. Do not hardcode API keys, tokens, secrets, or credentials.

Avoid cleartext traffic unless there is a documented reason. Lock-screen notifications should avoid exposing sensitive aircraft details unnecessarily.

## 15. Release Behavior

Before pushing: build, lint, inspect git status, check for duplicate project files, scan for obvious secrets, update README accurately, and clean generated junk.

Push only scoped, intentional app/source/docs changes. Only push important changes, or changes large enough to add major functionality.

## Newer Rule Priority

When the user gives a newer concrete rule that supersedes one of these rules, the newer rule takes priority. Update this file so later agents do not preserve obsolete guidance.

## Code Organization

Flight Alert code should read like objects doing things.

`FlightMapView` is the coordinator. It should coordinate map state, selection, draw order, and user input. Feature objects should own feed parsing, route validation, photo lookup, impact scoring, settings math, motion projection, and feature-specific business rules.

Prefer small objects with names that explain their job, such as `CurrentRouteValidator`, `AircraftPositionProjector`, or `AircraftRoutePresenter`. New feature work should usually start as a new object/file with a narrow public method, then be called by the coordinator.

Use underscore-style names for app-owned functions and properties as the code is refactored, matching The Cistern's readability style. Do not rename Android framework overrides, manifest entry-point class names, generated/build API names, or external source keys that must match provider data.

## Agent Workflow

Before changing code, read this file and `docs/code-organization.md`.

For optimization work, use a connected physical phone when one is available. Preserve a pre-optimization copy of the app before making changes. Compare against that copy or `master` so the app displays the same information and preserves the same user experience except for lag reductions and real bug fixes.

Use subagents more often for low-conflict work with a clearly bounded scope, especially read-only history/code research, test-harness additions, standalone verification helpers, and small files that can be owned independently. Put subagents in a separate workspace or explicitly assign a disjoint file set before allowing edits. Do not touch files owned by an active editing subagent until it reports back, unless the user explicitly redirects ownership. After spawning a subagent, confirm it has actually started thinking by waiting briefly for status or a useful result; if there is no evidence it started, send a nudge or treat it as not active. Close completed subagents after their useful result has been captured.

After meaningful changes, run:

```powershell
.\gradlew.bat assembleDebug
```

For refactors, compare against `master` on an emulator when practical. Treat styling or behavior drift as a regression unless it is explicitly tied to a bug fix or honesty fix.

When taking Android screenshots from PowerShell, prefer `adb shell screencap -p /sdcard/name.png` followed by `adb pull /sdcard/name.png local.png`. Direct `adb exec-out screencap -p > file.png` can produce PNGs that local image viewers fail to decode.
