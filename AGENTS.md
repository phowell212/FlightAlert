# Flight Alert Agent Rules

Flight Alert is a drone situational-awareness app. Refactors must preserve the current user experience and styling unless a visible difference fixes a real bug or honesty issue.

These rules are ordered by priority. If rules conflict, follow the higher-priority rule. Within the same priority, newer concrete user instructions supersede older ledger notes or older agent assumptions; update this file when that happens so obsolete guidance is not preserved.

## 0. Optimization Priorities And Master Baseline

For optimization work, preserve these priorities in order: 1) functionality to the end user, 2) visuals to the end user, 3) frame timing and implementation elegance. Performance work is only successful when the user-visible behavior remains functionally intact and visually faithful; FPS gains that introduce flicker, pop, missing layers, wrong motion, changed styling, or reduced information are failures.

Use `master` as the current required comparison point and implementation idea source. Do not rediscover performance gains from scratch when `master` already contains a useful strategy; inspect, port, adapt, or improve every safe master performance gain so this branch reaches master-level performance and then exceeds it while preserving the current accepted UX and visual/detail fidelity. If a master gain cannot be ported because it conflicts with current functionality, visuals, data honesty, or stability, record that reason in the ledger. If `master` is visually better for a protected surface, treat that as evidence to investigate and preserve. If `master` is faster because it omits newer accepted behavior or weaker visual guarantees, keep the newer accepted behavior and port only the underlying safe performance idea.

Treat the master-comparison stage as a finite optimization loop, not a permanent tunnel. Maintain a separate detached `master` worktree, normally `C:\Users\h\AndroidStudioProjects\FlightAlert-master-compare`, for read-only comparisons against the dirty optimization branch. Before inventing new renderer/backend strategies, inventory the compatible and notable master performance ideas by surface area, including map tile scheduling/fallback, satellite/reference rendering, street rendering, traffic state building, traffic overlay drawing, feed/cache behavior, and detail/chrome warm paths. Each master candidate must end in one ledger status: `ported/accepted`, `already present`, `not portable because ...`, `rejected with evidence`, or `deferred because ...`. When all compatible and notable master gains are exhausted, add an explicit ledger entry saying so, then retire or remove the active master-first procedural instructions from this file and the thread goal before starting a new self-directed optimization-discovery phase. Do not delete the accepted/rejected Optimization Ledger entries when retiring the master-first loop; those working and nonworking fixes remain the guide rails for future work.

The one-file consolidation exists to make cross-cutting renderer/backend flaws easier for Codex and Pro to see: logic that is far apart but behaviorally coupled should be identified and, when useful, moved closer or redesigned under the hood. The user is open to fundamental implementation changes, helper objects, and backend rethinks of rendering, data acquisition, caching, scheduling, and state flow as long as functionality and visuals are preserved with about 99% accuracy or better, the code philosophy/design priorities remain intact, and any visible difference is a deliberate bug fix, not a performance shortcut.

## 1. No Pretending

Never fake aircraft, map, route, photo, alert, altitude, location, or source data.

If real data is unavailable, show `Unavailable`, show `Loading`, hide the feature, disable the control, or label uncertainty clearly. Never use pregenerated aircraft, location, route, tile, or path data as if it were live.

## 2. Creative Reinvention, Never The Axe

Preserve the visible and user-facing result first, then reinvent the implementation underneath until it is fast.

When performance is bad, solve the expensive work directly with profiling, caching, batching, precomputation, better data structures, retained real imagery/data, smarter request scheduling, or another equivalent implementation. Do not win frame time by removing, hiding, delaying, shrinking, darkening, simplifying, skipping, or approximating accepted visuals, features, information density, layer content, aircraft morphing, map labels, tile transitions, details, photos, or alerts unless the user explicitly asks for that tradeoff.

If a protected visual or feature is a real bottleneck, replace its implementation with one that produces the same accepted pixels, information, timing honesty, and interaction behavior faster.

Optimization bands are diagnostic tools, not tradeoff zones. If a change improves one zoom band, map mode, gesture, or layer state by moving jank, flicker, missing information, or visual inconsistency into another band/state, reject it and keep digging for the root cause. A real optimization reduces or eliminates the underlying expensive work consistently while preserving the accepted user experience everywhere.

## 3. Real Sources Only

Map tiles must come from real map providers such as OpenStreetMap, CARTO, or real satellite imagery providers.

Aircraft traffic must come from live aircraft APIs or documented live feed formats. Flight paths must come from real trace/path APIs after an aircraft is selected. Do not accumulate app-session aircraft positions and call that a real flight path.

## 4. Safety Bias

Accuracy matters more than impressive visuals. Vertical separation is critical.

If altitude, location, source freshness, or feed data is missing, do not claim an aircraft is safe or inside/outside an alert volume unless the calculation is supported.

## 5. Alerts

Alerts are based on a 3D volume around the user: horizontal distance plus vertical separation.

Notify when an aircraft enters or leaves the configured alert volume. Do not spam repeat notifications for the same aircraft every interval. The persistent extreme-priority notification means non-clearable while active, self-clearing when the extreme-priority list empties, and text-updatable while active; it should only beep or buzz on the first transition from no extreme-priority aircraft to at least one extreme-priority aircraft.

## 6. UI Honesty

Anything that looks like a button must be interactable.

Unavailable or unverified features must be hidden, disabled, or labeled honestly. Do not show fake radar graphics or fake aviation overlays. Attribution can move later, but do not misrepresent providers.

## 7. Layout Quality

No text clipping, hidden labels, or strange overlaps.

Support portrait, landscape, folding devices, resizing, and emulator hardware input. Text should stay physically consistent unless constraints require smart adaptation. Back should close overlays/screens before leaving the app.

## 8. Map Interaction

Panning, pinch zoom, wheel zoom, and keyboard zoom should feel smooth.

Aircraft sprites may move smoothly every frame using interpolation from speed, heading, and last report. Do not request aircraft positions every frame. Sprite/dot morphing, icon sizing, outlines, satellite tile transitions, layer readability, aircraft details/photo loading states, and other accepted UI fixes are protected behavior. Optimize their implementations; do not optimize them out.

No aircraft sprite, dot, outline, or symbol gate may create a sudden apparent size jump anywhere in the zoom range. Transition starts, outline introduction, dot fading, symbol entry, and selected/priority styling must be continuous curves tied to zoom/appearance state.

## 9. Flight Paths

Only show the path button when a real usable path was retrieved.

The path must represent the selected aircraft's actual current flight/trace, not stale old legs. If the trace endpoint stops before the live sprite position, extend the visible trail to the current live position only when the live report is fresh. Fix path-jump root causes instead of hiding bugs with arbitrary implausible-jump filters.

## 10. Aircraft Details And Photos

Try exact aircraft photos first from real aircraft-photo sources.

If exact photo lookup fails, a representative same make/model photo is acceptable only with a clear "not this exact aircraft" note. Search-engine fallback photos must be labeled investigable and include source/proof view buttons. Claimed make/model/owner data must come from official or documented sources.

## 11. Military Handling

Only show military-specific stats if the aircraft is actually tagged military.

Military origin/base claims require real flight-origin and aerodrome/source data. Otherwise show unavailable or non-military status honestly. Registry-country fallback may use real ICAO 24-bit allocation ranges when registration prefix data is unavailable, but it must be labeled as an allocation-derived result.

## 12. Codebase / Repo Discipline

Use Kotlin and work directly in the Android Studio project structure.

Keep one real project: one manifest, one root Gradle setup, and no duplicate generated/source projects. Do not commit Android Studio local machine-state churn, build outputs, temporary screenshots, secrets, `local.properties`, generated junk, stale comparison artifacts, or agent scratch output.

## 13. Testing

Build before release. Use lint and unit/instrumented tests when the touched area warrants them.

Use emulators for visual inconsistency, layout, aspect-ratio, clipping, and theme checks. Use physical devices for optimization, timing, responsiveness, smoothness, and performance tests when available.

For map performance work, automated and manual panning/zooming tests must choose the test region from the traffic-density UTC timetable in the Optimization Ledger before choosing a city. Do not default to an old Denver scenario just because earlier artifacts used it. Exercise dense aircraft regions around a major city center with major airports/corridors in the active high-density region: European targets such as London, Amsterdam, Frankfurt, Paris, Madrid, or dense western/central Europe corridors during Europe-heavy windows; inland US targets such as Dallas-Fort Worth, Atlanta, Phoenix, Las Vegas, Chicago, New York, or Los Angeles during US-heavy windows when the route-proof envelope remains land/traffic centered. Full zoom-sweep evidence must use targets whose continent/wide view stays land/traffic centered. The test algorithm must keep pinch and pan gestures bounded around the target map area; choosing a valid start point is not enough if the scripted gestures drift into water or empty terrain. Do not test over open ocean, lake/sea drift, empty polar regions, unintended rural/upstate drift, upper Canada, smaller fallback metros, or the launcher/home screen; if a visible run drifts into those areas, discard that run as evidence and rerun.

Include quick anchored pinch gestures, smooth slow anchored pinch gestures, horizontal pans, vertical pans, diagonal pans, slight circular or elliptical map motion, and combined pinch-while-panning gestures over visible aircraft at multiple zoom levels. Pinch tests must keep the target city/traffic focus anchored so zooming back in returns to the same target.

Device perf commands must be deliberately bounded. Harness runs are capped at 3 minutes, sanity checks should usually target about 30 seconds of device/video motion, short visual checks should usually use 60 seconds or less, and thorough runs should usually stay at 150 seconds or less. Do not leave a hanging command running past 5 minutes; stop it, fix the harness or test philosophy causing the hang, and discard the partial run as evidence. Full-range zoom tests must relaunch or otherwise re-anchor over the timetable-selected US/EU target before reversing direction so zooming back in never lands over unrelated desert, ocean, sparse terrain, or a random map location.

Human-like motion is a first-class test requirement. Prefer slightly overlapping pan and pinch gestures with bounded, natural-feeling movement over mechanical sequences of isolated pan, pause, zoom, pause. Harness sanity checks should still exercise meaningful map motion. Prefer a short pan-plus-overlapped-zoom smoke test such as `satellitePanZoomSanityPerf`; use a pan-only test only when the explicit question is close-pan label stability.

Performance tests must separate results by scale band instead of hiding them inside one average. Always include known difficult bands around the 100 mi scale-bar range and the 200-500 mi scale-bar range, plus local, country, and continent/global checks in both street and satellite modes when the change could affect map/traffic rendering.

Version-to-version app comparisons must be thorough enough to support the claim being made. A quick bounded sanity run can reject an obviously bad change or guide the next investigation, but it is not enough to declare a pushed checkpoint, branch, or build broadly faster. For accepted comparisons, run comparable timetable-selected dense-region tests across the affected hard bands and map modes, include human-like overlapping pan/zoom motion, include roads/borders-on video inspection when satellite roads/labels/borders could be affected, and report produced FPS, present mean FPS, p50/p95/p99, Android jank, and route proof for each artifact.

At least one hotspot-finding test in an optimization cycle should sweep through the practical zoom range in and out, pan at representative zoom levels, and pan while zooming. The artifacts must make frame timing/FPS data correlatable to the active zoom band or phase. Treat those hotspots as starting points for broader root-cause investigation; do not patch a single band by reducing information, visual quality, morph continuity, outlines, labels, or layer fidelity.

For satellite tile-load work, include a fast close-to-wide zoom-out test that starts at close/high-detail satellite imagery over an inland major-airport traffic target, then quickly pinches out to country/continent scale and measures how long exact visible tiles remain unloaded. Use this to optimize real tile catch-up time; do not judge satellite load speed only from slow zooms or at-rest screenshots.

For visual completeness checks, compare against the latest valid feature-complete UI state. Capture or inspect the app at rest, right as movement starts, and during active movement so panning/zooming-only flashes, outline changes, tile flicker, or morph jumps are treated as regressions.

Satellite roads-on testing must include motion-video inspection, not just still screenshots, contact sheets, route proof, or aggregate layer counters. The June 21 manual user check caught road flicker in satellite view that prior evidence missed, so future harness/video review must look for frame-to-frame road opacity changes, road LOD scale swaps, whole-road-layer blink, parent/exact overlay crossfades, and label/border/road mismatches while panning and zooming with roads enabled. A test mechanism that cannot observe those temporal failures is incomplete even if frame stats and route proof pass. When this class of bug is found, fix both the renderer issue and the detection mechanism that missed it.

Every meaningful performance test should report frame-time and FPS evidence, not only subjective smoothness, screenshots, or a single jank percentage. Capture or summarize `gfxinfo`/FrameTimeline data when available, including p50/p95/p99 frame timing, mean/present FPS or FPS-equivalent cadence, produced FPS, Android jank percentage, and 120Hz present-drop or latency-miss percentage. Treat jank counters as supporting evidence, not the sole optimization target.

During device tests, accept runtime permission prompts that are specifically requested by the Flight Alert app so permission dialogs do not invalidate smoothness or visual captures.

When taking Android screenshots from PowerShell, prefer `adb shell screencap -p /sdcard/name.png` followed by `adb pull /sdcard/name.png local.png`. Direct `adb exec-out screencap -p > file.png` can produce PNGs that local image viewers fail to decode.

## 14. Code Organization

Flight Alert code should read like objects doing jobs.

`FlightMapView` is the coordinator. It should coordinate map state, selection, draw order, and user input. Feature objects should own feed parsing, route validation, photo lookup, impact scoring, settings math, motion projection, and feature-specific business rules.

Prefer small objects with names that explain their job, such as `CurrentRouteValidator`, `AircraftPositionProjector`, or `AircraftRoutePresenter`. New feature work should usually start as a new object/file with a narrow public method, then be called by the coordinator.

Keep settings and tuning values in explicit settings files. Give major features their own files or small packages, but do not create one-folder islands for tiny concepts.

## 15. Agent Workflow

Before changing code, read this file and `docs/code-organization.md`.

For optimization work, use a connected physical phone when one is available. Preserve a pre-optimization copy or branch before making changes. Compare against that copy or the latest accepted baseline so the app displays the same information and preserves the same user experience except for lag reductions and real bug fixes.

Normal optimization loop after master inventory:

1. The June 21 master-derived optimization inventory is exhausted and retired as the active tunnel. Keep `C:\Users\h\AndroidStudioProjects\FlightAlert-master-compare` read-only as a reference, but do not restart a full master-first pass unless `master` changes or a fresh hotspot directly points to a master implementation difference.
2. Use the pushed `one-huge-file` checkpoint after the master inventory as the normal comparison baseline for future work. The June 21 checkpoint tag is `checkpoint-one-huge-file-20260621-fair-master`; compare candidate changes against that tag or a newer accepted checkpoint, not against a half-tested local experiment.
3. For each new hotspot, start from profiling/detail evidence, run the Android Studio MCP function audit before hot renderer/perf edits, make the smallest behavior-preserving implementation change, build with Android Studio MCP when available, and validate on a physical device.
4. Version-to-version comparisons must be thorough. Quick sanity runs can reject a candidate or identify a hotspot, but accepted branch/checkpoint comparisons need comparable timetable-selected dense-region coverage across affected hard bands, map modes, and protected visual states, with video inspection where roads/labels/borders or other visual claims matter.
5. Record every accepted, rejected, or deferred optimization in the ledger with artifacts and exact frame evidence before switching targets or pushing a new comparison checkpoint.

Flight Alert is a consumer of shared MCP/Pro tooling, not the place where that tooling is developed. Do not add Pro bridge scripts, MCP proxy scripts, startup prompts, or bridge-status experiments here. Source-of-truth guidance for consuming those tools lives in `C:\Users\h\plugins\pro-agent-bridge\docs\CONSUMER-WORKSPACE-MCP.md`; bridge defects belong in `C:\Users\h\plugins\pro-agent-bridge`, not in this app repo.

For Android work on this machine, prefer the Android Studio MCP server when Android Studio is open. Use it to inspect real IDE state (`get_file_problems` with `errorsOnly: false`, open files, modules, dependencies, run configs, symbol info/refactors), make or reformat focused edits when useful, and build with `build_project` after code changes. Default project path: `C:\Users\h\AndroidStudioProjects\FlightAlert`; current high-traffic Kotlin file: `app/src/main/java/com/flightalert/FlightAlertRewritten.kt`. If the Android Studio MCP is not exposed in the active tool surface, use the bridge-owned consumer wrapper from this project root instead of copying wrapper logic into Flight Alert.

For renderer/performance work, Android Studio MCP function-specific audit is required before speculative hot-code edits, not optional. Use native `android_studio` tools when exposed: `search_symbol` to locate the hot function and exact line range, `get_symbol_info` on the function declaration position, `get_file_problems(errorsOnly:false)` on the file, `search_regex`/`search_text` when available for callers, related state, timing logs, and rejected experiments, and `build_project` after edits. Keep Gradle/ADB/logcat as runtime evidence, but use MCP to reduce wrong edits in complex Kotlin/Java surfaces. Do not replace the required audit with plain grep unless Android Studio is closed or MCP is genuinely unavailable; grep may supplement the audit, not substitute for it. If native `android_studio` tools are not exposed in the active chat, run:

```powershell
node C:\Users\h\plugins\pro-agent-bridge\scripts\android-studio-mcp-call.mjs --function-audit draw_aircraft_symbols --file app/src/main/java/com/flightalert/FlightAlertRewritten.kt --max-results 5 --timeout 60000
```

This function audit must succeed for the function being changed, or the agent must state the exact blocker. For timing analysis, first identify the hot function from logs, `gfxinfo`, or perf artifacts; audit that function; search for existing timing instrumentation such as `debug_detail`, `elapsed`, `SystemClock`, `trace`, `perf`, or the function name; add narrowly scoped timing only around the exact function or branch if timing is missing; then run the bounded physical-device harness, compare p50/p95/p99, present mean FPS, produced FPS, jank, and route proof, and record the result in this ledger.

Use subagents only for low-conflict work with a clearly bounded scope, especially read-only history/code research, test-harness additions, standalone verification helpers, and small files that can be owned independently. Put subagents in a separate workspace or explicitly assign a disjoint file set before allowing edits. Do not touch files owned by an active editing subagent until it reports back, unless the user explicitly redirects ownership.

When Pro mode / Pro Agent Bridge is available and useful, use it for parallelizable, high-value analysis such as independent diagnosis, visual/performance strategy, UI/architecture critique, or test-result review. Follow the bridge-owned consumer guide above: keep Pro use scoped and advisory, attach or summarize only selected evidence, prefer native registered MCP tools in fresh/reloaded sessions, avoid repeated tiny asks, and accept a Pro result only when a new complete assistant answer is read back for the current prompt.

Latest user instruction on June 21, 2026: Pro mode is enabled for Flight Alert optimization work. Use native Pro tools when healthy, or the bridge-owned wrapper path from the consumer guide when native tools are unavailable.

When asking Pro to compare branches, give Pro explicit evidence from both branches: scoped diffs, `git show master:path` snippets, screenshots/videos, logs, and artifact summaries. Do not assume Pro can see local branches, the filesystem, or Git history unless Codex uploaded or inlined the relevant materials. For Flight Alert optimization work, include the current branch snippet, matching `master` snippet or trick inventory, accepted-current visual constraints, and the specific question Pro should answer.

For hard optimization problems, prefer asking Pro for candidate solutions before implementing a new substantial renderer/backend strategy, then synthesize and implement only the candidates that fit the exact-fidelity contract and current evidence. Also cue Pro on broader performance optimizations and on specific visual/performance failures that have already survived one local fix attempt. If a Pro call actually fails, returns no fresh assistant answer for the current prompt, or the local evidence already makes a solution clear enough to synthesize safely, continue locally and document that reason when it matters.

Recommended Android loop: inspect IDE diagnostics through Android Studio MCP when available, make focused edits, validate with Android Studio diagnostics/build tools, then use physical-device ADB evidence for performance and visual claims. Use Pro when its larger context, independent review, or broader optimizer perspective materially helps.

After meaningful changes, run:

```powershell
.\gradlew.bat assembleDebug
```

When an optimization attempt works, fails, is reverted, or remains pending, record it in the Optimization Ledger below with the scenario and measured evidence. This is required before context compaction, handoff, or switching to a different optimization target.

## 16. Optimization Ledger

Record only meaningful optimization evidence here. Do not keep rejected experiments in source just because they were tried.

Current target: resume the normal evidence-driven optimization loop toward 120 Hz/120 fps while preserving the accepted Flight Alert UX, functionality, visual/detail fidelity, and the current fixed no-blink satellite roads/labels/borders baseline. A single local/close-pan or sanity result is only a narrow gate, not acceptance. Accepted version comparisons require physical-device human-like zoom in/out evidence across the practical zoom range, including local/close, the 100 mi scale-bar band, the 200-500 mi band, country, continent/wide, and the dot-to-aircraft morph transition range, with frame evidence separated by scale band and no major performance regression. Choose the region for each new run from the UTC traffic-density timetable below so the test keeps real aircraft on-screen.

Traffic-density geography clock map for performance target choice:

- Use this as a density heuristic, then confirm with a quick live-feed sanity check before treating a run as comparable. The goal is to keep enough real aircraft on-screen to stress map and traffic rendering even during weird low-traffic hours for the US. Do not compare a sparse overnight US run against a dense European daytime run as an optimization win or loss unless the traffic count/visual density is intentionally part of the scenario.
- This timetable is the target-selection rule for new optimization evidence. Historical Denver artifacts in this ledger are past evidence only, not instructions to keep testing Denver.
- All times below are UTC. During US daylight time, Eastern time is UTC-4; during central European summer time, Paris/Amsterdam/Frankfurt/Madrid are UTC+2 and London is UTC+1. Recompute offsets after daylight-saving changes.
- `05:00-10:00 UTC`: Europe is usually the better dense-traffic target. This is `01:00-06:00 ET`, when US traffic is in the overnight lull, while Europe is ramping into morning (`06:00-11:00 London`, `07:00-12:00 CEST`). Prefer London, Amsterdam, Frankfurt, Paris, Madrid, or dense western/central Europe corridors. This matches the June 21 observation that around `02:00 ET` Europe is a better stress target than the US.
- `10:00-15:00 UTC`: both regions can be useful. Europe is still strong through midday/afternoon, while the US East Coast and central hubs are ramping. Use this window for Europe-vs-US robustness checks, but keep artifacts separated by region.
- `15:00-22:00 UTC`: US is usually the better dense-traffic target. This is US late morning through evening and Europe evening into night. Prefer inland/high-density US targets such as Dallas-Fort Worth, Atlanta, Phoenix, Las Vegas, Chicago, New York, or Los Angeles when the route-proof envelope and map focus remain land/traffic centered.
- `22:00-05:00 UTC`: US density tapers from evening into overnight; Europe is mostly night until the morning ramp. Use US west/central/east metros early in this window, then switch to Europe once the UTC clock approaches `05:00`. Avoid oceanic transatlantic-only traffic as primary perf evidence because the map rules require land/traffic-centered runs, but use the eastbound/westbound transatlantic banks as a reminder that traffic can migrate between regions and affect density.
- For any optimization claim, record the test region, local clock time, UTC time, city/corridor, zoom band, aircraft count/density, map source, and whether the region was chosen for density or for a sparse-control comparison.

Branch comparison evidence:

- `test/local-perf` working tree versus clean `master` on physical Fold `RFCX40KPN3B`, Denver, `countryScaleZoomContinuityStreetPerf` / `countryScaleZoomContinuitySatellitePerf`, 120 Hz summaries, June 19, 2026: street continent/wide improved on the test branch from 33.62% to 5.04% present-drop120, 27.68% to 19.95% Android jank, and 75.05 ms to 16.68 ms present p95. Street country worsened from 5.04% to 15.25% present-drop120, 8.96% to 20.31% Android jank, and 33.36 ms to 83.41 ms present p95. Satellite regressed in that A/B: continent 50.43% -> 99.02% present-drop120 and 83.4 ms -> 183.5 ms present p95; country 4.2% -> 95.28% present-drop120 and 8.34 ms -> 183.45 ms present p95. Important caveat: `master` uses an older harness and often draws fewer accepted visual/test states, so low master timings are not automatically feature-complete wins. Treat the street-wide improvement as useful evidence and the country/satellite numbers as hotspots to investigate, not as permission to drop visuals.
- Read-only `master` trick inventory, June 21, 2026: use `master` as a required performance and visual comparison target plus an implementation idea source. Useful tricks to preserve/beat include the split renderer architecture; retained satellite interim raster tiles during LOD changes; priority tile executors with separate satellite/reference workers; parent and child fallback for base map tiles; reference overlay LOD readiness checks with center coverage; dense traffic dot batching; aircraft symbol mask LRU caching; Matrix-based symbol drawing; and the aircraft symbol pan-cache/idle-guard path. Important differences: `master` reference labels/borders do not protect visible reference tiles from shared LRU eviction, allow a 92% ready LOD switch, keep broad stale high-detail reference LOD retention, and have reference parent fallback disabled. Port the safe performance ideas from `master` instead of rediscovering them, but require this branch to beat or match `master` frame evidence while preserving the current accepted satellite roads/labels/borders behavior and the June 21 fixed no-blink baseline.
- Master traffic-symbol cache comparison, June 21, 2026: native Android Studio MCP could inspect only the current open project and reported the master compare worktree was not an open Android Studio project; fallback/static source audit was used for `C:\Users\h\AndroidStudioProjects\FlightAlert-master-compare`. Status for active scaled symbol-overlay bridging: `already present/current ahead of master`, because current `draw_symbol_overlay_pan_cache` can bridge bounded interaction scale while master returns `reason=scale` for any non-1.0 transform. Status for dense symbol zoom reuse: `already present/current ahead of master`, because current `shifted_dense_symbol_overlay` allows bounded interaction zoom reuse while master still has an unconditional `if (zoom_changed) return null`. Status for master's synchronous full-overlay `record_symbol_overlay_cache`: `not portable as-is`; it appears unused in master, draws a full transparent overlay tile, records all aircraft as cached, and would risk stale moving-aircraft positions. Current already rejected full transparent symbol-overlay draws and synchronous record-as-draw experiments with physical evidence. Do not revive that path without a new exact-motion design.

Active master optimization checklist, started June 21, 2026 per user request to list the working master-branch optimizations and exhaust them one by one:

- `exhausted/retired as active workflow`: the master-derived checklist has no remaining safe missing master gain after the June 21 static source audit and current-vs-master classification. Use the pushed `one-huge-file` checkpoint after this ledger update as the normal comparison baseline, and use master only as a read-only reference or when a fresh hotspot points back to it.
- `exhausted as already present/current ahead`: active scaled symbol-overlay bridge, shifted dense-symbol zoom reuse, dense-symbol motion-padding, and active-gesture exact-fidelity diagnostics. Current has the safe active-gesture reuse that master lacks; master synchronous full-overlay recording is not portable as-is and overlaps with rejected full transparent/synchronous recording experiments.
- `exhausted as already present/current ahead`: retained satellite raster handoff/interim tiles. Static source audit matched master's retained-raster handoff family, while current also has the later rectangle reuse and stricter reference/transportation visual guards. Do not remove current guards to chase master's lower visual burden.
- `exhausted as already present/current safer`: reference overlay LOD planning, center-coverage readiness, visible-reference protection, parent fallback, and transportation motion-alpha hold. Master is faster partly because it is looser here, so do not port master's 92% commit, broad stale retention, or disabled reference parent fallback as a performance gain.
- `exhausted as already present`: satellite/base parent and child fallback plus priority satellite/reference worker pools. Current monolith contains the same renderer-owned executors/fallback structure, with current reference fallback depths and visual protection intentionally safer than master.
- `exhausted as already present`: street tile provider/fallback/worker path. June 21 source audit matched master split `StreetMapTileRenderer.kt` to current monolith: CARTO Voyager cache keys, 6-worker priority tile queue, background disk executor, newest visible request priority, parent prefetch priority/depth 3, parent fallback, and child mosaic fallback are already in current.
- `exhausted as already present/current ahead`: dense traffic dot batching and async dot-overlay recording. Current retains master's chunked dot overlay recording budget/clear path and adds newer traffic-detail timing, dense-symbol motion padding, and active bridge diagnostics, so there is no remaining master-only dot-cache import.
- `exhausted as already present`: count-only spatial-index probing, aircraft symbol mask LRU caching, Matrix-based cached-symbol drawing, rotorcraft rendering support, and the 768-entry aircraft symbol body-path cache are all present in current.
- `exhausted as organization-only for performance`: split renderer architecture. `docs/code-organization.md` still wants renderer extraction after the data shape is stable, and master keeps renderers in separate files, but current contains the same renderer classes inside `FlightAlertRewritten.kt` for cross-cutting optimization work. File splitting alone is not a proven frame-time optimization; only perform a local extraction if it enables a measured hot-path change without behavior changes.
- `exhausted as already present`: feed/cache and detail/chrome warm paths. Static source audit found master split classes for details warm cache/requester/prefetch, details/photo coordination, photo cache/fetcher, settled aviation-layer cache, and `FlightMapChromeRenderer`; current has the matching classes in the monolith. Treat future edits here as independent profiling work, not master-port work.
- `accepted narrow current-only keeper`: reference request-history pruning is not a master-only optimization; master has the same repeated oldest-entry scan. Current replaces repeated oldest-entry scans with one stale-removal pass plus one oldest-key sort/removal pass and keeps debug-only history counters under `MapDetailTiming`. Android Studio MCP build passed. Physical Fold, timetable-selected Atlanta, roads/borders on, `tools/perf/out/us-atlanta-satellite-panzoom-mapdetail-refprune-20260621-1711/`, route proof passed (`samples=6`, `maxDistanceKm=45.1`), and the comparable heavy reference timing gate improved versus `refprotect`: produced FPS `27.7 -> 30.1`, present mean FPS `27.9 -> 30.7`, present p95 `99.98 -> 83.34 ms`, Android jank `98.15% -> 91.53%`, while the overflow sample reduced `refGen` max from `27.99 ms` to `4.70 ms` with `refHistStart=3249`, `refHist=2400`, `refStale=460`, `refOverflow=389`. This is only a narrow reference-history keeper, not a broad app-version win; broad checkpoint comparisons still require the thorough multi-band evidence rule above.

Keeper candidates currently allowed on this clean branch:

- Still-screen and zoom-transition satellite label/border flicker fix, June 21, 2026: root cause was not one paint bug; it was unstable reference-overlay residency and LOD selection. Visible boundary/place reference tiles could be evicted from the shared reference LRU while roads/prefetch work was loading, and the draw planner could switch or retain a not-quite-complete LOD during zoom (`92%` ready or stale high-detail retention), with reference parent fallback unavailable. The accepted fix protects the actually drawn visible reference keys plus the current target LOD across the previous/current frame eviction window, commits LOD switches only when the target is fully loaded and fade-ready, excludes non-holdable stale previous LODs from fallback selection, limits stale high-detail retention to nearby zooms, enables real parent fallback for reference overlays, and uses visual fallback coverage for overlay alpha. Exact still evidence on physical Fold `RFCX40KPN3B`: `tools/perf/out/codex-still-label-flicker-z9801991-depth4-forced/`, launched at `PERF_ZOOM=9.801991`, satellite, roads/borders on, Baltimore/Chesapeake focus; after the initial warm frame, logs remained `labels=160/160`, `borders=80/80`, `roads=80/80`, `lod=10`, `req=0`. Zoom evidence: `tools/perf/out/codex-zoom-label-flicker-z9801991-depth4/`, same launch zoom/location with keyboard plus/minus zoom sequence; contact sheet showed no visible border/label blink-out, final exact zoom settled back to full `160/160`, `80/80`, `80/80`, and transient missing exact tiles were covered by reference fallback rather than disappearing. Use this pushed state as the visual/performance baseline before attempting more optimizations.
- Satellite roads-on motion flicker keeper, June 21, 2026: the user's manual satellite-roads check exposed a blind spot where stills, route proof, and aggregate counters could pass while roads flickered frame-to-frame during motion. Root cause was the transportation overlay's coverage alpha being recomputed directly from live exact/parent fallback coverage while panning and zooming, so temporary coverage fluctuations could reduce or blink the road layer even when real usable visual coverage was present. `SatelliteMapTileRenderer` now holds the previous transportation coverage-alpha floor during active interaction when full/center visual coverage is sufficient, while still allowing normal non-interactive alpha and not drawing fake roads. The debug summary now logs both `coverageAlpha` and `rawCoverageAlpha` so future tests can catch held-versus-raw behavior. The harness now requires roads-on satellite video evidence to produce a `road-motion-strip.jpg`, documents the temporal checks in route proof, fixes ffmpeg invocation so banner stderr cannot skip strip generation, and shortens short-run foreground video hold (`MaxRunSeconds=60` now holds 15 s rather than 25 s) so bounded tests do not consume their own timeout. First attempt `tools/perf/out/satellite-road-flicker-fix-20260621-110043/` is explicitly rejected as evidence because instrumentation timed out and no road-motion strip was generated, though its route proof stayed over timetable-selected Atlanta. Accepted physical Fold evidence `tools/perf/out/satellite-road-flicker-fix-20260621-110339/` ran in 43.5 s over timetable-selected Atlanta at UTC `15:03`, `route_focus_passed=True`, `samples=9`, `maxDistanceKm=93.8`, `accepted_optimizer_evidence=true`, and saved `satellite-road-flicker-fix-20260621-110339-road-motion-strip.jpg`; strip inspection showed continuously visible roads through human-like pan/overlapped zoom, with no whole-road blink or opacity pulse. Log evidence during the risky zoom-in included raw transportation coverage dropping to `0.00` while held `coverageAlpha=1.00`, preserving visible roads instead of blinking. Performance for this map-only/roads-on visual gate was produced FPS `44.7`, present mean FPS `42.4`, present p95 `91.65 ms`, Android jank `49.60%`; keep optimizing performance separately, but do not remove this motion-alpha hold or the road-motion-strip evidence gate without a replacement that catches the same temporal class.
- Traffic symbol-cache miss diagnostic keeper, June 21, 2026: `FlightAlertRewritten.kt` now resets stale `debug_symbol_cache_miss_detail` at symbol-cache entry and, only when detail timing is enabled, logs active visual-bridge miss detail (`markerD`, scaled `iconD`, scaled `dotD`, signature/theme/appearance match, center/dimension flags). This fixed a mechanism blind spot where `active_visual_change` did not say which exact fidelity key blocked reuse. Physical Fold diagnostic `tools/perf/out/atlanta-regional-symbolmiss-detail-20260621-1124/` used timetable-selected Atlanta at UTC `15:25`, satellite roads/borders on, full traffic detail, completed in `39.5 s`, passed route proof (`samples=6`, `maxDistanceKm=59.0`, `accepted_optimizer_evidence=true`), and measured produced FPS `25.7`, present mean FPS `27.3`, present p95 `99.98 ms`, Android jank `97.32%` under heavy detail instrumentation. Evidence showed active misses were not signature/theme/appearance churn (`sig=true app=true theme=true`) but visual-key deltas: scale `0.9` had `markerD=17 iconD=12 dotD=2`, scale `1.0` had `markerD=13 iconD=47 dotD=51`, and scale `1.6` had `markerD=1 iconD=248 dotD=347`. The same run also exposed that some nominal cache hits had only `cached=24` and still direct-drew about `1.7k` moving/excluded aircraft, so the remaining structural problem is not a simple scale threshold; it is exact-fidelity handling of active morph/scale changes plus moving-aircraft direct fallback. Avoid repeating threshold widening, full-overlay transparent draws, synchronous recording, or direct sprite/atlas caches unless a new profile proves color/key reuse and memory safety.
- Dense-symbol query motion-padding keeper, June 21, 2026: replace the fixed `180dp` shifted dense-symbol grid query extra with exact render padding plus a cache-age-bounded motion pad derived from the cached grid's max axis velocity and max motion limit. The accepted invariant is that the shifted query remains a superset before the final exact draw cull: render padding still covers max symbol/selected-ring geometry, and motion padding covers cached screen-point displacement since the dense cache was built. This removes unnecessary overfeed into the shifted-state/direct loop without hiding aircraft or changing symbol/morph pixels. Physical Fold `RFCX40KPN3B` timetable-selected Atlanta detail gate `tools/perf/out/atlanta-regional-densesymbol-motionpad-20260621-1150/` completed in `46.3 s`, passed route proof (`samples=8`, `maxDistanceKm=84.3`, `accepted_optimizer_evidence=true`), and improved against the heavy diagnostic `atlanta-regional-symbolmiss-detail-20260621-1124`: produced FPS `25.7 -> 50.6`, present mean FPS `27.3 -> 36.9`, present p95 `99.98 -> 66.66 ms`, Android jank `97.32% -> 70.19%`, `directSymbols` max `27.57 -> 15.82 ms`, avg `11.15 -> 7.16 ms`, and average direct-drawn count about `1948.9 -> 1268.1`. Visual evidence `tools/perf/out/atlanta-regional-densesymbol-motionpad-video-20260621-1155/` passed route proof (`samples=18`, `maxDistanceKm=104.8`); the raw MP4 had launcher tail after the active map segment, so use the active trim/contact sheet (`*-active-map-trim.mp4`, `*-active-map-contact-sheet.jpg`) plus `*-road-motion-strip.jpg` as the clean visual evidence. The active contact sheet stayed on the SE US/Atlanta traffic region with dense aircraft during pan/zoom, and the road strip showed no obvious satellite roads blink. Reject `tools/perf/out/atlanta-regional-densesymbol-motionpad-video-r2-20260621-1159/` as visual evidence because it captured launcher/startup/loading frames. Cross-checks did not show breakage: `tools/perf/out/atlanta-pan-across-zoom-motionpad-20260621-1203/` passed broad `panAcrossZoomLevels` route proof (`maxDistanceKm=307.7`, produced FPS `192.8`, present mean FPS `59.0`, p95 `66.67 ms`, jank `50.26%`) but was street-style only; `tools/perf/out/atlanta-country-satellite-motionpad-20260621-1207/` passed satellite country proof (`maxDistanceKm=291.3`, produced FPS `91.5`, present mean FPS `68.8`, p95 `58.32 ms`, jank `32.89%`). Remaining direct-symbol spikes from `active_visual_change`/scale misses still need a separate exact-fidelity design; do not treat this keeper as solving the active visual-key mismatch family.
- Close zoom fast-pan satellite label/border flicker fix, June 21, 2026: physical-device MP4 evidence over Denver at `z12.2`, satellite, roads/borders on, showed the remaining fast-movement root cause was not missing tiles outright; the reference planner could temporarily drop the entire transportation and boundary/place overlays from target `lod=12` to coarser `lod=11` while the same viewport still had complete real visual coverage via exact tiles plus parent fallback. That cross-layer LOD scale swap made roads/labels appear to flicker or pop during fast panning. Fix: when the target close-pan LOD was already selected, keep drawing that target LOD if exact plus real parent fallback covers the full viewport and center, instead of selecting a coarser committed LOD. Baseline artifact `tools/perf/out/direct-closepan-label-flicker-baseline-denver-z122-video/` had `roads_lod11_lines=3`, `borders_lod11_lines=3`, `roads_fallback_lines=12`, `label_fallback_lines=8` in z12.2 draw logs. Fixed video artifact `tools/perf/out/direct-closepan-label-flicker-holdlod-denver-z122-video/` had `roads_lod11_lines=0`, `borders_lod11_lines=0`, `roads_fallback_lines=4`, `label_fallback_lines=2`; the contact sheet no longer showed the coarse road/label scale pop. Fixed no-video repeat `tools/perf/out/direct-closepan-label-flicker-holdlod-denver-z122-novideo/` also held `roads_lod11_lines=0` and `borders_lod11_lines=0`, with remaining fallback frames staying at `lod=12`. Keep this same-LOD visual-hold rule for close fast pans unless future video shows it causing stale or fake labels.
- Map-label reference overlay stability keeper, June 20/21: `SatelliteMapTileRenderer` now chooses reference road/border LODs using full-viewport plus center-viewport readiness, retains the last drawable LOD until the replacement is ready, runs quiet prefetches that upgrade to exact redraws when visible, and same-LOD overscans reference tiles ahead of panning. The later June 21 no-blink fix supersedes the earlier no-parent-fallback assumption by enabling real reference parent fallback only to cover otherwise missing exact tiles during zoom/load transitions. This specifically preserves the accepted local/reference map-label visuals while preventing road/street-label dropout and tile-box road endings. Physical Fold `RFCX40KPN3B` clean close-pan video `map-labels-close-pan-r4-satellite-labels-overscan-clean` passed route proof over Denver, showed stable roads/street labels/borders in sampled MP4 frames, and logged 99 debug samples with `labelPartial=0`, `roadsPartial=0`, `roadsCenterPartial=0`, `roadsReqFrames=0`, `bordersPartial=0`, and `bordersCenterPartial=0`; frame evidence was produced FPS 619.4, present mean FPS 70.7, present p50 8.33 ms, present p95 49.99 ms, Android jank 19.54%. The same build's panel-on close-pan run `map-labels-close-pan-r3-satellite-labels-overscan` measured produced FPS 998.7, present mean FPS 89.2, present p95 16.67 ms, Android jank 15.94%. Fast zoom-out hotspot video `map-labels-fastzoomout-road-hotspot-r24-maponly-overscan` still had expected edge/fade partials during rapid zoom but retained complete center coverage and visually avoided the prior missing-label/box-edge road artifacts; keep investigating broad zoom-out frame time, but keep the reference overlay stability path unless a future video proves a regression.
- Reusable `Matrix` drawing for cached aircraft symbol masks. It preserves the same fill/stroke masks and transform while reducing per-symbol canvas state churn. Bounded DFW z5.4 street diagonal run `opt-matrix-symboldraw-dfw-z54-street-diag-real` measured Android jank about 11.28%, present-drop120 about 8.55%, present p50 about 8.34 ms, present p95 about 41.62 ms. Same-target A/B with the old save/translate/scale/rotate path, `opt-ab-savecanvas-dfw-z54-street-diag-real`, worsened to about 22.22% Android jank and 20.43% present-drop120. Keep the Matrix path unless a separate visual/video check catches a transform mismatch.
- Count-only spatial-index probing for dense traffic state. It preserves the same padded aircraft set and same rendered aircraft pixels, but replaces the first allocating `TrafficSpatialIndex.query()` used only for density/symbol decisions with `query_count()`, then performs the real padded query once. Real-phone Denver z6.2 satellite full-traffic run `traffic-countquery-denver-z62-satellite-real` improved the immediate comparison from about 55.56% Android jank / 58.33% present-drop120 / 91.72 ms present p95 to about 21.43% / 22.94% / 50.04 ms, with motion screenshot visually intact and labels at `288/288 labelReq=0`.
- Active pinch dense-symbol overlay bridge, June 21, 2026: during active map interaction only, dense traffic can reuse the padded symbol state cache across bounded zoom deltas and draw the retained symbol bitmap when the scaled visual buckets still match closely. This keeps the same aircraft/dot morph detail instead of suppressing symbols, but avoids repeated direct redraws of about 4k aircraft while a human-like pinch is in progress. Physical Fold `RFCX40KPN3B`, UTC `08:48`, timetable-selected Frankfurt/Europe, satellite, roads/borders on, no-video country-band comparison: baseline `tools/perf/out/eu-frankfurt-country-satellite-perf-novideo-r5/` measured produced FPS `39.3`, present mean FPS `37.3`, present p95 `66.69 ms`, Android jank `43.10%`, and logs had `symbolCache=miss reason=active_visual_change direct=4105-4299`; fixed `tools/perf/out/eu-frankfurt-country-satellite-perf-novideo-r6-symbolbridge/` measured produced FPS `112.1`, present mean FPS `85.6`, present p95 `33.33 ms`, Android jank `20.16%`, with active frames at `symbolCache=hit ... direct=116`. Visual evidence `tools/perf/out/eu-frankfurt-country-satellite-symbolbridge-video-r1/` recorded a 29.6 s MP4 and `eu-frankfurt-country-satellite-symbolbridge-video-r1-strip.jpg`; contact-sheet inspection showed Flight Alert over Europe/Frankfurt with dense aircraft, no launcher/home screen, and no Sahara/random-location drift. Keep this bridge separate from the rejected idle symbol-cache reuse experiments below; this keeper is active-gesture-only and does not reintroduce idle refresh.
- Satellite tile/reference draw-loop rectangle reuse, June 21, 2026: `SatelliteMapTileRenderer` now reuses renderer-owned `RectF` instances for base tiles, reference overlay tiles, interim tiles, and child fallback rectangles instead of allocating new rectangles in the hot draw loops. This does not change tile selection, alpha, fallback, labels, roads, borders, or pixels; it only removes per-frame allocation churn. Physical Fold `RFCX40KPN3B`, timetable-selected Frankfurt/Europe, satellite, roads/borders on: close-band no-video `tools/perf/out/eu-frankfurt-close-satellite-perf-novideo-r2-rectreuse/` improved over `r1` from present mean FPS `57.1` to `70.3`, present p95 `49.99 ms` to `41.66 ms`, Android jank `41.27%` to `34.52%`, and mean map time `9.65 ms` to `8.51 ms`. Fast zoom-out no-video `tools/perf/out/eu-frankfurt-fastzoomout-satellite-perf-novideo-r3-rectreuse/` improved over `r2-symbolbridge` from present mean FPS `70.5` to `76.8`, Android jank `38.32%` to `27.11%`, and mean map time `9.04 ms` to `8.37 ms`; p95 stayed `33.33 ms`.
- Aircraft symbol body path cache keeper, June 21, 2026: `AircraftSymbolRenderer` keeps `SYMBOL_BODY_PATH_CACHE_MAX_ENTRIES = 768` rather than the previous `512` so dense regional symbol/morph buckets do not churn reusable body paths. This does not change aircraft shapes, colors, sizing, morphing, or filtering; it only keeps more already-built real symbol paths resident. Physical Fold, timetable-selected Frankfurt/Europe, satellite roads/borders on, regional/100 mi `satellitePanZoomSanityPerf`: current 768 run `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r7-pathcache768-current/` measured produced FPS `56.3`, present mean FPS `41.2`, present p95 `58.34 ms`, Android jank `86.34%`; temporary 512 comparison `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r8-pathcache512/` measured produced FPS `45.3`, present mean FPS `38.6`, present p95 `58.33 ms`, Android jank `85.71%`. Keep 768 because it improves produced and present mean cadence without visual-surface changes, while continuing to investigate the regional present-frame bottleneck separately.
- Regional/100 mi traffic bottleneck diagnostic, June 21, 2026, not a code keeper by itself: bounded physical Fold Frankfurt/Europe `satellitePanZoomSanityPerf` isolation showed traffic is the current regional limiter, not chrome. Same timetable-selected route with `-SkipTraffic`, `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r9-skiptraffic-diagnostic/`, measured produced FPS `81.1`, present mean FPS `73.8`, present p95 `33.33 ms`, Android jank `22.81%`, route proof `maxDistanceKm=86.8`; the matching `-SkipChrome` run, `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r10-skipchrome-diagnostic/`, measured produced FPS `52.5`, present mean FPS `41.0`, present p95 `58.32 ms`, Android jank `81.82%`. Temporary traffic-detail counter artifact `tools/perf/out/eu-frankfurt-regional100mi-sanity-trafficdetail-r2-symbolmix/` showed direct fallback was almost entirely bitmap-mask symbols (`bitmapSymbols` avg `1176.9`, p95 `2182`) rather than rotorcraft (`rotorcraftSymbols` avg `39.3`, p95 `61`), with direct symbols avg `1216.1` and max `2243`. Follow-up temporary distribution artifact `tools/perf/out/eu-frankfurt-regional100mi-sanity-trafficdetail-r3-symboldistribution/`, route proof `passed=True`, `samples=8`, `maxDistanceKm=64.4`, measured produced FPS `61.4`, present mean FPS `41.3`, present p95 `58.32 ms`, Android jank `80.23%`; its debug counters showed avg `1064.0` bitmap symbols per sampled frame, avg `40.6` rotorcraft, `98.36%` of bitmap symbols rotated, only `1.64%` unrotated, avg `212.9` color pairs, and airliners about `68.19%` of all drawn symbols. That rules out an axis-aligned bitmap fast path as a meaningful regional fix and makes draw-call reduction via simple color/symbol batching unlikely. The map-side skip-traffic map-detail probe `tools/perf/out/eu-frankfurt-regional100mi-sanity-mapdetail-r1-skiptraffic/` measured produced FPS `101.1`, present mean FPS `72.9`, present p95 `33.33 ms`, Android jank `29.52%`, route proof `passed=True`, `samples=7`, `maxDistanceKm=88.1`; detail samples showed avg `LastMap=12.55 ms`, `Upper=3.92 ms`, `Reference=6.45 ms`, `RefPlan=2.46 ms`, `RefPrefetch=3.06 ms`, with the worst sampled map frame split between upper satellite draw (`13.19 ms`) and reference (`10.73 ms`). Temporary cache-miss reason artifact `tools/perf/out/eu-frankfurt-regional100mi-sanity-trafficdetail-r4-cachemiss-reasons/`, route proof `passed=True`, `samples=7`, `maxDistanceKm=89.7`, showed the high-direct active misses were caused by the cached symbol bitmap being completely offscreen during zoom-out (`active_offscreen` direct `2134-2457`), not merely by a small coverage edge; one active visual-change miss drew `1560`, cache hits still drew avg `784` motion exclusions, and later scale gates at `1.68` and `1.88` drew `1138` and `974`. The r4 diagnostic changed only debug strings and was reverted from source after capture. Useful next work should reduce exact bitmap/direct-symbol draw cost or draw-call pressure, or find a new map-side/active-cache approach, without relaxing motion accuracy, widening overlay coverage, hiding symbols, changing aircraft selection, or weakening the accepted reference-overlay visual rules.
- Timetable-backed bounded human-like harness keeper, June 21, 2026: the perf harness now selects a UTC timetable-backed EU/US traffic target when no city is provided, hard-caps instrumentation (`MaxRunSeconds` max 180 s), starts video only after Flight Alert is foreground, holds foreground for short video evidence, avoids unbounded `waitForIdleSync()` in animated map startup, and uses human-like overlapping pan/pinch gestures for sanity and scale-band tests. Accepted sanity video `tools/perf/out/harness-sanity-frankfurt-human-panzoom-video-r4/` ran over Frankfurt with `route_focus_passed=True`, `samples=39`, `maxDistanceKm=64.4`, no launcher frames in the contact sheet, and about 30 s of pan-plus-overlapped zoom motion. Use separate bounded commands for wide, country, regional/sanity, close, and fast-zoom bands rather than recreating a hidden multi-band mega-test.
- Active optimizer harness target cleanup, June 21, 2026: `FlightMapGesturePerfTest.java` and `tools/perf/RunFlightMapGesturePerf.ps1` no longer list Denver/`den` as an active optimizer target or accepted route-proof alias. Historical Denver artifacts remain ledger evidence only. The older sweep/reverse Java paths now relaunch/re-anchor over the same timetable-selected US/EU target before their reverse leg, so zooming back in/out cannot inherit a wandered camera over unrelated desert, ocean, sparse terrain, or random map. Android Studio MCP targeted build passed for the Java harness and renderer after the cleanup.
- Street tile-load catch-up keeper, June 19/20: labeled street tiles now use real CARTO Voyager raster tiles with a separate `carto_voyager` cache key instead of `tile.openstreetmap.org`; no-label tiles already used CARTO Voyager no-labels. `StreetMapTileRenderer` uses 6 tile workers, async disk writes after memory display, newest visible request priority, coarsest parent requests before exact requests, parent fallback to depth 3, and a conservative complete child-mosaic fallback from cached higher-detail real street tiles before showing `Loading map`. Real-phone Denver map-only video run `bestof-layer16-street-fastzoomout-childfallback-visual-denver-rfcx` passed route proof, showed no visible `Loading map` recovery hole in the contact sheet, and measured produced FPS 79.2, present mean FPS 101.2, present p50 8.33 ms, present p95 25 ms, present-drop120 5.93%, and Android jank 5.52%. This improved over `bestof-layer15-street-fastzoomout-carto-labeled-visual-denver-rfcx`, which still had one small 250 ms recovery hole and measured present mean FPS 73.4 / present p95 41.65 ms / Android jank 7.04%. Earlier `bestof-layer14-street-fastzoomout-parentpriority-visual-denver-r3cx` on OSM showed many placeholders and only 61.1 present mean FPS, confirming provider/tail-latency mattered as well as fallback behavior.
- The FPS-first summarizer output in `tools/perf/SummarizeFrameStats.ps1` is a testing keeper. It keeps jank fields in CSV, but the default table now leads with produced FPS, present mean FPS, and frame-time percentiles so visual review and performance review can stay separate.

Rejected or not-yet-allowed on this clean branch:

- Rejected June 21 Frankfurt satellite micro-optimizations after bounded physical-device tests, all reverted from `FlightAlertRewritten.kt`: (1) reference-overlay fully-transparent tile draw skipping did not exercise the close-band route (`empty=0` in `eu-frankfurt-close-satellite-perf-novideo-r3-transparentref`) and only found one or two empty tiles during fast zoom-out, so the extra per-tile lookup was not a useful keeper; (2) base satellite next-lower-LOD zoom-out prefetch lost badly in the comparable fast map-only run `tools/perf/out/eu-frankfurt-fastzoomout-satellite-perf-novideo-r5-zoomoutprefetch/`, with route proof passing over Frankfurt but present mean FPS falling to `66.4`, present p95 `41.66 ms`, and Android jank `40.15%` versus accepted `r3-rectreuse` at `76.8`, `33.33 ms`, and `27.11%`; (3) skipping retained interim rasters when the exact lower satellite grid covered the viewport was visually plausible and did produce `interimDraw=false` once fully covered, but the full evidence was mixed/negative, so it was also reverted: fast `r6-coveredinterim` was only neutral on mean (`76.4` vs `76.8`) and worse on Android jank (`32.16%` vs `27.11%`), close `r4-coveredinterim` was neutral versus the full-chrome close baseline (`70.3` present mean FPS, `41.66 ms` p95), and country `r7-coveredinterim` regressed from the accepted active-symbol bridge run's `85.6` present mean FPS / `20.16%` jank to `73.8` / `28.04%`; (4) skipping `Paint.alpha` set/reset work for full-alpha tile draws looked harmless and improved close p95 in `eu-frankfurt-close-satellite-perf-novideo-r5-paintalpha/`, but comparable fast and country evidence regressed (`r7-paintalpha` fast `73.6` present mean FPS / `34.39%` jank versus accepted `76.8` / `27.11%`; `r8-paintalpha` country `73.2` / `41.03%` versus accepted `85.6` / `20.16%`). Do not retry these exact approaches unless a new design removes their measured downside and proves equal pixels with timetable-selected EU/US evidence.
- Rejected June 21 Frankfurt follow-up micro-optimizations, all reverted from `FlightAlertRewritten.kt`: (1) a narrowly bounded post-interaction exact symbol-cache draw still logged `symbolCache=miss reason=inactive_after_interaction direct=4371` and regressed `tools/perf/out/eu-frankfurt-country-satellite-perf-novideo-r10-postexact/` to present mean FPS `65.3`, present p95 `41.66 ms`, and Android jank `40.73%` versus accepted `r6-symbolbridge` at `85.6`, `33.33 ms`, and `20.16%`; (2) reference-overlay target-coverage early return looked behavior-preserving but regressed fast zoom-out hard in `tools/perf/out/eu-frankfurt-fastzoomout-satellite-perf-novideo-r8-refcoverageearly/` to present mean FPS `56.9`, present p95 `49.99 ms`, and Android jank `50.00%` versus accepted `r3-rectreuse` at `76.8`, `33.33 ms`, and `27.11%`; the close companion `tools/perf/out/eu-frankfurt-close-satellite-perf-novideo-r6-refcoverageearly/` was mixed (`71.8` mean / `33.33 ms` p95 but worse `40.00%` jank), so it was not enough to keep; (3) retained interim tile object reuse avoided per-frame object creation but added a hot-loop interim-map lookup and regressed close in `tools/perf/out/eu-frankfurt-close-satellite-perf-novideo-r7-interimreuse/` to present mean FPS `59.5`, present p95 `33.33 ms`, and Android jank `37.01%` versus accepted `r2-rectreuse` at `70.3`, `41.66 ms`, and `34.52%`. Do not retry these exact guard/plan/allocation shortcuts without a new design and full equal-visuals evidence.
- Rejected June 21 Frankfurt reference-prefetch/planning micro-optimizations, all reverted from `FlightAlertRewritten.kt`: map-detail diagnostics showed reference-overlay prefetch bookkeeping was expensive, but suppressing or reshaping that path moved jank across scale bands. Full repeated prefetch-grid throttling improved close in `tools/perf/out/eu-frankfurt-close-satellite-perf-novideo-r10-refprefetchthrottle/` (`93.3` present mean FPS, `25 ms` p95, `18.87%` jank) but did not pass the fast zoom-out gate: `tools/perf/out/eu-frankfurt-fastzoomout-satellite-perf-novideo-r10-refprefetchthrottle/` measured `76.4`, `41.66 ms`, `35.41%`, and repeat `r11-refprefetchthrottle-repeat` measured `80.7`, `41.66 ms`, `23.93%`, leaving p95 worse than accepted `r3-rectreuse` (`76.8`, `33.33 ms`, `27.11%`). A tighter `45 ms` throttle regressed fast further in `r12-refprefetchthrottle45` (`70.4`, `41.66 ms`, `42.13%`), and pan-prefetch-only throttling regressed close in `r12-panprefetchthrottle` (`54.9`, `50 ms`, `37.62%`). Deferred reference tile URL creation looked nonvisual but repeated fast zoom-out regressed in `r13-reference-url-defer` (`66.4`, `41.65 ms`, `44.57%`) and `r14-reference-url-defer-repeat` (`66.1`, `41.66 ms`, `45.42%`). Moving parent-fallback availability checks behind exact-tile readiness also regressed close in `r14-coverage-fallback-deferral` (`52.5`, `49.99 ms`, `53.57%`). A preflight queued-prefetch guard that mirrored the later `request_reference_tile` requeue check regressed close in `r15-prefetch-queued-guard` (`62.4`, `41.67 ms`, `38.44%`), so lock/queue prechecks in the hot prefetch loop are not a keeper either. A generation-guarded "fully loaded prefetch grid" cache was safer in theory because it skipped only unchanged all-loaded grids, but it still failed the 100 mi/regional sanity gate: current accepted baseline `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r1-current-baseline/` measured `42.3` present mean FPS, `50.02 ms` p95, and `83.78%` Android jank, while `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r2-loadedprefetchcache/` fell to `38.2`, `58.33 ms`, and `85.12%`. A localized prefetch-zoom-list allocation cleanup that preserved the intended order but replaced `linkedSetOf`, sequence sorting, and reversed temporary lists also failed the regional map-only gate: `tools/perf/out/eu-frankfurt-regional100mi-sanity-mapdetail-r2-prefetchlist/` measured produced FPS `94.2`, present mean FPS `68.0`, present p95 `33.33 ms`, Android jank `30.49%`, route proof `maxDistanceKm=84.3`, versus the immediate skip-traffic map-detail baseline `r1-skiptraffic` at `101.1`, `72.9`, `33.33 ms`, and `29.52%`. Do not retry these exact reference-prefetch throttles, loaded-grid caches, URL deferral, fallback-check reordering, queued-prefetch guards, or prefetch-list allocation rewrites unless a new design proves close, 100 mi/regional, and fast zoom-out with equal visual behavior.
- Rejected June 21 debug-string micro-optimization, reverted from `FlightAlertRewritten.kt`: replacing the satellite `lodAlpha` debug-summary `String.format` call with a custom formatter was visually irrelevant but did not pass evidence. Close run `tools/perf/out/eu-frankfurt-close-satellite-perf-novideo-r16-lodalpha-format/` measured present mean FPS `53.9`, present p95 `50 ms`, and Android jank `49.66%` versus accepted close `r2-rectreuse` at `70.3`, `41.66 ms`, and `34.52%`. Later same-session close runs may be affected by device heat/noise after many repeated tests, but ambiguous micro-optimizations must still be rejected, not kept.
- Rejected June 21 reference LOD planner allocation rewrite, reverted from `FlightAlertRewritten.kt`: replacing `associateWith`/filtered map entries/comparator ranking in `reference_overlay_draw_plan` with arrays and manual ranking was intended to preserve the exact LOD choice while reducing allocation, but it did not pass the fresh same-session close baseline. Current accepted-state refresh `tools/perf/out/eu-frankfurt-close-satellite-perf-novideo-r17-current-baseline-refresh/` measured present mean FPS `55.8`, present p95 `41.66 ms`, and Android jank `45.21%`; the allocation rewrite `tools/perf/out/eu-frankfurt-close-satellite-perf-novideo-r18-plan-allocation/` measured `54.3`, `49.99 ms`, and `49.50%`. Do not retry this exact manual ranking rewrite without a stronger correctness/perf reason and cross-band evidence.
- Superseded June 21 renderer pause-point context, kept only for artifact history: an earlier uncommitted renderer experiment in `app/src/main/java/com/flightalert/FlightAlertRewritten.kt` split alpha-zero satellite upper-LOD work out of the visible draw path and skipped drawing retained interim rasters when the exact lower satellite grid fully covered the viewport. Historical evidence used Denver artifacts: accepted baseline `tools/perf/out/direct-closepan-label-flicker-holdlod-denver-z122-novideo/` averaged `map=27.18 ms`, `mapTiles≈249`, `interim≈246`; bounded z12.2 run `tools/perf/out/satellite-upper-prefetch-split-denver-z122-smallpan-novideo/` averaged `map=22.21 ms`, `mapTiles≈57`, `interim≈57`, with `roads_lod11_lines=0`, `borders_lod11_lines=0`, `roads_fallback_lines=0`, and `label_fallback_lines=0`; video `tools/perf/out/satellite-upper-prefetch-split-denver-z122-smallpan-video/` looked stable. Do not treat this as current pickup instruction; new optimizer evidence must follow the UTC timetable, bounded human-like scale-band tests, and the newer accepted/rejected entries above.
- Do not port satellite renderer/layer rewrites wholesale from `test/local-perf`: the latest branch comparison showed satellite country and continent frame-time regressions versus clean `master`, even though parts of the label work were useful.
- Do not reintroduce optimizations that hide, remove, delay, shrink, darken, simplify, or approximate aircraft morphing, outlines, symbol detail, labels, tile transition behavior, details/photos, alert behavior, or traffic density.
- Rejected June 19 active aircraft-overlay-cache experiment: allowing symbol overlay recording during active wide zoom did not produce cache hits in `bestof-layer11-street-fullapp-activeoverlaycache-perf-denver-rfcx`; full-app street fast-zoom remained about 56.5 present FPS / 23.53% Android jank with `symbolCache=miss reason=active_coverage direct=4306` and produced FPS fell versus the prior full-app perf run. Keep investigating the aircraft overlay miss path, but do not keep active recording unless a future implementation proves actual cache hits, equal visuals, and better full-app frame data.
- Rejected June 21 regional aircraft symbol-overlay coverage expansion, reverted from `FlightAlertRewritten.kt`: raising `SYMBOL_OVERLAY_WIDE_COVERAGE_MAX_ZOOM` from `6.0` to `8.2` tried to keep the larger cached symbol bitmap active through the 100 mi/regional band, preserving symbol detail while reducing `active_coverage` direct draws. Physical Fold, timetable-selected Frankfurt/Europe, satellite roads/borders on, `satellitePanZoomSanityPerf`: current accepted baseline `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r1-current-baseline/` measured `42.3` present mean FPS, `50.02 ms` p95, and `83.78%` Android jank; wide-coverage run `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r3-symbolwidecoverage/` fell to `38.4`, `58.31 ms`, and `84.38%`. Bigger symbol-overlay bitmaps are not a free regional fix; do not retry this threshold bump without a new memory/draw strategy and cross-band proof.
- Rejected June 21 direct aircraft symbol sprite-cache experiment, reverted from `FlightAlertRewritten.kt`: caching composited fill+stroke ARGB symbol sprites looked like a way to replace two mask draws per direct non-rotorcraft symbol with one bitmap draw, but it created severe cache/memory stalls and carried subtle antialiasing risk. Physical Fold Frankfurt regional sanity `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r4-symbolspritecache/` collapsed to `20.8` present mean FPS and `174.97 ms` p95 versus accepted regional baseline `42.3` and `50.02 ms`. Do not retry composed direct-symbol sprite caching unless a future design prewarms/bounds memory and proves exact visual equivalence.
- Rejected June 21 traffic spatial-index callback iterator, reverted from `FlightAlertRewritten.kt`: replacing temporary `TrafficSpatialIndex.query(...)` result lists with direct cell iteration in dense dot, dense symbol, and visible aircraft state builders was behavior-preserving in theory, but did not produce a reliable device win. Frankfurt regional sanity `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r5-spatialforeach/` measured `40.0` present mean FPS, `49.99 ms` p95, and `84.71%` jank, and repeat `r6-spatialforeach-repeat` measured `41.8`, `58.32 ms`, and `83.78%`, versus accepted baseline `42.3`, `50.02 ms`, and `83.78%`. Do not keep allocation rewrites in the traffic hot path on theory alone.
- Rejected June 21 regional traffic micro-optimizations, reverted from `FlightAlertRewritten.kt`: (1) post-interaction symbol-overlay bookkeeping short-circuit avoided computing an overlay key/signature when `inactive_after_interaction` had no completed recording to install, but same-route evidence was mixed and not a keeper: `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r11-inactiveafter-shortcircuit/` measured produced FPS `47.6`, present mean FPS `41.0`, present p95 `50.00 ms`, Android jank `83.33%`; repeat `r12-inactiveafter-shortcircuit-repeat/` measured `51.5`, `41.7`, `58.33 ms`, `84.83%` versus current 768 path-cache reference `r7` at `56.3`, `41.2`, `58.34 ms`, `86.34%`; (2) skipping label-count math when labels were disabled and skipping the unused rotorcraft mask preparation did not produce a reliable direct-style win, with `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r13-directstyle-cleanup/` measuring produced FPS `49.4`, present mean FPS `40.6`, present p95 `58.34 ms`, Android jank `83.33%`; (3) precomputing full-appearance scrim shadow/stroke alpha colors in `AircraftIconFrameStyle` preserved intended pixels only for aircraft whose appearance progress was already `1f`, but failed the same-session full-traffic regional gate: fresh baseline `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r20-current-refresh/` measured produced FPS `52.6`, present mean FPS `42.0`, present p95 `49.99 ms`, Android jank `81.88%`, route proof `maxDistanceKm=75.7`; `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r21-fullappearance-colors/` fell to `47.0`, `40.6`, `50.00 ms`, and `84.89%`, route proof `maxDistanceKm=81.4`. Do not keep or retry these tiny direct-style/bookkeeping/color cleanups unless a future run shows a clear cross-band improvement.
- Rejected June 21 regional symbol-overlay coverage/early-skip follow-ups, reverted from `FlightAlertRewritten.kt`: (1) a new middle regional symbol-overlay padding tier (`300dp` until zoom `7.4`) was intentionally narrower than the earlier rejected `420dp` wide-coverage threshold and preserved the same cached pixels/motion exclusions, but it still failed repeat evidence: `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r14-symbolregionalpadding/` measured produced FPS `52.0`, present mean FPS `41.0`, present p95 `50.01 ms`, Android jank `83.55%`, while repeat `r15-symbolregionalpadding-repeat/` fell to `46.2`, `39.6`, `58.32 ms`, `84.17%`; (2) pre-position skipping of cached aircraft keys when the symbol overlay already covered the direct fallback area preserved draw eligibility but did not improve the real gate, with `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r16-cachedkey-earlyskip/` measuring produced FPS `48.6`, present mean FPS `41.4`, present p95 `58.33 ms`, Android jank `85.82%`. Do not retry modest regional padding tiers or cached-key early-skip loop shortcuts without a new reason beyond the current direct-symbol evidence.
- Rejected June 21 regional cull-math micro-optimization, reverted from `FlightAlertRewritten.kt`: precomputing the per-viewport max icon scale for direct symbol culling preserved exact cull math but did not produce a reliable physical-device win. Frankfurt regional sanity `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r17-cullscale-precompute/` measured produced FPS `52.9`, present mean FPS `41.9`, present p95 `50.00 ms`, Android jank `85.81%`; repeat `r18-cullscale-precompute-repeat/` measured `53.1`, `41.9`, `58.34 ms`, `87.25%` versus current path-cache reference `r7` at `56.3`, `41.2`, `58.34 ms`, `86.34%`. Do not keep per-item cull math reshaping on theory alone.
- Rejected June 21 direct symbol matrix-value rewrite, reverted from `FlightAlertRewritten.kt`: replacing the accepted reusable `Matrix` post-translate/scale/rotate/translate path with an explicit 9-float affine `setValues` matrix kept the same intended transform and two ALPHA mask draws, but physical-device evidence regressed badly. Frankfurt regional sanity `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r19-directmatrix-values/` measured produced FPS `44.2`, present mean FPS `38.6`, present p95 `49.99 ms`, Android jank `88.89%`, versus current path-cache reference `r7` at `56.3`, `41.2`, `58.34 ms`, `86.34%`. Keep the accepted reusable `Matrix.post*` path; do not retry manual matrix-value construction unless a future graphics proof shows a different low-overhead exact transform route.
- Rejected June 21 aircraft symbol idle-cache reuse experiment: the tempting hotspot after `postwarning-london-z54-satellite-real` was `symbolCache=miss reason=inactive_after_interaction direct≈2931`, so a short-lived idle reuse/refresh of the pan symbol bitmap was tried and then reverted. The aggressive refresh version `tools/perf/out/idle-symbol-cache-london-z54-satellite-real/` was much worse than the accepted baseline, dropping to produced FPS `25.2`, present mean FPS `45.6`, present-drop120 `81.48%`, and Android jank `88.76%`; logs showed `idle_expired` misses doing huge direct draws while recording/building overlays. The narrowed post-interaction reuse version `tools/perf/out/postinteraction-cache-london-z54-satellite-real/` was also worse, with produced FPS `17.6`, present mean FPS `25.4`, present p95 `150.01 ms`, present-drop120 `96.43%`, and Android jank `100%`. Do not reintroduce idle symbol-overlay reuse/refresh unless a future design proves equal pixels and better full-frame evidence.
- Rejected June 21 Atlanta interaction-settle alignment retry, reverted from `FlightAlertRewritten.kt`: Pro reviewed the 60 ms mismatch between `DENSE_DOT_SYMBOL_SETTLE_MS = 360L` and `DENSE_SYMBOL_CACHE_INTERACTION_SETTLE_MS = 420L` and agreed it was a narrower test than idle reuse or visual-threshold widening, with the reject condition being continued `inactive_after_interaction`/scale misses or stale shifted symbols. The code temporarily set `DENSE_DOT_SYMBOL_SETTLE_MS` to `420L`. Physical Fold timetable-selected Atlanta `tools/perf/out/atlanta-regional-settle420-20260621-1227/` completed in `45.5 s` and passed route proof (`samples=8`, `maxDistanceKm=84.3`, `accepted_optimizer_evidence=true`), but regressed versus the accepted motion-padding artifact `atlanta-regional-densesymbol-motionpad-20260621-1150`: produced FPS `50.6 -> 39.5`, present mean FPS `36.9 -> 33.0`, present p95 `66.66 -> 83.33 ms`, Android jank `70.19% -> 90.23%`. Logs still showed `symbolCache=miss reason=inactive_after_interaction`, `reason=scale`, and large `active_visual_change` direct draws (`direct=2235-3219`, `directSymbols` up to `23.37 ms`), so the mismatch was not the root fix. Keep `DENSE_DOT_SYMBOL_SETTLE_MS = 360L`; do not retry simple interaction-settle alignment unless a new design proves the renderer and dense-state cache lifetimes together without increasing stale/shifted-symbol frames.
- June 21 Pro/state-builder diagnostic, not a code keeper by itself: a bounded Pro reviewer ask returned one candidate after a partial first answer: commit-frame coalescing, meaning draw only the newest exact camera/traffic revision once per presentation opportunity so superseded intermediate direct-symbol passes are not rendered. Treat this as advisory until the camera/gesture invalidation and traffic-overlay draw-entry surfaces are audited; reject it on any stale frame, map/traffic misregistration, stable-keyframe pixel mismatch, route-proof failure, or less than 15% direct-symbol CPU reduction in Atlanta regional, broad zoom, and dense EU checks. Temporary per-entry timing was also added and removed from `FlightAlertRewritten.kt` after measurement so normal rendering would not carry debug function-call overhead. Diagnostic artifacts `tools/perf/out/symbolfilter-breakdown-20260621-1220/` and `tools/perf/out/symbolstate-breakdown-20260621-1225/` both used timetable-selected Atlanta and passed route proof (`maxDistanceKm=34.1` and `27.2`), but are not performance acceptance because the extra timers intentionally distorted FPS. The useful local finding: sampled build frames spent most of `symFilter` in `traffic_aircraft_overlay_state` construction, not projection or culling (`symState` about `18-23 ms`, projection/cull about `2-8 ms` combined under heavy timers); the finer run attributed meaningful time to appearance lookup/progress, color, and per-aircraft motion-clock sampling (`symStateAppearance=5.28 ms`, `symStateProgress=6.00 ms`, `symStateColor=1.96 ms`, `symStateMotionClock=2.37 ms` on a `4650`-state build frame). Because previous appearance/clock/state-builder micro-optimizations regressed in stronger gates, do not simply replay those old patches; any revisit must be a new exact-fidelity state-construction design with Frankfurt/Atlanta regional plus broad zoom evidence.
- June 21 commit-frame coalescing audit status: a follow-up Pro ask with the Android Studio MCP/static audit evidence did not produce a complete accepted answer, so no additional Pro guidance was accepted. Local audit covered `onDraw`, `should_redraw_from_ticker`, `FlightMapPanelChrome.request_animation_frame`, `TrafficOverlayChrome.request_animation_frame`, `draw_traffic_overlay`, and dense-symbol state entry points with Android Studio MCP plus static master comparison. Finding: normal animation callbacks already use `postInvalidateOnAnimation`, Android coalesces invalidations at the View boundary, `onDraw` performs only one traffic state build per delivered draw, and master has the same unconditional interaction ticker redraw. Therefore do not implement a blind scheduling/coalescing patch as a keeper; treat commit-frame coalescing as rejected for now unless a future narrowly scoped instrumentation run proves multiple completed traffic builds are being delivered for superseded camera/traffic revisions and shows at least a 15% direct-symbol CPU reduction with no stale frame, touch-lag, route-proof, road/traffic misregistration, or stable-keyframe pixel regression.
- June 21 ticker-redraw diagnostic keeper plus rejected suppression experiment: after Pro's existing conversation suggested measuring cross-frame ticker suppression, `FlightAlertRewritten.kt` gained debug-only ticker reason counters in the existing `Debug draw perf` log (`tickerPosts`, `iOnly`, `iMotion`, `sameCameraTraffic`, and `iOnlySameCameraTraffic`). This is diagnostic instrumentation, not a user-visible optimization. Physical Fold timetable-selected Atlanta diagnostic `tools/perf/out/us-atlanta-ticker-diagnostic-20260621-1249/` passed route proof (`city=Atlanta`, `samples=7`, `maxDistanceKm=105.5`, `accepted_optimizer_evidence=true`) and measured produced FPS `40.0`, present mean FPS `33.2`, present p95 `75.01 ms`, Android jank `83.92%` under traffic detail timing. It found `98` ticker redraw posts, `92` interaction-only posts, `97` ticker-requested draws, `36` same-camera/cache ticker draws, and `32` interaction-only same-camera/cache draws, making the candidate worth a bounded test. The actual suppression experiment then skipped interaction ticker posts only when camera/cache matched, no appearance animation was active, and normal aircraft-motion gating said no frame was due; it was rejected and reverted. `tools/perf/out/us-atlanta-ticker-suppress-20260621-1256/` passed route proof (`samples=7`, `maxDistanceKm=59.0`) but regressed to produced FPS `30.8`, present mean FPS `28.2`, present p95 `83.32 ms`, Android jank `96.90%`, despite suppressing only `14` interaction-only posts. Do not reintroduce interaction ticker suppression/gating without a fundamentally different proof; Android/coalescing and touch timing make the naive same-camera/cache gate a regression.
- Rejected June 21 regional active-symbol scale-gate extension, reverted from `FlightAlertRewritten.kt`: cache-miss diagnostic `r4-cachemiss-reasons` showed late zoom-in misses at transform scales `1.68` and `1.88`, so `SYMBOL_OVERLAY_INTERACTION_SCALE_MAX` was temporarily raised from `1.62` to `1.92` while leaving the existing visual-bucket tolerances unchanged. Physical Fold Frankfurt regional sanity `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r22-scalegate192/` regressed to produced FPS `43.2`, present mean FPS `36.4`, present p95 `58.34 ms`, Android jank `88.57%`, route proof `maxDistanceKm=85.8`, versus fresh current baseline `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r20-current-refresh/` at `52.6`, `42.0`, `49.99 ms`, and `81.88%`. Keep `SYMBOL_OVERLAY_INTERACTION_SCALE_MAX = 1.62f`; do not widen this early scale gate without a fundamentally different visual proof and cross-band evidence.
- Rejected June 21 active recenter symbol-overlay recording, reverted from `FlightAlertRewritten.kt`: after `r4-cachemiss-reasons` proved the large active misses were fully offscreen caches, a narrower active-recording design requested a current-center symbol overlay only when an existing active cache was offscreen, while still drawing direct for that frame and preserving the existing key/visual checks before any cached draw. Physical Fold Frankfurt regional sanity `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r23-active-recenter/` still regressed to produced FPS `47.2`, present mean FPS `39.8`, present p95 `58.34 ms`, Android jank `80.99%`, route proof `maxDistanceKm=65.1`, versus fresh current baseline `r20-current-refresh` at `52.6`, `42.0`, `49.99 ms`, and `81.88%`. Do not retry active symbol-overlay recording/recentering without a substantially different design that proves actual post-recording hits, equal visual behavior, and better regional plus cross-band frame data.
- Rejected June 21 regional dense state-builder micro-optimizations, reverted from `FlightAlertRewritten.kt`: (1) caching a normalized `icao_key` in `TrafficSpatialEntry` and using it for dense path-focus/selected comparisons looked exact because it preserved `appearance_key` fallback semantics, but physical Fold timetable-selected Frankfurt regional sanity `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r24-icokey/` regressed to produced FPS `44.2`, present mean FPS `38.4`, present p95 `66.64 ms`, Android jank `86.86%`, route proof `maxDistanceKm=55.6`; logs still showed a rebuild at `symFilter=13.46 ms`; (2) sharing one dense-symbol motion-build elapsed timestamp across the main rebuild loop avoided per-aircraft clock reads but was not a safe keeper, with `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r25-motionclockonce/` measuring produced FPS `45.3`, present mean FPS `40.0`, present p95 `58.51 ms`, Android jank `84.21%`, route proof `maxDistanceKm=73.9`, and worst frames moving into direct symbol-mask draw. Do not retry cached ICAO-key or one-clock dense state-builder cleanups as regional fixes unless a new design proves clear frame wins without changing motion fidelity.
- Rejected June 21 direct symbol paint-reset micro-optimization, reverted from `FlightAlertRewritten.kt`: removing the per-symbol `symbol_mask_paint.alpha = 255` reset after the two cached bitmap-mask draws looked redundant because each subsequent draw sets `symbol_mask_paint.color`, but the physical Fold timetable-selected Frankfurt regional sanity gate failed badly. `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r26-symbolmask-alphareset/` measured produced FPS `40.9`, present mean FPS `36.0`, present p95 `58.33 ms`, Android jank `93.94%`, route proof `maxDistanceKm=64.4`. Keep the reset unless a different graphics-level proof shows equal pixels and a real frame win.
- June 21 current-tree regional recheck, not a code keeper/reject by itself: after many same-session runs, physical Fold timetable-selected Frankfurt `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r27-current-recheck/` measured produced FPS `44.6`, present mean FPS `38.2`, present p95 `58.34 ms`, Android jank `88.57%`, route proof `maxDistanceKm=88.1`. Treat this as a noisy/possibly warm-device immediate comparator for later r28+ experiments, not as a replacement for the stronger earlier accepted `r20-current-refresh` baseline.
- Rejected June 21 selected-null guard micro-optimization, reverted from `FlightAlertRewritten.kt`: guarding `item.appearance_key == normalized_selected_id` behind `normalized_selected_id != null` in the symbol and dot-batch hot loops looked exact for no-selection perf routes, but same-session physical Fold Frankfurt regional sanity `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r28-selectedguard/` regressed below the immediate current recheck to produced FPS `37.5`, present mean FPS `35.3`, present p95 `66.65 ms`, Android jank `90.48%`, route proof `maxDistanceKm=72.3`. Do not keep selected-comparison branch guards on theory alone.
- Rejected June 21 regional dense-symbol state padding increase, reverted from `FlightAlertRewritten.kt`: raising `DENSE_SYMBOL_CACHE_MAX_REUSE_DP` from `180f` to `260f` tried to avoid active `symMode=build` frames by giving the shifted dense state cache more exact offscreen runway. It preserved the intended visible draw contract in theory because renderer culling is smaller than the state-cache padding, but the same-session physical Fold Frankfurt regional sanity gate failed: `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r29-densestatepadding260/` measured produced FPS `38.8`, present mean FPS `34.6`, present p95 `66.67 ms`, Android jank `88.55%`, route proof `maxDistanceKm=64.4`. Do not retry modest dense-symbol state padding bumps without a new design that avoids increasing shifted-state/direct-loop work.
- Rejected June 21 proactive active-edge symbol overlay prefetch, reverted from `FlightAlertRewritten.kt`: this differed from the earlier active-recenter attempt by requesting a current-center symbol-overlay recording only while the existing active cache was still drawable but no longer covered the direct-fallback area, hoping the replacement would be ready before a later offscreen burst. Regional no-video evidence was mixed rather than decisive: `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r30-active-edge-prefetch/` measured produced FPS `45.8`, present mean FPS `39.7`, present p95 `58.32 ms`, Android jank `82.14%`, route proof `maxDistanceKm=79.0`; repeat `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r31-active-edge-prefetch-repeat/` measured produced FPS `55.8`, present mean FPS `38.6`, present p95 `58.32 ms`, Android jank `87.79%`, route proof `maxDistanceKm=88.1`. The required country-band cross-check failed versus the accepted active-symbol bridge: `tools/perf/out/eu-frankfurt-country-satellite-perf-novideo-r30-active-edge-prefetch-crosscheck/` measured produced FPS `66.8`, present mean FPS `68.2`, present p95 `49.99 ms`, Android jank `44.55%`, route proof `maxDistanceKm=193.0`, compared with accepted `r6-symbolbridge` country evidence at `85.6` present mean FPS, `33.33 ms` p95, and `20.16%` jank. Do not retry active-edge/proactive symbol-overlay recording unless a new design proves cross-band wins and actual post-recording cache hits without background recording contention.
- Rejected June 21 lazy per-frame aircraft symbol-mask resolution, reverted from `FlightAlertRewritten.kt`: changing `AircraftIconFrameStyle` to resolve cached symbol masks only when each non-rotorcraft symbol was first drawn in the frame preserved the same mask cache, progress bucket, mask size, and resolution scale in theory, but did not beat the stronger regional evidence. Physical Fold timetable-selected Frankfurt regional sanity `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r32-lazysymbolmasks/` stayed correctly over Europe (`maxDistanceKm=64.4`) and measured produced FPS `49.2`, present mean FPS `41.2`, present p95 `58.32 ms`, Android jank `79.43%`, versus fresh accepted-state reference `r20-current-refresh` at produced FPS `52.6`, present mean FPS `42.0`, present p95 `49.99 ms`, Android jank `81.88%`. Because it was not a clear win and worsened p95 against the stronger baseline, do not retry lazy frame-mask resolution unless a future profile shows a different mask-prep bottleneck.
- June 21 current-tree regional recheck after reverting r32, not a code keeper/reject by itself: physical Fold timetable-selected Frankfurt `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r33-current-after-r32revert/` measured produced FPS `48.5`, present mean FPS `41.9`, present p95 `58.32 ms`, Android jank `85.82%`, route proof `maxDistanceKm=64.4`. Use this as the immediate same-session comparator for r34+ experiments, not as a replacement for stronger `r20-current-refresh`.
- Rejected June 21 dense-symbol rebuild pre-size micro-optimization, reverted from `FlightAlertRewritten.kt`: pre-sizing the dense symbol rebuild `ArrayList`/`HashSet` from the spatial-query size preserved the same query/filter/result semantics, but physical Fold timetable-selected Frankfurt regional sanity `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r34-densesymbol-presize/` regressed to produced FPS `44.6`, present mean FPS `37.1`, present p95 `58.34 ms`, Android jank `84.25%`, route proof `maxDistanceKm=65.1`, versus immediate current `r33` at `48.5`, `41.9`, `58.32 ms`, and `85.82%`. Do not retry container pre-size/allocation-only dense symbol rebuild tweaks unless a future profile points to resize churn as the real bottleneck.
- Rejected June 21 full-bitmap cached symbol-overlay draw, reverted from `FlightAlertRewritten.kt`: replacing occupied-tile slice draws with one full transparent overlay bitmap draw preserved intended pixels in theory and reduced draw-call count, but physical Fold timetable-selected Frankfurt regional sanity `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r35-symboloverlay-fulldraw/` regressed to produced FPS `44.0`, present mean FPS `37.5`, present p95 `66.66 ms`, Android jank `87.86%`, route proof `maxDistanceKm=90.0`, versus immediate current `r33` at `48.5`, `41.9`, `58.32 ms`, and `85.82%`. The occupied-tile draw path is likely avoiding costly transparent fill-rate; keep it unless a future tile-packing design proves better cross-band evidence.
- Rejected June 21 symbol bitmap paint flag change, reverted from `FlightAlertRewritten.kt`: replacing `Paint(Paint.ANTI_ALIAS_FLAG)` plus explicit bitmap filter/dither properties with `Paint(Paint.FILTER_BITMAP_FLAG or Paint.DITHER_FLAG)` for the aircraft symbol mask and symbol-overlay bitmap paints was intended to preserve bitmap sampling while removing a geometry flag, but physical Fold timetable-selected Frankfurt regional sanity `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r36-symbolbitmappaint-flags/` measured produced FPS `47.2`, present mean FPS `37.5`, present p95 `58.36 ms`, Android jank `85.81%`, route proof `maxDistanceKm=90.0`, versus immediate current `r33` at `48.5`, `41.9`, `58.32 ms`, and `85.82%`. Keep the existing symbol bitmap paint construction unless a future graphics profile proves a different exact paint setup helps.
- June 21 current-tree map-only regional diagnostic, not a code keeper/reject by itself: physical Fold timetable-selected Frankfurt `tools/perf/out/eu-frankfurt-regional100mi-sanity-mapdetail-r3-skiptraffic-current/` used the bounded human-like pan plus overlapped zoom sanity path, skipped traffic, kept satellite roads/borders on, and passed route proof with `maxDistanceKm=79.0`. It measured produced FPS `91.8`, present mean FPS `70.0`, present p95 `41.66 ms`, Android jank `21.79%`; logcat showed the remaining map-only spikes mostly in reference/prefetch work during zoom transitions. This is better than the older skip-traffic map-detail `r1`/`r2` comparison, so do not treat map-only reference code as the current primary limiter without fresh contrary evidence.
- Rejected June 21 terminal aircraft-appearance state micro-optimization, reverted from `FlightAlertRewritten.kt`: marking fully appeared `TrafficAircraftOverlayState` items as terminal (`appearance_first_seen_ms=0`, `appearance_progress=1f`) tried to avoid repeated fade-progress timing for aircraft that had already reached full opacity/scale, while preserving new-aircraft fade-in semantics. Physical Fold timetable-selected Frankfurt regional sanity `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r37-appearance-terminal/` regressed to produced FPS `42.3`, present mean FPS `35.6`, present p95 `66.66 ms`, Android jank `84.29%`, route proof `maxDistanceKm=84.3`, versus immediate current `r33` at `48.5`, `41.9`, `58.32 ms`, and `85.82%`. Do not retry aircraft-appearance terminal-state rewrites as a regional fix without a new profile showing that appearance progress is the bottleneck.
- June 21 scale-band diagnostics after the bounded timetable/human-motion harness fixes, not code keepers by themselves: wide/continent satellite `tools/perf/out/eu-frankfurt-continent-satellite-diagnostic-r1-current-detail/` passed route proof over Frankfurt with `maxDistanceKm=295.0`, produced FPS `84.6`, present mean FPS `81.1`, present p95 `41.66 ms`, Android jank `9.55%`; detail logs showed dot-batch traffic was cheap after initial recording and no direct symbols were drawn. Close satellite `tools/perf/out/eu-frankfurt-close-satellite-diagnostic-r1-current-detail/` passed route proof with `maxDistanceKm=4.7`, produced FPS `141.6`, present mean FPS `57.1`, present p95 `41.67 ms`, Android jank `48.14%`; logs showed traffic around `1 ms` and the spikes mainly in map/reference prefetch. These runs reinforce that the current painful scale is regional symbol/morph traffic plus close reference prefetch, not continent dots.
- June 21 current-tree multi-zoom harness diagnostic, not a code keeper/reject by itself: physical Fold timetable-selected Frankfurt `tools/perf/out/eu-timetable-pan-across-zoom-levels-current-r1/` used the bounded `panAcrossZoomLevels` test with human-like pans plus overlapping zoom-in gestures across country, regional, and close phases. The command completed in `43 s`, selected Frankfurt automatically at `12:32 UTC`, passed route proof with `maxDistanceKm=217.6`, and the contact sheet stayed over dense European land traffic instead of launcher/home/random terrain. Frame evidence measured produced FPS `306.8`, present mean FPS `76.8`, present p95 `49.99 ms`, Android jank `32.22%`. Logs showed the broad street-mode hotspot is still dense traffic symbol fallback, especially `symbolCache=miss reason=active_visual_change` or `reason=scale` with direct draws of roughly `3.7k-4.1k` aircraft and `directSymbols` up to about `24 ms`; map work was negligible in this run. Do not treat this as acceptance, but use it as fresh evidence that broad zoom variety is now covered by a short timetable-safe diagnostic and that remaining 120 Hz work needs a structural exact-fidelity fix for active zoom symbol reuse rather than another simple scale-gate widening.
- June 21 Atlanta direct-symbol mask split diagnostic, not an optimization keeper by itself: after the master-exhausted checkpoint, `FlightAlertRewritten.kt` added debug-only `TrafficDetailTiming` counters for `symbolMaskSetup`, `symbolMaskFill`, and `symbolMaskStroke` inside the existing exact cached-mask direct draw path; the normal non-detail path keeps the same Matrix sequence and the same fill/stroke bitmap draws. Android Studio MCP hot-function audit covered `draw_aircraft_symbols`, `draw_aircraft_icon`, and `draw_cached_aircraft_symbol`; Android Studio MCP build passed. Baseline physical Fold Atlanta `panAcrossZoomLevels`, roads/borders on, `tools/perf/out/us-atlanta-pan-across-trafficdetail-baseline-20260621-1745/`, passed route proof (`samples=20`, `maxDistanceKm=286.2`) and measured produced FPS `237.1`, present mean FPS `73.4`, present p95 `50.00 ms`, Android jank `43.61%`; debug samples showed max-frame `directSymbols` avg `12.45 ms`, p95 `25.73 ms`, max `40.32 ms`, and `symbolMask/directSymbols` avg `0.53`. The first mask-split rerun `tools/perf/out/us-atlanta-pan-across-trafficdetail-masksplit-20260621-1756/` is rejected because the phone was taken by another test and the harness pulled no same-run framestats. The completed mask-split rerun `tools/perf/out/us-atlanta-pan-across-trafficdetail-masksplit-20260621-1805/` passed route proof (`samples=16`, `maxDistanceKm=276.7`) but is diagnostic-only because the extra per-bitmap timers distorted frame timing (produced FPS `149.6`, present mean FPS `48.7`, present p95 `74.99 ms`, Android jank `56.10%`). Its useful evidence: sampled `symbolMask/directSymbols` avg `0.55`; max-frame `directSymbols` avg `12.13 ms`, p95/max `35.82 ms`, `symbolMask` avg `6.77 ms`, `symbolMaskSetup` avg `1.67 ms`, `symbolMaskFill` avg `1.42 ms`, and `symbolMaskStroke` avg `1.20 ms`, with the worst `scale` miss drawing `4784` direct symbols at `35.82 ms` direct and `24.37 ms` mask. Normal no-detail Atlanta satellite sanity after the debug split, `tools/perf/out/us-atlanta-satellite-panzoom-normal-after-masksplit-20260621-1812/`, passed route proof (`samples=7`, `maxDistanceKm=114.2`) and measured produced FPS `38.6`, present mean FPS `34.0`, present p95 `83.33 ms`, Android jank `82.71%`; use it only as a normal-path smoke, not a broad comparison. This confirms the next implementation gate remains exact active direct-symbol body/mask reduction or reuse, not another cache threshold, ticker, reference, or appearance micro-optimization. No new Pro answer was accepted for this step.
- Rejected June 21 direct symbol one-pass AGSL shader prototype, reverted from `FlightAlertRewritten.kt`: an API-33+ `RuntimeShader` experiment sampled the existing fill and stroke `ALPHA_8` masks and computed the same intended stroke-over-fill premultiplied result in one draw, leaving the old two-bitmap path as fallback. Android Studio MCP build passed, but physical evidence rejected it. First Atlanta satellite sanity `tools/perf/out/us-atlanta-satellite-panzoom-symbolshader-20260621-1830/` is invalid as performance evidence despite produced FPS `110.2`, present mean FPS `89.3`, p95 `16.66 ms`, and route proof `maxDistanceKm=116.7`, because debug logs showed `symbols=0 dots=0` and `symbolCache=none` throughout. The traffic-loaded broad Atlanta run `tools/perf/out/us-atlanta-pan-across-symbolshader-20260621-1837/` passed route proof (`samples=17`, `maxDistanceKm=305.4`) with real traffic (`symbols≈9.1k`, `dots≈11.8k`, direct misses up to `5001`), but regressed to produced FPS `160.3`, present mean FPS `53.8`, present p95 `49.99 ms`, Android jank `61.88%` even without detail timing, worse than the earlier traffic-detail baseline `us-atlanta-pan-across-trafficdetail-baseline-20260621-1745` at `73.4` present mean FPS and `43.61%` jank. Do not retry one-pass AGSL/RuntimeShader mask compositing as a direct-symbol fix unless a future design proves lower per-symbol shader/uniform overhead and exact visual equivalence.
- June 21 Atlanta symbol-overlay recording-composition diagnostic, not an optimization keeper by itself: `FlightAlertRewritten.kt` now adds debug-only `TrafficDetailTiming` counters to the installed symbol-overlay recording summary (`recCached`, `recMotion`, `recRotor`, `recOff`, and `recMotionMax`) so cache hits and subsequent misses say whether the retained bitmap is mostly blocked by motion exclusions or by scale/coverage reuse. Android Studio MCP file audit and build passed. Clean current broad Atlanta before this diagnostic, `tools/perf/out/us-atlanta-pan-across-clean-current-20260621-1740/`, passed route proof (`samples=18`, `maxDistanceKm=248.7`) and measured produced FPS `208.0`, present mean FPS `71.0`, present p95 `66.66 ms`, Android jank `49.87%`; its logs showed large early direct bursts from `symbolCache=hit cached=22 direct=5682` and later `reason=scale`, so the recording composition was ambiguous. The new detail run `tools/perf/out/us-atlanta-pan-across-recdiag-20260621-1755/` passed route proof (`samples=19`, `maxDistanceKm=274.0`) and should be read as diagnostic-only; its useful evidence was `recCached=8886`, `recMotion=0`, `recRotor=245`, `recOff=2`, `recMotionMax=0.0` on the active recording. Cache-hit frames direct-drew only about `164-166` exclusions, while the expensive frames were still `reason=scale` or `reason=active_coverage` with thousands of direct symbols (`direct=2131-4904`, `directSymbols` up to `31.00 ms`). A normal no-detail smoke after the counters, `tools/perf/out/us-atlanta-pan-across-clean-after-recdiag-20260621-1805/`, passed route proof (`samples=18`, `maxDistanceKm=285.8`) and measured produced FPS `234.3`, present mean FPS `68.7`, present p95 `58.33 ms`, Android jank `41.75%`; use it only as a smoke, not a broad branch comparison. This rules out "moving aircraft exclusions are the primary cache-hit limiter" for this Atlanta pass and shifts the next exact-fidelity target back to active scale/coverage/dimension reuse or a new non-UI-thread retained-symbol design. Do not replay the already rejected simple scale-gate, padding, active-recenter, active-edge-prefetch, synchronous recording, key-skip, or sprite-cache patches.
- June 21 Atlanta active-coverage dimension diagnostic, not an optimization keeper by itself: `FlightAlertRewritten.kt` added debug-only `TrafficDetailTiming` fields to `active_coverage` misses (`dims`, expected/cached bitmap size, current padding, recorded padding, and zoom) while still drawing exactly the same pixels. Android Studio MCP hot-function audit and build passed. Physical Fold timetable-selected Atlanta `panAcrossZoomLevels`, roads/borders on, `tools/perf/out/us-atlanta-pan-across-activecovdims-20260621-1351/`, passed route proof (`samples=20`, `maxDistanceKm=296.8`) and measured produced FPS `272.1`, present mean FPS `74.8`, present p50/p95/p99 `8.33/49.99/83.33 ms`, Android jank `41.57%`; visible evidence was intentionally unchecked, so treat the run as diagnostic-only. The useful log parse found 7 `active_coverage` samples and all were `dims=false`, with `expected=3331x2778`, `cached=4381x3828`, `pad=577.5`, and `recPad=1102.5` at zoom `8.22`, proving the scale-band padding/dimension mismatch exists. However these close-band active-coverage misses direct-drew only about `806-822` symbols and cost about `4.79-5.05 ms`; the worst direct-symbol spikes were the initial inactive build (`direct=5834`, `directSymbols=55.84 ms`) and `reason=scale` during active zoom (`direct=4702-4813`, `directSymbols=35.21-45.25 ms`). Do not treat stored recording padding or dimension mismatch alone as the next root fix; the bigger unresolved lever is exact active-scale reuse or another structural way to avoid thousands of direct symbols during scale misses without replaying the rejected simple scale-gate widening.
- June 21 Atlanta scale-miss geometry diagnostic, not an optimization keeper by itself: `FlightAlertRewritten.kt` now also adds debug-only `TrafficDetailTiming` fields to `reason=scale` misses (`intersects`, `dims`, expected/cached bitmap size, current/recorded padding, zoom, and center delta) while preserving normal rendering. Android Studio MCP errors-only check and build passed. Physical Fold timetable-selected Atlanta detail run `tools/perf/out/us-atlanta-pan-across-scaledims-20260621-1358/` passed route proof (`samples=19`, `maxDistanceKm=291.3`) and measured produced FPS `236.6`, present mean FPS `71.0`, present p50/p95/p99 `8.33/58.34/116.66 ms`, Android jank `45.26%`; visible evidence was intentionally unchecked, so use it only as a diagnostic. The useful parse found 13 `scale` samples and every one was `intersects=false` plus `dims=false`, again with `expected=3331x2778`, `cached=4381x3828`, `pad=577.5`, and `recPad=1102.5`, across zoom `6.17-8.21`. The top expensive samples were `reason=scale` at `scale=1.7`, zoom `6.17`, `direct=4762`, `directSymbols=33.03 ms`, and `reason=active_coverage` at scales `1.5-1.6`, zoom `6.01-6.05`, `direct=4872-4908`, `directSymbols=25.23-30.71 ms`; all of these were dimension/padding mismatches crossing the wide-coverage threshold, not simply a too-low scale gate. A normal no-detail smoke after the diagnostic fields, `tools/perf/out/us-atlanta-pan-across-clean-after-scaledims-20260621-1405/`, passed route proof (`samples=19`, `maxDistanceKm=291.9`) and measured produced FPS `228.0`, present mean FPS `73.5`, present p95 `50.00 ms`, Android jank `42.82%`; treat it as smoke only, not an accepted branch comparison. Do not retry plain `SYMBOL_OVERLAY_INTERACTION_SCALE_MAX` widening here: the cache geometry would still fail. Any next exact-fidelity attempt must solve the zoom-6 padding-band retained-overlay handoff without reusing the rejected broad stored-padding draw, wide-coverage threshold bump, regional padding tier, active recenter/prefetch, or UI-thread sync recording patches.
- Rejected June 21 narrow zoom-6 recorded-padding handoff experiment, reverted from `FlightAlertRewritten.kt`: a deliberately narrower variant of the old stored-padding idea allowed recorded wide-padding geometry only during active interaction, only inside a `0.12` zoom span above `SYMBOL_OVERLAY_WIDE_COVERAGE_MAX_ZOOM`, only when a wider recorded bitmap exactly matched the recorded dimensions, and still required the existing visual bridge checks before drawing. Android Studio MCP function audit/build passed, but physical Fold Atlanta `panAcrossZoomLevels` detail run `tools/perf/out/us-atlanta-pan-across-handoff012-detail-20260621-1420/` rejected it. Route proof passed (`samples=19`, `maxDistanceKm=171.8`) and aggregate present mean was superficially high (`75.6` FPS, p95 `49.99 ms`), but Android jank worsened to `52.03%` and log evidence showed the handoff fired as `active_visual_change`, not as a cache hit: `handoffPad=1102.5`, `curPad=577.5`, zoom `6.09`, `direct=4785`, `directSymbols=32.93 ms`. Worse, subsequent sampled cache hits had only `recCached=29` with `direct=5569-5715` and `directSymbols=25.74-39.85 ms`, meaning the attempted handoff recreated the rejected partial-cache/large-direct-fallback failure. The post-revert normal no-detail smoke `tools/perf/out/us-atlanta-pan-across-clean-after-handoff-revert-20260621-1428/` reinstalled the reverted build, passed route proof (`samples=20`, `maxDistanceKm=310.9`), and measured produced FPS `270.2`, present mean FPS `74.5`, present p95 `49.99 ms`, Android jank `40.23%`. Do not retry narrow zoom-threshold recorded-padding handoffs unless a future design also proves stable recorded coverage/key population and avoids stale tiny-cache installs.
- Rejected June 21 partial active symbol-overlay install guard, reverted from `FlightAlertRewritten.kt`: after the rejected handoff run exposed tiny retained recordings (`recCached=29`) paired with thousands of direct fallback draws, a narrow guard was tested that discarded pathologically small completed symbol-overlay recordings only when active interaction tried to install `any` pending recording. Android Studio MCP function audit/errors-only check and build passed, but the physical Fold timetable-selected Atlanta detail run `tools/perf/out/us-atlanta-pan-across-partialguard-detail-20260621-1414/` proved the guard did not fire at all (`partialRecording` log count `0`); the installed recording was large (`recCached=8762`, `recRotor=321`, `recOff=5`) and the run measured produced FPS `215.0`, present mean FPS `65.2`, present p95 `66.66 ms`, Android jank `47.89%`, route proof `samples=20`, `maxDistanceKm=310.9`. The guard was reverted immediately. Post-revert normal no-detail smoke `tools/perf/out/us-atlanta-pan-across-clean-after-partialguard-revert-20260621-1416/` passed route proof (`samples=21`, `maxDistanceKm=291.9`) and measured produced FPS `270.0`, present mean FPS `72.9`, present p95 `58.33 ms`, Android jank `37.04%`. A later current-branch diagnostic did reproduce tiny retained recordings without the handoff code, but the second guard retry below still failed; do not keep or retry a simple active install-any tiny-recording guard.
- Rejected June 21 reference-prefetch per-tile timing split, reverted from `FlightAlertRewritten.kt`: after `tools/perf/out/us-atlanta-regional-satellite-maptrafficdetail-20260621-1418/` showed `reference` max `45.77 ms` with `refPrefetch` max `29.75 ms`, a debug-only split temporarily added `refPfKey`, `refPfCache`, `refPfReq`, `refPfTiles`, and `refPfMiss` around `request_reference_overlay_prefetch_grid`. Android Studio MCP audited `request_reference_overlay_prefetch_grid`, `reference_overlay_draw_plan`, and `draw_reference_overlay_layers`, and build passed. The detail diagnostic `tools/perf/out/us-atlanta-regional-satellite-refprefsplit-20260621-1422/` passed route proof (`samples=7`, `maxDistanceKm=109.3`) but was heavy/diagnostic only: produced FPS `43.2`, present mean FPS `36.9`, present p95 `75.02 ms`, Android jank `80.00%`; it showed `refPrefetch` avg/max `4.56/12.31 ms`, `refPfReq` avg/max `1.99/6.44 ms`, `refPfTiles` avg/max about `1082/1553`, and `refPfMiss` avg/max about `471/977`, pointing to candidate volume rather than a safe simple skip. Normal smokes immediately after were poor (`tools/perf/out/us-atlanta-satellite-normal-after-refprefsplit-20260621-1423/`: produced FPS `32.8`, present mean `29.4`, p95 `83.31 ms`, jank `94.53%`, route `47.3 km`; repeat `tools/perf/out/us-atlanta-satellite-normal-after-refprefsplit-repeat-20260621-1425/`: `29.0`, `29.8`, `83.33 ms`, `96.67%`, route `59.0 km`). The split was reverted because diagnostic timing must not add risk; post-revert smoke `tools/perf/out/us-atlanta-satellite-normal-after-refprefsplit-revert-20260621-1427/` still measured only produced FPS `32.2`, present mean `31.2`, p95 `83.32 ms`, jank `91.74%`, route `95.8 km`, so the bad satellite sanity band was not caused solely by the split. Do not re-add per-tile prefetch timing unless there is a bounded measurement need and it is removed before normal comparisons.
- Rejected June 21 second partial-recording install guard retry, reverted from `FlightAlertRewritten.kt`: a fresh current-branch diagnostic after the reference-split revert, `tools/perf/out/us-atlanta-satellite-maptrafficdetail-after-refsplit-revert-20260621-1429/`, reproduced tiny retained symbol recordings in Atlanta satellite sanity without the rejected handoff code. It passed route proof (`samples=6`, `maxDistanceKm=116.3`) and measured produced FPS `28.2`, present mean FPS `28.7`, present p95 `91.68 ms`, Android jank `97.48%`. Detail aggregation showed `recCached` was always `16` while direct fallback averaged `1671.5` aircraft and maxed at `3224`; `directSymbols` avg/p95/max was `11.50/19.96/19.96 ms`, `symbolMask` `6.27/10.41/10.41 ms`, while map `reference` avg/p95/max was `9.90/23.14/23.14 ms` and `refPrefetch` `5.59/13.75/13.75 ms`. A proportional active install-any guard was retried after Android Studio MCP audit/build: it rejected pending recordings only if their recorded count was below 25% of the current symbol list, with a small floor capped by current size. The physical Fold diagnostic `tools/perf/out/us-atlanta-satellite-partialrecordguard-detail-20260621-1432/` passed route proof (`samples=6`, `maxDistanceKm=109.3`) but did not improve (`produced FPS 28.3`, present mean `28.1`, p95 `83.34 ms`, jank `98.25%`) and did not hit the intended `partial_recording` branch; logs still showed a tiny cache already installed (`cached=15`, `recCached=15`) before active install-any could reject it. The code was reverted and Android Studio build passed. Do not retry active install-any tiny-recording guards; the actual problem is earlier tiny exact recording/installation or a larger retained-overlay lifecycle design, and previous active recording/recentering/sync-record designs are also rejected.
- June 21 sparse symbol-overlay recording invalidation is a narrow keeper candidate, not a broad 120fps fix: after Atlanta satellite sanity normal no-detail reproduced a pathological retained symbol overlay (`tools/perf/out/us-atlanta-satellite-recsource-normal-20260621-1440/`, route proof `samples=6`, `maxDistanceKm=109.3`, produced FPS `29.8`, present mean FPS `30.1`, present p95 `83.32 ms`, Android jank `94.92%`) with `symbolCache=hit cached=11` while direct fallback drew about `1421-1484` current aircraft, `FlightAlertRewritten.kt` kept debug provenance counters and added `drop_sparse_symbol_overlay_recording_if_needed`. The drop recycles an already-installed retained overlay only when current traffic is dense (`>=256`) and the retained recording is less than one-eighth of the current aircraft count, then falls back to exact direct drawing for the current frame instead of hiding or simplifying aircraft. Ratio `4` was rejected as too aggressive because Frankfurt `tools/perf/out/eu-frankfurt-satellite-sparse-drop-normal-20260621-1446/` dropped a medium cache (`cached=426` around `recCur=2147`) despite route proof `samples=7`, `maxDistanceKm=65.1`, produced FPS `49.9`, present mean FPS `39.3`, present p95 `74.99 ms`, Android jank `82.00%`. The tightened ratio `8` preserved that medium cache in Frankfurt `tools/perf/out/eu-frankfurt-satellite-sparse-drop-ratio8-normal-20260621-1447/` (route proof `samples=8`, `maxDistanceKm=86.8`, produced FPS `55.7`, present mean FPS `43.0`, present p95 `75.0 ms`, Android jank `78.71%`, `cached=409`, no `dropped_sparse`) and retained dense Atlanta caches in `tools/perf/out/us-atlanta-satellite-sparse-drop-ratio8-normal-20260621-1448/` (route proof `samples=7`, `maxDistanceKm=102.4`, produced FPS `34.9`, present mean FPS `33.8`, present p95 `91.65 ms`, Android jank `77.54%`, `cached=2252`). Treat this as an anti-pathological-cache safety valve only; it does not solve active `reason=active_visual_change` or `reason=scale` direct-symbol spikes, and do not widen it back to ratio `4` without stronger cross-region visual and frame evidence.
- June 21 post-ratio8 Atlanta map/traffic detail diagnostic, not a code keeper by itself: the first attempt `tools/perf/out/us-atlanta-satellite-maptrafficdetail-postratio8-20260621-1452/` is invalid as frame evidence because the script was given a custom `-ArtifactName` while `satellitePanZoomSanityPerf` writes fixed `flightalert-perf-satellitePanZoomSanityPerf-*` files, so no same-run framestats were pulled. The corrected physical Fold diagnostic `tools/perf/out/us-atlanta-satellite-maptrafficdetail-postratio8-20260621-1456/` used timetable-selected Atlanta at `18:56 UTC`, satellite roads/borders on, `TrafficDetailTiming` and `MapDetailTiming`, no video, and passed route proof (`samples=7`, `maxDistanceKm=114.2`) but is diagnostic-only (`accepted_optimizer_evidence=false` because visible evidence was unchecked). Frame evidence was produced FPS `37.7`, present mean FPS `33.6`, present p50/p95/p99 `16.67/83.34/100.01 ms`, Android jank `85.71%`. Useful max-frame log evidence: direct-symbol spikes remained (`active_visual_change` direct `2952`, `directSymbols=33.64 ms`; `inactive_after_interaction` direct `1011`, `directSymbols=23.65 ms`), dense state rebuilds still matter (`stateBuild=44.50 ms`, `symFilter=25.53 ms`, `symGrid=8.65 ms`), and one map/reference spike was real draw work (`reference=45.78 ms`, `refDraw=28.66 ms`) rather than the earlier `refGen` blind spot. Keep using this as hotspot guidance only; it does not prove a branch comparison.
- June 21 active marker-morph bridge tolerance keeper candidate: after Android Studio MCP audited `draw_symbol_overlay_pan_cache`, `request_symbol_overlay_cache_recording`, and `symbol_overlay_interaction_visual_bridge_matches`, `SYMBOL_OVERLAY_INTERACTION_MARKER_BUCKET_TOLERANCE` was raised from `6` to `18` while leaving the active scale gate (`0.62..1.62`), aircraft signature, appearance/theme checks, dimensions/coverage checks, and icon/dot scale tolerances unchanged. The change targets active-interaction frames where the retained bitmap already has the same aircraft set and visual family but misses only by about `0.9-1.8%` marker morph progress, turning large exact direct-symbol fallbacks back into retained-overlay hits without hiding traffic. Same-session physical Fold Atlanta satellite sanity with roads/borders on improved from `tools/perf/out/us-atlanta-satellite-current-postrevert-20260621-1502/` (route proof `samples=7`, `maxDistanceKm=71.7`, produced FPS `37.0`, present mean FPS `33.5`, p95 `74.97 ms`, Android jank `86.67%`) to `tools/perf/out/us-atlanta-satellite-marker18-20260621-1507/` (same route proof distance, produced FPS `52.0`, present mean FPS `41.9`, p95 `66.64 ms`, jank `83.89%`). Frankfurt A/B confirmed the signal: candidate `tools/perf/out/eu-frankfurt-satellite-marker18-20260621-1512/` measured `72.9` produced FPS, `52.3` present mean FPS, p95 `50 ms`, jank `64.67%`, route proof `samples=8`, `maxDistanceKm=64.4`, while the immediately restored marker-6 baseline `tools/perf/out/eu-frankfurt-satellite-marker6-ab-20260621-1515/` measured `55.5`, `41.5`, `66.66 ms`, and `76.73%`, route proof `maxDistanceKm=89.7`. Broader Atlanta `panAcrossZoomLevels` was neutral rather than a clean win (`tools/perf/out/us-atlanta-pan-across-marker18-20260621-1510/`: `251.2` produced FPS, `69.8` present mean FPS, p95 `58.34 ms`, jank `41.69%`, route proof `maxDistanceKm=288.9`), but Frankfurt broad video was strong (`tools/perf/out/eu-frankfurt-pan-across-marker18-video-20260621-1521/`: `342.3`, `78.4`, p95 `41.66 ms`, jank `24.50%`, route proof `samples=104`, `maxDistanceKm=239.5`). Visual evidence: the raw Frankfurt satellite video artifact `eu-frankfurt-satellite-marker18-video-20260621-1518` started with launcher/home frames in the generated strip, so do not use that raw strip alone as accepted visual proof; reviewed derived artifacts `eu-frankfurt-satellite-marker18-video-20260621-1518-trimmed-map-contact-sheet.jpg`, `...-trimmed-road-motion-strip.jpg`, `eu-frankfurt-pan-across-marker18-video-20260621-1521-video-contact-sheet.jpg`, and `...-trimmed-motion-strip.jpg` show Flight Alert over timetable-selected Frankfurt with dense traffic, continuous map/road motion, and no obvious aircraft symbol popping/flicker from the tolerance change. Keep marker tolerance at `18` as a narrow active-pinch keeper candidate; do not widen it further or combine it with the rejected scale-gate/padding/recording handoff ideas without new video plus cross-band evidence.
- June 21 dense-symbol rebuild reason diagnostic, not a code keeper by itself: after Android Studio MCP audited `shifted_dense_symbol_overlay`/dense-symbol builder state and build passed, `FlightAlertRewritten.kt` added debug-only `symBuild=` detail timing to explain why dense symbol state cannot shift/reuse before rebuilding. Physical Fold timetable-selected Atlanta at UTC `19:22`, `tools/perf/out/us-atlanta-satellite-densebuildreason-20260621-1522/`, ran `satellitePanZoomSanityPerf` with satellite roads/borders on, `TrafficDetailTiming` plus `MapDetailTiming`, no video, and passed route proof (`samples=7`, `maxDistanceKm=116.3`); it is diagnostic-only because visible evidence was unchecked. Frame evidence was produced FPS `49.9`, present mean FPS `40.8`, present p50/p95/p99 `16.67/66.65/100.03 ms`, Android jank `84.83%`, present-drop-at-120 `85.59%`. Useful max-frame log evidence: only two sampled dense-symbol rebuilds appeared, both `reuse_off`: source identity changed while zoom did not (`symBuild=reuse_off source=true zoom=false iReuse=false idle=false`, `stateBuild=37.42 ms`, `symOverlay=28.46 ms`, `symFilter=21.07 ms`, `symGrid=7.27 ms`, cache miss `appearance`) and zoom changed while neither interaction nor idle reuse was allowed (`symBuild=reuse_off source=false zoom=true iReuse=false idle=false`, `stateBuild=8.54 ms`, `symOverlay=7.14 ms`, `symFilter=5.99 ms`, cache miss `inactive_after_interaction`). Separate sampled spikes still exist outside dense rebuilds, especially direct-symbol/mask work even on cache hits (`directSymbols=23.12 ms`, `symbolMask=10.38 ms`) and reference prefetch/planning (`reference=10.17 ms`, `refPlan=5.49 ms`, `refPrefetch=2.09 ms`). Treat this as evidence that the next safe target is understanding or redesigning dense-symbol reuse across live traffic source replacement/interaction-state boundaries, not retrying simple scale-gate widening, padding bands, source-blind stale-state reuse, or the previously rejected direct-symbol mask/sprite/shader shortcuts.
- June 21 dense-symbol reuse-age follow-up diagnostic, not a code keeper by itself: current `FlightAlertRewritten.kt` added debug-only `cacheAge`, `intAge`, and interaction flag fields to `symBuild=reuse_off` after Android Studio MCP errors-only/build checks passed. Physical Fold Atlanta `tools/perf/out/us-atlanta-satellite-densereuseage-20260621-1527/` used the same satellite roads/borders-on `satellitePanZoomSanityPerf` timing path and passed route proof (`samples=8`, `maxDistanceKm=84.3`) but is diagnostic-only (`accepted_optimizer_evidence=false`, no visible evidence). Frame evidence under heavier logging was produced FPS `39.9`, present mean FPS `34.8`, present p50/p95/p99 `16.66/83.32/116.67 ms`, Android jank `84.12%`. Useful max-frame evidence: all three costly `reuse_off` rebuilds had `source=true`; one was startup/idle (`cacheAge=543`, `intAge=690632436`, `stateBuild=20.23 ms`, `symFilter=14.71 ms`), and two were after interaction with flags already false (`cacheAge=943`, `intAge=689`, `stateBuild=17.72 ms`; `cacheAge=2098`, `intAge=429`, `stateBuild=14.47 ms`). One separate coverage rebuild (`coverage scale=0.71`, `stateBuild=18.69 ms`) and direct/reference spikes remained (`directSymbols=19.13 ms`, `reference=27.72 ms`, `refPrefetch=19.15 ms`). Read-only master has the same `DENSE_SYMBOL_CACHE_INTERACTION_SETTLE_MS = 420L`, so this is not a missing master trick. A narrow candidate is to align dense-symbol active settle with the already accepted aircraft-refresh deferral window (`MAP_INTERACTION_AIRCRAFT_REFRESH_DELAY_MS = 550L`), but do not extend beyond that or source-blindly reuse stale states; any candidate must prove no visual/information-delay regression and pass cross-region timing.
- Rejected June 21 dense-symbol interaction-settle alignment, reverted from `FlightAlertRewritten.kt`: changing `DENSE_SYMBOL_CACHE_INTERACTION_SETTLE_MS` from `420L` to the existing aircraft-refresh deferral window (`MAP_INTERACTION_AIRCRAFT_REFRESH_DELAY_MS`, `550 ms`) was a narrow attempt to reuse dense symbol state only inside an already accepted post-interaction feed-deferral period. Android Studio MCP errors-only/build checks passed. The Atlanta detail run `tools/perf/out/us-atlanta-satellite-symbolsettle550-detail-20260621-1530/` matched the intended mechanism by eliminating sampled `reuse_off` rebuilds and measured produced FPS `50.3`, present mean FPS `40.6`, present p95 `58.35 ms`, Android jank `80.00%`, route proof `maxDistanceKm=114.2`; however no-detail cross-checks were not strong enough. Atlanta no-detail `tools/perf/out/us-atlanta-satellite-symbolsettle550-normal-20260621-1531/` was mixed versus the marker18 reference (`49.7` produced FPS, `41.3` present mean, p95 `74.98 ms`, jank `79.02%`, route proof `maxDistanceKm=84.3`). Frankfurt no-detail `tools/perf/out/eu-frankfurt-satellite-symbolsettle550-normal-20260621-1531/` (`60.2` produced FPS, `46.7` present mean, p95 `58.33 ms`, jank `76.77%`, route proof `maxDistanceKm=88.1`) failed to reproduce the earlier marker18 Frankfurt strength (`72.9`, `52.3`, p95 `50 ms`, jank `64.67%`), but the immediate restored-420 smoke `tools/perf/out/eu-frankfurt-satellite-postsettle-revert-normal-20260621-1533/` was similarly low (`58.5`, `45.6`, p95 `58.33 ms`, jank `78.43%`, route proof `maxDistanceKm=81.4`), so treat this as a no-clear-win/risk rejection rather than proof of a durable direct regression. Keep the dense symbol interaction settle at `420L`; do not retry simple settle-window extension as a reuse/source-change fix without a new design that improves cross-region no-detail evidence and proves no fresh-traffic delay.
- Rejected June 21 full-opacity satellite tile draw alpha fast path, reverted from `FlightAlertRewritten.kt`: after Android Studio MCP audited `draw_reference_overlay_layers`, `draw_reference_overlay_grid`, and `draw_tile_bitmap`, a tiny exactness-preserving theory patch added an early `alpha >= 1f` path in `SatelliteMapTileRenderer.draw_tile_bitmap` to skip alpha coercion/rounding and the post-draw reset while still using the same filtered/dithered `bitmap_paint` at alpha `255`. Android Studio MCP errors-only/build checks passed, but the same-session physical Fold Atlanta normal satellite sanity rejected it. Comparator `tools/perf/out/us-atlanta-satellite-current-tilealpha-comparator-20260621-1502/` passed route proof (`samples=7`, `maxDistanceKm=71.7`) and measured produced FPS `43.0`, present mean FPS `37.1`, present p95 `74.99 ms`, Android jank `82.01%`; the fast-path run `tools/perf/out/us-atlanta-satellite-tilealpha-fastpath-20260621-1504/` passed route proof (`samples=6`, `maxDistanceKm=71.7`) but regressed to produced FPS `38.2`, present mean FPS `35.1`, present p95 `83.3 ms`, Android jank `82.09%`. The code was reverted and Android Studio MCP build passed. Do not retry full-opacity tile alpha/reset shortcuts as a map/reference fix without a much stronger graphics-specific profile.
- Rejected June 21 known-missing reference request shortcut, reverted from `FlightAlertRewritten.kt`: after Android Studio MCP audited `request_reference_overlay_prefetch_grid`, `draw_reference_overlay_grid`, `draw_reference_overlay_layers`, and `request_reference_tile`, a candidate passed a `known_missing` flag from call sites that had just observed `reference_tile_bitmap(key) == null` so `request_reference_tile` could skip the immediate duplicate memory-cache lookup while the background task still rechecked before disk/network. The theory preserved exact tile requests and pixels. Android Studio MCP errors-only/build checks passed, and the Atlanta map-detail A/B initially looked promising: candidate `tools/perf/out/us-atlanta-satellite-refknownmissing-mapdetail-20260621-1538/` passed route proof (`samples=8`, `maxDistanceKm=116.3`) and measured produced FPS `65.2`, present mean FPS `49.3`, p95 `58.33 ms`, Android jank `74.05%`; immediate restored comparator `tools/perf/out/us-atlanta-satellite-refknownmissing-comparator-mapdetail-20260621-1539/` passed route proof (`samples=7`, `maxDistanceKm=83.5`) but measured `47.5`, `39.5`, p95 `66.66 ms`, jank `82.64%`. Log parsing also showed worst sampled `refPrefetch` reduced from `20.58 ms` to `12.95 ms`. The candidate failed normal cross-region evidence, though: Atlanta no-detail `tools/perf/out/us-atlanta-satellite-refknownmissing-normal-20260621-1541/` was mixed (`53.1` produced FPS, `41.0` present mean, p95 `74.99 ms`, jank `83.77%`, route proof `maxDistanceKm=76.2`), and Frankfurt no-detail `tools/perf/out/eu-frankfurt-satellite-refknownmissing-normal-20260621-1542/` was not a keeper (`64.0`, `44.4`, p95 `66.63 ms`, jank `73.81%`, route proof `maxDistanceKm=88.1`) versus stronger current/marker18 references. Do not retry duplicate reference-request memory-lookup shortcuts unless a future profile proves the lookup itself is the limiter and normal cross-region evidence improves.
- June 21 current Atlanta map/traffic detail diagnostic after rejected reference shortcut, not a code keeper by itself: physical Fold timetable-selected Atlanta at UTC `19:46`, `tools/perf/out/us-atlanta-satellite-current-maptrafficdetail-20260621-1548/`, ran `satellitePanZoomSanityPerf` with satellite roads/borders on, `TrafficDetailTiming` plus `MapDetailTiming`, no video, and passed route proof (`samples=8`, `maxDistanceKm=112.5`) but is diagnostic-only because visible evidence was unchecked. Frame evidence was produced FPS `61.8`, present mean FPS `49.6`, present p50/p95/p99 `16.66/58.33/74.99 ms`, Android jank `69.03%`. Useful max-frame evidence: the worst sampled frame still combined exact direct-symbol fallback and reference prefetch (`symbolCache=miss reason=inactive_after_interaction`, `direct=983`, `directSymbols=50.94 ms`, `symbolMask=29.92 ms`, `symbolMaskSetup=11.70 ms`, `reference=33.45 ms`, `refPrefetch=24.55 ms`, `stateBuild=8.10 ms`); another active visual-key miss still drew `1711` direct aircraft with `directSymbols=17.43 ms` and `symbolMask=10.54 ms`. Treat this as the current hotspot guide: direct symbol-mask draw is still the dominant worst-frame cost, with reference prefetch as a secondary spike.
- June 21 symbol-overlay motion-horizon diagnostic, not a code keeper by itself: after Android Studio MCP audited `AircraftSymbolOverlayRecorder`, `motion_cache_error_squared`, and `symbol_overlay_debug_recording_summary`, `FlightAlertRewritten.kt` added debug-only recording counters for motion exclusions that would satisfy the same `0.42 px` error bound at `0.85 s` and `0.65 s`; normal cache admission and drawing behavior are unchanged. Android Studio MCP errors-only/build checks passed. Physical Fold timetable-selected Atlanta at UTC `20:09`, `tools/perf/out/us-atlanta-satellite-motionhorizon-detail-20260621-1609/`, ran `satellitePanZoomSanityPerf` with satellite roads/borders on, `TrafficDetailTiming` plus `MapDetailTiming`, no video, and passed route proof (`samples=8`, `maxDistanceKm=109.3`) but is diagnostic-only because visible evidence was unchecked. Frame evidence was produced FPS `56.2`, present mean FPS `42.9`, present p50/p95/p99 `16.66/58.33/108.34 ms`, Android jank `77.56%`. Every sampled recording summary showed `recMotion=636`, `recMotion085=636`, `recMotion065=636`, `recRotor=105`, `recOff=0`, `recMotionMax=0.6`, meaning the sampled motion exclusions were barely outside the current `1.25 s` retained-overlay horizon and all would be within the same pixel-error bound at shorter horizons. This is evidence to investigate an explicitly age-bounded or split-layer moving-symbol cache, not permission to simply lower `SYMBOL_OVERLAY_DYNAMIC_EXCLUSION_HORIZON_SEC` or reuse stale moving-aircraft pixels without an overlay-age/lifetime proof, visual motion evidence, and cross-region timing.
- June 21 symbol-overlay recording-age diagnostic, not a code keeper by itself: follow-up debug-only `recAgeMs` instrumentation showed why naive short-horizon motion inclusion is unsafe. Android Studio MCP errors-only/build checks passed. Physical Fold timetable-selected Atlanta at UTC `20:13`, `tools/perf/out/us-atlanta-satellite-motionage-detail-20260621-1612/`, ran the same satellite roads/borders-on `satellitePanZoomSanityPerf` timing path and passed route proof (`samples=7`, `maxDistanceKm=84.3`) but is diagnostic-only because visible evidence was unchecked. Frame evidence was produced FPS `49.6`, present mean FPS `39.8`, present p50/p95/p99 `16.66/74.98/99.99 ms`, Android jank `83.56%`. The same motion distribution repeated (`recMotion=653`, `recMotion085=653`, `recMotion065=653`, `recMotionMax=0.6`), but sampled `recAgeMs` was already `1997-5758 ms`; the large direct fallback frames included `direct=1543` at `recAgeMs=1997`, `direct=3130` at `4113`, and `direct=1004` at `5703`. Therefore a simple "record moving symbols with 0.65/0.85 s guarantee" would usually be expired before these frames and must not be tried as a keeper. Any moving-symbol cache design must either refresh a separate moving layer close enough to actual use or preserve per-aircraft motion exactness another way, with road/traffic video if visible motion is affected.
- June 21 dense-symbol source-change identity diagnostic, not a code keeper by itself: after Android Studio MCP errors-only/build checks passed, `FlightAlertRewritten.kt` added debug-only `srcCur/srcCached/srcSame/srcAdd/srcDrop` detail to `symBuild=reuse_off` frames where the dense-symbol aircraft-list identity changes. Physical Fold timetable-selected Atlanta at UTC `20:18`, `tools/perf/out/us-atlanta-satellite-sourcesame-detail-20260621-1617/`, ran `satellitePanZoomSanityPerf` with satellite roads/borders on, `TrafficDetailTiming` plus `MapDetailTiming`, no video, and passed route proof (`samples=8`, `maxDistanceKm=105.5`) but is diagnostic-only because visible evidence was unchecked. Frame evidence was produced FPS `48.4`, present mean FPS `36.3`, present p50/p95/p99 `16.67/74.99/100.01 ms`, Android jank `84.71%`. This run produced zero `srcCur=` detail lines because the sampled dense-symbol rebuilds were `source=false`; the costly sampled rebuilds were zoom/interaction-expiry related (`symBuild=reuse_off source=false zoom=true`, `stateBuild=24.29 ms` and `8.09 ms`), while direct fallback and reference spikes remained (`inactive_after_interaction`, `scale`, `direct=1014-3086`, `reference` up to `17.41 ms`). Do not pursue source-identity reuse from this scenario alone; return to direct symbol-mask cost, overlay lifetime/recording age, or reference spikes unless a future source-change-heavy artifact recurs.
- June 21 current normal-path Atlanta comparator after the source-change diagnostic, not a code keeper by itself: physical Fold timetable-selected Atlanta at UTC `20:2x`, `tools/perf/out/us-atlanta-satellite-current-normal-20260621-162x/`, ran `satellitePanZoomSanityPerf` with satellite roads/borders on and no extra detail timing or video. Route proof passed (`samples=7`, `maxDistanceKm=116.3`), visible evidence was unchecked, and frame evidence measured produced FPS `50.8`, present mean FPS `42.0`, present p50/p95/p99 `16.66/58.33/83.34 ms`, Android jank `79.86%`. Use this only as a nearby normal-path sanity comparator for candidate rejection/triage; it is not a thorough branch/version acceptance run.
- June 21 current broad default/street normal-path Atlanta comparator, not a code keeper by itself: physical Fold timetable-selected Atlanta at UTC `20:23`, `tools/perf/out/us-atlanta-pan-across-current-normal-20260621-1625/`, ran `panAcrossZoomLevels` with roads/borders arguments but the test logged `map_source=default`/`street=1`, so treat it as default/street traffic evidence, not satellite evidence. Route proof passed (`samples=18`, `maxDistanceKm=276.7`), one screenshot/contact sheet was saved, no video was recorded, and `accepted_optimizer_evidence=false` because visible evidence was unchecked. Frame evidence measured produced FPS `211.8`, present mean FPS `68.0`, present p50/p95/p99 `8.33/50.00/124.97 ms`, Android jank `41.06%`. Log excerpts confirm the remaining broad traffic spikes are still active retained-symbol misses around zoom `6.17` (`reason=scale` direct `4340-4414`, `reason=active_coverage` direct `4475`), with later close-band scale misses drawing fewer direct symbols. Use this as a broad normal-path comparator and hotspot guide only; satellite branch comparisons still need the satellite-specific tests and roads-on motion-video review where visual claims matter.
- June 21 broad scale-bridge diagnostic, not a code keeper by itself: after Android Studio MCP audited `draw_symbol_overlay_pan_cache`, `symbol_overlay_scale_miss_detail`, `symbol_overlay_interaction_visual_bridge_matches`, `request_symbol_overlay_cache_recording`, and `install_symbol_overlay_recording_result`, `FlightAlertRewritten.kt` added debug-only `reason=scale` bridge detail so scale misses with a retained symbol overlay report the same marker/icon/dot visual-key deltas as `active_visual_change`. Android Studio MCP errors-only and file rebuild passed. Physical Fold timetable-selected Atlanta `tools/perf/out/us-atlanta-pan-across-scalebridge-detail-20260621-r1/` ran `panAcrossZoomLevels`, roads/borders arguments on but logged `map_source=default`/`street=1`, `TrafficDetailTiming`, no video, and passed route proof (`samples=20`, `maxDistanceKm=274.0`) but is diagnostic-only (`accepted_optimizer_evidence=false`, visible evidence unchecked). Frame evidence measured produced FPS `222.4`, present mean FPS `70.2`, present p50/p95/p99 `8.33/41.67/108.34 ms`, Android jank `42.45%`. Useful log evidence: the costly zoom `6.17-6.46` scale misses were not safe near-hits; they reused a stale country/wide recording (`cached=4381x3828`, `recPad=1102.5`) while current regional geometry expected `3331x2778`, `pad=577.5`, with `dims=false`, `center=false`, and visual bridge deltas beyond tolerance (`scale=1.7`: `markerD=8 iconD=200 dotD=130`, `direct=4040-4233`, `directSymbols` up to `34.64 ms`; `scale=2.0-2.1`: `markerD=11-12 iconD=267-300 dotD=196-230`, `direct=3745-4000`, `directSymbols` up to `41.88 ms`). Later zoom `8.21` scale misses drew fewer aircraft but still failed geometry and marker fidelity (`markerD=56`, `direct=752-785`). Active-coverage misses also showed the stale wide recording dimensions/padding blocking reuse even when nominal scale was near `1.0`. Do not retry simple scale-gate widening, padding-band reuse, or stored-padding geometry alone; the next exact-fidelity design must either produce a current regional/close recording before the active pinch needs it or reduce direct-symbol body/mask cost without hiding aircraft.
- Rejected June 21 settled-interaction symbol-overlay prewarm, reverted from `FlightAlertRewritten.kt`: the candidate requested an exact current symbol-overlay recording only after the user's fingers left the map while the existing interaction-settle window was still active, never drew the recording on the same miss frame, and left the existing exact key/coverage gates before cached draws. Android Studio MCP errors-only and targeted file build passed, but physical Fold timetable-selected Atlanta `tools/perf/out/us-atlanta-pan-across-settledprewarm-detail-20260621-r1/` ran `panAcrossZoomLevels`, roads/borders arguments on but logged `map_source=default`/`street=1`, `TrafficDetailTiming`, no video, and passed route proof (`samples=20`, `maxDistanceKm=285.8`) as diagnostic-only (`accepted_optimizer_evidence=false`, visible evidence unchecked). Compared with `tools/perf/out/us-atlanta-pan-across-scalebridge-detail-20260621-r1/` at present mean FPS `70.2`, p95 `41.67 ms`, and Android jank `42.45%`, the candidate was flat/mixed at present mean FPS `71.1`, p95 `41.66 ms`, and worse Android jank `42.69%`. Mechanically it failed: `settledRec=true` appeared only three times, no sampled later frame showed `recReq=settled_interaction`, there were no useful subsequent cache hits, and the costly `scale` misses still direct-drew thousands of aircraft (`direct` roughly `3750-4434` around zoom `6.17-6.46`, `directSymbols` up to about `24 ms`). Do not retry settled/prewarm interaction recording unless a new design proves timely install and actual hits without background recording contention.
- June 21 dense-symbol source-change diagnostics and rejected same-reference fast path, reverted from behavior: after Android Studio MCP audited `shifted_dense_symbol_overlay`, `can_reuse_dense_symbol_cache_while_idle`, `dense_symbol_source_change_detail`, `traffic_aircraft_overlay_state`, and `TrafficSpatialEntry`, `FlightAlertRewritten.kt` added debug-only `TrafficDetailTiming` fields for `srcPos/srcMove/srcFresh/srcRef/srcIdxRef/srcMaxLatE6/srcMaxLonE6` on dense-symbol `symBuild=reuse_off source=true` frames. Android Studio MCP errors-only and targeted builds passed. Diagnostics `tools/perf/out/us-atlanta-pan-across-sourcedelta-detail-20260621-r1/` and `tools/perf/out/us-atlanta-pan-across-sourceref-detail-20260621-r1/` ran timetable-selected Atlanta `panAcrossZoomLevels`, default/street (`map_source=default`, `street=1`), no video, and passed route proof (`maxDistanceKm=285.8` and `286.2`) as diagnostic-only. They showed both true feed changes and list churn: one sampled rebuild was all identical references in the same order (`srcSame=10454`, `srcAdd=0`, `srcDrop=0`, `srcPos=0`, `srcMove=0`, `srcFresh=0`, `srcRef=10454`, `srcIdxRef=10454`) with `stateBuild=71.07 ms`, but other source rebuilds were real updates (`srcRef=0`, thousands of changed positions/motion/freshness fields, max deltas up to millions of microdegrees). The exactness-preserving candidate treated only same-size/same-order/same-object-reference lists as unchanged and updated the cached list pointer; it was reverted because the quick physical gate `tools/perf/out/us-atlanta-pan-across-sameref-fastpath-detail-20260621-r1/` was not better than the immediate diagnostic comparator (`sourceref` present mean FPS `73.4`, p95 `49.99 ms`, Android jank `43.36%`; candidate present mean FPS `71.9`, p95 `50 ms`, Android jank `41.76%`, p99 worsened to `125 ms`). Logs also showed the candidate run's sampled rebuilds were mostly real source changes, not same-reference churn. Keep the source-change diagnostics for future profiling, but do not accept or retry same-reference dense-symbol source gating unless a future artifact shows repeated same-reference rebuilds in the tail and a cross-region no-detail comparison improves.
- June 21 current-versus-master satellite roads/labels comparison checkpoint, no code keeper by itself: after the user clarified not to compare satellite+roads/labels against default/street mode, the earlier broad `panAcrossZoomLevels` default/street artifacts and the first current close-label attempt `tools/perf/out/compare-master-current-closeSatelliteLabels-atlanta-20260621-r1/` are not satellite roads-on visual evidence because r1 skipped video after Flight Alert left foreground during the video gate. The valid matched-mode comparison used physical Fold, timetable-selected Atlanta, `map_source=SATELLITE`, `MapRoads On`, `MapBorders On`, foreground MP4s, route proof, and road-motion strips (master strips were generated manually from MP4s because the older master harness lacks automatic strip generation). Close satellite labels: current `tools/perf/out/compare-master-current-closeSatelliteLabels-atlanta-20260621-r2/` passed route proof (`samples=61`, `maxDistanceKm=5.5`) and measured produced FPS `121.0`, present mean FPS `40.9`, present p50/p95/p99 `16.66/83.29/133.35 ms`, Android jank `63.64%`; master `C:\Users\h\AndroidStudioProjects\FlightAlert-master-compare\tools\perf\out\compare-master-master-closeSatelliteLabels-atlanta-20260621-r2/` passed route proof (`samples=71`, `maxDistanceKm=17.0`) and measured produced FPS `178.5`, present mean FPS `17.6`, p50/p95/p99 `33.34/166.63/183.35 ms`, Android jank `91.86%`. Video/contact-sheet inspection found both were true satellite+roads/labels runs with no launcher contamination; current's close roads/labels stayed visually steadier, while master showed early label/border/reference loading unevenness that matches its log `labelReq`/`borderReq` spikes. Quick satellite zoom stress: current `tools/perf/out/compare-master-current-quickSatelliteZoom-atlanta-20260621-r1/` passed route proof (`samples=20`, `maxDistanceKm=215.4`) and measured produced FPS `41.6`, present mean FPS `19.2`, p50/p95/p99 `41.66/116.67/133.35 ms`, Android jank `85.20%`; master `C:\Users\h\AndroidStudioProjects\FlightAlert-master-compare\tools\perf\out\compare-master-master-quickSatelliteZoom-atlanta-20260621-r1/` passed route proof (`samples=97`, `maxDistanceKm=113.1`) and measured produced FPS `328.5`, present mean FPS `21.3`, p50/p95/p99 `33.32/141.66/174.98 ms`, Android jank `88.64%`. The master quick run printed Gradle local handshake warnings, but the instrumented test passed and artifacts were pulled. Treat these as useful matched-mode checkpoint evidence, not a broad branch acceptance claim, because the shared test bodies differ between current and master and the close-label sample durations differ; do not port master's looser satellite/reference behavior as a performance gain when the current branch is preserving stricter road/label visual guarantees.
- June 21 same-driver current-versus-master branch benchmark checkpoint, no code keeper by itself: after the user asked why current looked worse than master, log inspection found the separate quick satellite zoom artifacts above were not apples-to-apples branch evidence. The current quick run had traffic work in every sampled debug line (`20/20` nonzero symbols, thousands of symbols/dots), while the master quick run's sampled debug lines had no traffic symbols (`0/15` nonzero), so its produced-FPS lead is diagnostic only and must not be used as a branch verdict. The fair rerun installed the current `androidTest` APK once, then swapped only the target app APK: first current, then read-only master, using the same current-driver `satellitePanZoomSanityPerf` command, timetable-selected Atlanta at UTC `23:23-23:26`, `map_source=SATELLITE`, roads/borders on, full traffic, `videoEvidenceHoldSeconds=60`, human-like overlapping pan/zoom, and FrameTimeline/gfxinfo framestats reset inside the harness. Current artifact `tools/perf/out/fair-current-satellitePanZoomSanity-atlanta-20260621-r1/` passed instrumentation in `70.785 s`, produced road-motion video/strip proof, and measured frames `175`, sample seconds `2.733`, produced FPS `64.0`, present mean FPS `44.9`, present p50/p95/p99 `16.66/58.32/58.33 ms`, Android jank `72.41%`; route/focus proof from debug lines was `samples=71`, average focus drift `7.8 km`, max `109.3 km`. Master artifact `tools/perf/out/fair-master-satellitePanZoomSanity-atlanta-20260621-r1/` used the same driver/test body against the master target APK, passed instrumentation in `72.371 s`, produced road-motion video/strip proof, and measured frames `125`, sample seconds `3.566`, produced FPS `35.1`, present mean FPS `34.3`, present p50/p95/p99 `24.99/74.99/83.33 ms`, Android jank `88.80%`; route/focus proof was `samples=38`, average focus drift `10.5 km`, max `116.7 km`. Road-motion-strip inspection showed both were true satellite+roads+labels runs over the Atlanta/Southeast region, with traffic symbols and the aircraft detail panel visible. Workload counters were comparable enough for this narrow benchmark: current `71/71` nonzero-symbol debug samples, avg symbols `1561.2`, avg dots `1129.3`, avg direct `786.8`, avg traffic draw `6.97 ms`, max traffic `10.40 ms`, label requests `0`; master `38/38` nonzero-symbol samples, avg symbols `1589.1`, avg dots `1165.0`, avg direct `812.7`, avg traffic draw `10.05 ms`, max traffic `17.90 ms`, label requests avg `15.1`, max `63`. Result: for this narrow same-driver regional satellite+roads+labels pan/zoom workload, current is faster/smoother than master on present-frame evidence; it is not a broad pushed-checkpoint acceptance, because accepted branch comparisons still need the multi-band/multi-mode thoroughness rule above.
- Rejected June 21 lazy reference URL construction, reverted from `FlightAlertRewritten.kt`: after Android Studio MCP audited `request_reference_tile`, `ReferenceTileOverlay`, and `request_reference_overlay_prefetch_grid`, a candidate passed `ReferenceTileOverlay` into `request_reference_tile` so the Esri URL string was built inside the worker only after memory, queued-request, stale-request, and disk-cache checks. The theory preserved exact tile identity, request priority, cache files, network URLs, and pixels while avoiding UI-thread URL string construction for already-queued or disk-cached reference requests. Android Studio MCP errors-only/build checks passed, but the same-session Atlanta diagnostic rejected it. Comparator `tools/perf/out/us-atlanta-satellite-current-maptrafficdetail-20260621-1548/` measured produced FPS `61.8`, present mean FPS `49.6`, present p95 `58.33 ms`, Android jank `69.03%`, route proof `maxDistanceKm=112.5`; candidate `tools/perf/out/us-atlanta-satellite-reflazyurl-maptrafficdetail-20260621-1558/` passed route proof (`samples=8`, `maxDistanceKm=83.5`) but measured only mixed/worse frame delivery: produced FPS `65.9`, present mean FPS `48.7`, present p95 `58.33 ms`, Android jank `73.29%`. The detail counters also moved the wrong way: worst sampled `refPrefetch` increased from `24.55 ms` to `37.48 ms` and worst sampled `reference` from `33.45 ms` to `45.21 ms`. Do not retry lazy URL/string-construction or request-admission reshaping as a reference fix unless a future profile proves URL construction itself is the limiter and normal cross-region evidence improves.
- Rejected June 21 reference prefetch-grid throttle, reverted from `FlightAlertRewritten.kt`: after Android Studio MCP audited `request_reference_overlay_prefetch_grid`, `draw_reference_overlay_layers`, and `begin_reference_tile_request_generation`, a candidate added a 120 ms per-grid throttle keyed by overlay, target LOD, buffer, and exact tile range, plus a debug-only `refPfSkip` counter. The intent was to avoid repeated UI-thread enumeration of the same non-visible reference prefetch grid while still admitting visible exact roads/borders/labels every draw and immediately admitting any changed tile range. Android Studio MCP errors-only/build checks passed, and the mechanism did fire (`refPfSkip` commonly `1-6` in sampled detail logs), but the same-session Atlanta diagnostic rejected it. Comparator `tools/perf/out/us-atlanta-satellite-current-maptrafficdetail-20260621-1548/` measured produced FPS `61.8`, present mean FPS `49.6`, present p95 `58.33 ms`, Android jank `69.03%`, route proof `maxDistanceKm=112.5`; candidate `tools/perf/out/us-atlanta-satellite-refpfgrid120-maptrafficdetail-20260621-1612/` passed route proof (`samples=7`, `maxDistanceKm=71.7`) but regressed to produced FPS `51.6`, present mean FPS `41.3`, present p95 `66.67 ms`, Android jank `76.67%`. Detail logs showed some prefetch windows became cheap, but cost moved rather than disappeared: sampled `active_visual_change` direct-symbol work still hit `32.32 ms` (`symbolMask=21.70 ms`) and sampled reference prefetch still reached `18.00 ms`. Do not retry same-grid reference prefetch throttling or non-visible prefetch admission suppression without visual road-motion proof and stronger normal cross-region evidence.
- Rejected June 21 active symbol bridge blended-scale-key candidate, reverted from `FlightAlertRewritten.kt`: after Android Studio MCP audited `draw_aircraft_symbols`, `draw_aircraft_symbols_with_pan_cache`, `symbol_overlay_interaction_visual_bridge_matches`, and `AircraftMarkerMorph`, a candidate added the actual `AircraftMarkerMorph.blended_icon_scale(viewport.zoom, marker_blend)` bucket to `AircraftSymbolOverlayCacheKey` and used that bucket instead of the separate raw icon/dot scale buckets for active bridge matching. The theory was exactness-preserving because the cached symbol overlay draws symbols using blended icon scale plus marker/shape progress, while `draw_internal_dot=false`; separate dot/icon buckets can be a conservative false miss. Android Studio MCP errors-only/build checks passed, but the Atlanta detail gate rejected it. Comparator `tools/perf/out/us-atlanta-satellite-current-maptrafficdetail-20260621-1548/` measured produced FPS `61.8`, present mean FPS `49.6`, present p95 `58.33 ms`, Android jank `69.03%`, route proof `maxDistanceKm=112.5`; candidate `tools/perf/out/us-atlanta-satellite-blendedscale-detail-20260621-1629/` passed route proof (`samples=7`, `maxDistanceKm=114.2`) but regressed to produced FPS `51.1`, present mean FPS `41.4`, present p95 `66.66 ms`, Android jank `83.22%`. The intended active miss was not solved under the existing 120 scale tolerance: the sampled `active_visual_change` still had `blendD=187`, `iconD=156`, `dotD=217`, and direct-drew `1542` aircraft. Do not retry blended-scale bridge matching or widen the active scale tolerance without fresh video plus cross-region evidence proving no symbol-size/morph pop.
- Rejected June 21 reference-memory-cache capacity increase, reverted from `FlightAlertRewritten.kt`: raising `MAX_REFERENCE_MEMORY_TILES` from `768` to `1024` preserved intended road/border/label pixels in theory by retaining more decoded reference tiles, and close satellite diagnostic `tools/perf/out/eu-frankfurt-close-satellite-diagnostic-r2-refcache1024/` improved produced FPS `141.6 -> 171.1`, present mean FPS `57.1 -> 65.8`, and Android jank `48.14% -> 31.69%`, but worsened present p95 `41.67 -> 49.99 ms`. The required regional cross-check `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r38-refcache1024/` crashed the app with a native RenderThread `SIGSEGV` before same-run framestats could be pulled, despite route proof staying over Frankfurt. Do not raise the reference memory-tile cap without a new memory/graphics safety design and clean regional plus close evidence.
- Rejected June 21 broad multi-zoom traffic-cache experiments, all reverted from `FlightAlertRewritten.kt`: (1) shrinking `DENSE_SYMBOL_CACHE_INTERACTION_ZOOM_STEPS` from `3.4` to `0.45` avoided some large scaled-overlay misses but moved cost into coverage misses/rebuilds. Broad Frankfurt `panAcrossZoomLevels` r3 looked mixed (`74.5` present mean FPS, `41.66 ms` p95, `34.56%` jank) and the clean repeat `tools/perf/out/eu-timetable-pan-across-zoom-levels-r5-zoomcap045-clean/` failed hard (`64.3`, `66.67 ms`, `34.71%`) versus restored current `tools/perf/out/eu-timetable-pan-across-zoom-levels-r6-current-restored-clean/` (`72.9`, `49.99 ms`, `36.48%`). (2) storing symbol-overlay recording padding fixed a real geometry bookkeeping concern but produced only partial cache hits and regressed `tools/perf/out/eu-timetable-pan-across-zoom-levels-r4-symbolpad-geometry/` to `72.9` present mean FPS, `49.99 ms` p95, and `35.91%` jank; it was reverted. (3) raising dense dot cache reuse padding improved broad `tools/perf/out/eu-timetable-pan-across-zoom-levels-r7-dotcache120/` (`77.0` present mean FPS, `41.66 ms` p95, `31.21%` jank), but it moved cost into the regional sanity gate: video sanity `tools/perf/out/eu-frankfurt-sanity-video-r8-dotcache120/` stayed on Frankfurt route with a valid 30 s foreground MP4 but measured only `37.8` present mean FPS and `66.67 ms` p95; no-video sanity `tools/perf/out/eu-frankfurt-sanity-novideo-r9-dotcache120/` measured `37.1`, `66.66 ms`, `89.06%` jank, and the milder `90dp` attempt `tools/perf/out/eu-frankfurt-sanity-novideo-r10-dotcache90/` was worse at `36.6`, `83.31 ms`, `88.49%`. Keep `DENSE_SYMBOL_CACHE_INTERACTION_ZOOM_STEPS = 3.4` and `DENSE_DOT_CACHE_MAX_REUSE_DP = 60f`; do not accept broad-run-only cache wins that increase regional point overdraw or move jank into the sanity path.
- Rejected June 21 regional backend micro-optimization reruns, reverted from `FlightAlertRewritten.kt`: (1) carrying precomputed cull radii on `TrafficAircraftOverlayState` and a frame-level cull scale through the direct renderer/async symbol recorder preserved the same cull formula in theory but failed the bounded timetable-selected Frankfurt regional sanity gate. The first custom-artifact attempt `instrumentation-eu-frankfurt-regional100mi-sanity-novideo-r11-cullprecompute-20260621-091020` completed over Frankfurt but was discarded as evidence because the runner used the wrong artifact prefix and pulled no same-run framestats; the corrected run `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r11-cullprecompute/` measured produced FPS `42.1`, present mean FPS `37.9`, present p95 `66.68 ms`, Android jank `86.67%`, route proof `maxDistanceKm=64.4`. (2) Lazily allocating/updating the dense-symbol `seen_keys` set only when selected/extreme fallback could need it mirrored the existing dense-dot pattern, but also failed regional sanity: `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r12-lazyseensymbols/` measured produced FPS `42.5`, present mean FPS `36.8`, present p95 `83.29 ms`, Android jank `83.94%`, route proof `maxDistanceKm=90.0`. Do not retry cull-radius state-shape rewrites or lazy dense-symbol seen-set bookkeeping as regional fixes without a stronger profile and cross-band proof.
- Rejected June 21 active sync-record symbol-overlay experiment, reverted from `FlightAlertRewritten.kt`: a structural "record as draw" fallback rendered the current stable aircraft symbols into the existing exact symbol-overlay bitmap path when the active cache no longer intersected the viewport, then direct-drew only motion/rotorcraft exclusions. It proved the expected direct-count reduction (`sync=true` frames drew about `106-162` direct exclusions instead of thousands), but the UI-thread recording cost was catastrophic: `cacheAttempt` spikes were about `197-238 ms`. Physical Fold timetable-selected Frankfurt `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r13-syncrecorddraw/` measured produced FPS `36.2`, present mean FPS `36.0`, present p95 `100.01 ms`, Android jank `83.69%`, route proof `maxDistanceKm=65.1`. Do not retry synchronous symbol-overlay recording on the UI thread; any future exact overlay-tiling/recording design must move this work off the frame path without causing the previously rejected background-recording contention.
- Rejected June 21 dense-state appearance-frame micro-optimization, reverted from `FlightAlertRewritten.kt`: carrying one elapsed timestamp on `TrafficOverlayFrame` and computing `TrafficAircraftOverlayState.appearance_progress` from the already fetched `AircraftAppearance` avoided one snapshot lookup/clock read per state in theory, but the bounded timetable-selected Frankfurt regional sanity gate did not improve. `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r14-appearanceframe/` measured produced FPS `43.9`, present mean FPS `35.9`, present p95 `58.34 ms`, Android jank `87.16%`, route proof `maxDistanceKm=90.0`, versus the stronger accepted-state regional references around `41.9-42.0` present mean FPS and lower/nearby p95. Detail logs showed the run mostly stayed in shifted-state mode and remained dominated by direct symbol drawing, so do not retry appearance-frame/map-lookup micro-optimizations without a profile proving appearance lookup is the actual limiter.
- Not accepted June 21 Atlanta appearance-time sampling retry, reverted from `FlightAlertRewritten.kt`: after context resumption, a narrower variant removed the duplicate appearance-progress callback lookup and sampled elapsed time once for the dense-symbol rebuild. It compiled and one timetable-selected Atlanta detail run, `tools/perf/out/atlanta-regional-appearance-state-sample-20260621-1138/`, passed route proof (`samples=7`, `maxDistanceKm=72.1`, `accepted_optimizer_evidence=true`) and looked better than the heavy diagnostic (`produced FPS 41.7`, present mean FPS `34.1`, present p95 `75 ms`, Android jank `79.59%`; `symFilter` max `12.82 ms` vs prior diagnostic `27.76 ms`). Do not treat that as an accepted keeper because it is the same optimization family as the rejected Frankfurt appearance-frame/motion-clock experiments, did not prove appearance lookup was the actual limiter, and the same run was still dominated by direct symbol-mask draw (`directSymbols` max `40.08 ms`) plus satellite reference/prefetch spikes. The code was reverted; keep the artifact only as a diagnostic reminder that single-region improvements cannot override stronger rejected regional gates.
- Rejected June 21 symbol-mask LRU capacity bump, reverted from `FlightAlertRewritten.kt`: raising `SYMBOL_MASK_CACHE_MAX_ENTRIES` from `640` to `768` preserved exact mask generation/draw semantics in theory and was checked with Android Studio MCP symbol/build audits, but the physical Fold regional evidence was too weak and unstable to keep. Fresh current comparator after the r14 revert, `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r15-current-after-r14revert/`, measured produced FPS `44.3`, present mean FPS `36.1`, present p95 `66.66 ms`, Android jank `84.03%`, route proof `maxDistanceKm=88.1`. First `768` run `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r16-symbolmaskcache768/` improved only modestly to produced FPS `46.4`, present mean FPS `37.0`, present p95 `66.65 ms`, Android jank `82.31%`, route proof `maxDistanceKm=73.9`, but repeat `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r17-symbolmaskcache768-repeat/` regressed to produced FPS `39.6`, present mean FPS `36.2`, present p95 `74.99 ms`, Android jank `90.15%`, route proof `maxDistanceKm=81.4`. Keep the symbol mask cache cap at `640` unless a future profile proves actual mask LRU churn and a bounded-memory design beats regional plus cross-band evidence.
- Rejected June 21 cached-symbol direct-fallback key-skip shortcut, reverted from `FlightAlertRewritten.kt`: skipping cached aircraft keys before center/motion math when the retained symbol bitmap already covered the direct-fallback envelope did fire (`keySkip=true`, cached about `973`, direct motion/rotorcraft exclusions about `676-722`) and looked exact in the narrow cache-hit path, but it did not beat cross-band evidence. Physical Fold timetable-selected Frankfurt regional detail `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r18-keyskip/` measured produced FPS `55.6`, present mean FPS `43.8`, present p95 `66.67 ms`, Android jank `75.50%`, route proof `maxDistanceKm=55.6`; clean regional repeat `tools/perf/out/eu-frankfurt-regional100mi-sanity-novideo-r19-keyskip-nodetail/` measured `52.9`, `41.2`, `58.34 ms`, and `83.66%`, which was only mixed versus accepted references. The broader timetable multi-zoom check `tools/perf/out/eu-timetable-pan-across-zoom-levels-r20-keyskip/` stayed over Frankfurt (`maxDistanceKm=215.6`) and contact-sheet review showed dense map traffic, but it regressed to present mean FPS `70.1`, p95 `50 ms`, Android jank `33.54%` versus restored-current broad references `r6-current-restored-clean` (`72.9`, `49.99 ms`, `36.48%`) and earlier current `r1` (`76.8`, `49.99 ms`, `32.22%`). Do not retry cached-key skip/center-check removal shortcuts as a regional or broad active-cache fix without new proof.
- June 21 Atlanta satellite/reference timing diagnostics, not code keepers by themselves: physical Fold timetable-selected Atlanta at UTC `17:00`, `tools/perf/out/us-atlanta-satellite-panzoom-mapdetail-refdiag-20260621-1700/`, ran `satellitePanZoomSanityPerf` with roads/borders on, full traffic, no video, and `MapDetailTiming`; route proof passed (`samples=7`, `maxDistanceKm=95.8`) but `accepted_optimizer_evidence=false` only because visible evidence was intentionally unchecked for this no-visual-claim diagnostic. Frame evidence was produced FPS `42.0`, present mean FPS `36.8`, present p95 `74.99 ms`, Android jank `80.74%`. The added timing showed a real blind spot: reference max `50.16 ms` contained `refOther=37.17 ms` after accounting for plan/draw/prefetch/alpha/bookkeeping. Follow-up split `tools/perf/out/us-atlanta-satellite-panzoom-mapdetail-refprotect-20260621-1707/` passed route proof (`samples=5`, `maxDistanceKm=116.3`) but was slower under heavier timing (produced FPS `27.7`, present mean FPS `27.9`, p95 `99.98 ms`, jank `98.15%`); it showed `refProtect` was tiny (max `0.09 ms`) and the real residual was `refGen` from `begin_reference_tile_request_generation()` (max `27.99 ms`). The post-change run `tools/perf/out/us-atlanta-satellite-panzoom-mapdetail-refprune-20260621-1711/` is recorded above as a narrow current-only keeper for the history-prune code path; keep these diagnostic artifacts as the explanation for why that narrow keeper exists, not as broad app-version evidence.
- Rejected June 21 harness evidence: `harness-sanity-frankfurt-human-panzoom-video-r2` and `r3` are not visual evidence because their videos contained launcher/home-screen frames. The old four-band `countryScaleZoomContinuitySatellitePerf` mega-test attempts `eu-frankfurt-multiband-satellite-perf-novideo-r2/r3/r4` are also rejected: r2/r3 hit the 150 s timeout, and r4 ended without usable framestats after repeated relaunches. Root fixes were removing unbounded `waitForIdleSync()` from startup, keeping country-scale as one band, and covering zoom variety through separate bounded commands.
- Rejected June 19 street generation-pruning experiment: a `PriorityBlockingQueue` plus aggressive stale queued-tile removal skipped useful visible tile work and produced `bestof-layer12-street-fastzoomout-priorityqueue-visual-denver-r3cx` with repeated `Loading map` placeholders, about 65.2 present mean FPS, present p95 41.66 ms, and Android jank 27.14%. Do not bring back tile request dropping/pruning unless a future test proves equal visuals and better sustained loading.
- Rejected previous experiments include feed-publish waiting/staging, visual signatures as a restoration shortcut, atlas packing, parallel retained-symbol tiling, low-scale direct vector fallbacks, scale-aware direct masks, redraw-cadence forcing, dot alpha threshold-only changes, and cadence-only traffic publish tuning unless a future branch proves the exact same accepted pixels and better full-frame evidence.

## 17. Security / Privacy

Prefer HTTPS-only APIs and assets. Do not hardcode API keys, tokens, secrets, credentials, personal device IDs, or private test artifacts.

Avoid cleartext traffic unless there is a documented reason. Lock-screen notifications should avoid exposing sensitive aircraft details unnecessarily.

## 18. Release Behavior

Before pushing: build, inspect git status, check for duplicate project files, scan for obvious secrets, update README accurately, and clean generated junk.

Push only scoped, intentional app/source/docs changes. Only push important changes, or changes large enough to add major functionality.

When the user gives a newer concrete rule that supersedes one of these rules, the newer rule takes priority. Update this file so later agents do not preserve obsolete guidance.
